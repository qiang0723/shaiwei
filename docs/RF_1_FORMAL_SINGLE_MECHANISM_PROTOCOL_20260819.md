# RF-1 正式单机制小批协议（2026-08-19 草案，双重门待批准）

- 机器真身：`config/rf_1_formal_single_mechanism_v1.yaml`，SHA-256
  `357975f23a039672bd46d4ba4f77bbf528797f1749980271a7170223ac37afdc`
- 状态：`RESULT_BLIND_FORMAL_PROTOCOL_FROZEN_PENDING_USER_APPROVAL_AND_R2_1_CHECKPOINT`
- 上位链：RF-0A 准备裁决 → RF-0C 预检 `GO_FORMAL_PROTOCOL`（注册表为去重权威）

## 1. 研究问题

中证800 PIT 成员、固定标签（t+1 开盘进、t+11 开盘出）、既有 Alpha158 与行业/市值暴露之外，
**隔夜跳空与日内吸收/反转的联合状态**能否提供稳定、非重复、扣成本的增量信息。裁决权全部
在冻结的 G1 裁判；本协议不预判结果。

## 2. 批合同（全部冻结）

- DeepSeek `deepseek-v4-pro`、至多 8 个完成响应、至多 3 个合格候选、单批熔断 $1.00、
  RF-1 总上限 $2.00、串行调用、失败/重复计入预算不补位；
- 候选合同：D1 安全 AST 子集、字段 open/high/low/close/vwap/volume、**必须同时引用 $open
  与 $close**（机制定义的直接推论）、回看 ≤50 交易日、≤20 token/≤80 AST 节点、规范表达式
  与经济陈述缺一不可；
- 去重第一层：规范 AST 命中 RF-0C 封存注册表即拒；语义盲评、暴露、IC 增量、组合增量四层
  在效果阶段冻结执行；
- 发现期 2016-06-01—2018-12-31（沿用 D1 已验证机器），按 |RankIC| 机械选 Top1/Top2，
  方向只冻结一次，无参数网格；
- Top1/Top2 依次进入 G1 全部冻结门（DSR≥0.95、NW t≥3.0、六窗口、压力期、成本翻倍等）。

## 3. 双重门（本协议批准 ≠ 执行授权）

1. 你批准本协议（现在）；
2. 执行还须同时满足：R2-1 自然前瞻检查点完成（20 日/2 次调仓门）+ 你对该批次的显式授权 +
   冻结执行 release。任一不满足，一贝都不能花。

## 4. 裁决

- G1 PASS：只授权起草独立模拟仓协议提案；
- G1 REJECT 或批耗尽：RF-1 关闭；
- 合同/执行失败：BLOCKED，按既有恢复纪律处理。

协议冻结推送后我停在原地：不动工、不调 LLM、不读任何效果。
