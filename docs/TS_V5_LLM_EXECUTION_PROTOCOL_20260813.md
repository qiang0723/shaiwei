# TS-v5 首批 DeepSeek 机制研究执行协议

日期：2026-08-13（UTC+8）

状态：`USER_APPROVED_ENGINEERING_BEFORE_EXECUTION`

## 1. 审批优先级裁决

用户明确批准机器scope SHA-256
`9947e1bebc10d5da32df63ff462a8c8e9403a12986dbfef0a891f69956325a88`：向DeepSeek串行获取
恰好12个完成响应（6个独立候选与6个反方改版），费用硬上限0.50美元。该逐字批准晚于预算v2，
因此本批恢复由v1的研究范围和发送边界控制；v2只保留“TS-v5总预算上限5美元”的计划背景，不扩大
本批响应数、费用或发送内容。v1、v2均保持字节不变。

## 2. 允许与禁止

允许发送冻结系统提示、严格候选Schema、产品约束、复杂度上限、公开知识摘要、匿名v3/v4失败记忆、
机制/尝试身份，以及同机制合法父候选；若父响应无效，只发送响应哈希与失败类别。禁止证券/名单、
原始行情、持仓/订单/信号、发现收益或风险指标、封存验证/测试、自然前瞻/生产结果、路径和凭据。

本批不读取任何行情或策略效果，不进行参数搜索、回测、模型、模拟仓、Web或生产变更。候选只是待
本地研究的非权威假设；12个完成响应中的解析、Schema或重复失败均计数且不得递补。

## 3. 执行与费用

先固定6个独立请求，再按同机制一一形成6个反方改版；并发固定1。每席位最多3次传输尝试，只对明确
未形成完成响应的连接失败、429、500或503重试；读取超时或悬空STARTED一律按计费不确定停止。每次
请求前以全缓存未命中最坏费用预留，整批超过0.50美元前失败关闭。

官方API的模型版本名和响应`model`字段是两个概念：请求`deepseek-v4-pro`，预期版本
`DeepSeek-V4-Pro`，冻结响应字段为`deepseek-v4-pro`。这一纠正发生在任何付费响应之前。

## 4. 隔离、证据和代码边界

正式执行使用短命、非root、只读根、无端口、1 CPU/512MiB Docker，只传
`DEEPSEEK_API_KEY`，只挂载TS-v5专属输出目录与两份专属追加式账本。运行前先做无secret TLS主机名
探测；独立审计容器断网、只读挂载且不接收密钥。scheduler镜像和容器不得重启或变更。

新增能力拆为传输release校验、批次编排、证据持久化、独立审计四个职责；复用已有候选Schema、提示
构造、DeepSeek传输和账本追加入口。新增生产模块均不超过架构宪法400行软上限，不复制D1/M3研究
业务。本节点先提交、测试并推送实现，再冻结绑定实现提交和镜像的正式release，最后才可读取密钥。
