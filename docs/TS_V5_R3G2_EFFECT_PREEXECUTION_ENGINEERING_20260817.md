# TS-v5 R3G-2 真实效果预执行工程验收（2026-08-17）

## 裁决

`READY_FOR_EXACT_USER_APPROVAL_NOT_EXECUTED`。

本轮只完成结果盲执行器、独立审计器、断网容器、键级预检和唯一 release scope。真实分数值、排名、
入场后行情、H00906 收益和策略效果均未读取；策略效果尝试为 0，批准文件不存在，效果与审计输出根
为空，`strategy_effective=NOT_EVALUATED`，`production_authorization=none`。

## 唯一发布身份

- 效果协议 SHA-256：`c3aa5a2bef199d8745b6e0399085dcf5a60d9f28f29a5426ea0104831f3572bf`。
- 最终实现提交：`3b43b6bdfaf35987ca82e810c04fbb2b15e725bf`，与 `origin/main` 一致。
- 代码快照 SHA-256：`9cc5e40ab3532967e420654f81ac6678cd47f32ff28cc479e734ca4ff0170278`。
- 不可变镜像：`sha256:dbf006ec6b75995ac45ff512693571afd4a05c032c82c725520494328b8a6053`，
  平台 `linux/arm64`，内嵌 Git 提交与最终实现提交一致。
- 镜像发布清单：930 个受控文件，清单 SHA-256
  `8dee08c115b34db50812347c3e76e87bce8f5de6d097058d1df3a237c3e813c8`，快照与宿主逐项一致。
- 唯一未授权 release scope：
  `961b62f288f61a6ae19f88ef04c0697f93f27bf52390ddb48b7c49064e19db75`；scope 文档 SHA-256
  `994a5d710510b48d9f32253c894024fd410d02f66f326bc2f2e1f679254ff075`。

## 结果防火墙与键级预检

- 三个冻结点保持不变：主点 `81833a47...9784`，邻点 `09bceb50...2ea5`、
  `35592634...8076`；首次真实效果读取才合计消费 3 次尝试。
- 发现期固定为 2021—2023；只有发现硬门通过才允许打开 2024—2025 留出期；2026 不读取。
- 键级预检报告 SHA-256：
  `3cc735eeeb7f75e106330c69545ffded42d4de3be2438f08465ebc20903114d0`。
- 发现期 727 个交易日，362 个重建且可排序事件；留出期 485 个交易日，358 个重建事件、352 个
  通过分区末退出可调度门。三个点在两个分区的分数键覆盖率均为 100%。
- 同一入口在最终镜像内复跑得到 `reused=true` 和相同报告哈希；分数值、入场后收益和基准值读取均为
  `false`，效果尝试为 0。

## 执行与审计边界

- runner 只允许断网、只读根、非 root、无 Docker socket、无 `.env`、无完整项目根、无生产账本，
  并执行 first pass + replay；auditor 使用独立入口，不挂行情、分数或基准源。
- 只读挂载两份已冻结 TS-R3F 研究谱系账本及 160 KiB 专用证据目录；未挂载整个 `ledger/`，scope
  显式区分“冻结研究谱系账本”与“生产账本”。
- DuckDB 一次性研究容器按实测把临时空间从 4 GiB 调整为 6 GiB、容器上限从 12 GiB 调整为
  14 GiB；Docker 总上限约 19.3 GiB。该资源只在短命研究容器运行时占用。
- scheduler 保持施工前同一容器 `183b8c6c5edd`、同一镜像
  `sha256:722f63de15932cb2698e82db7c0140f02921f27f126e1c0235ca62e0a1213b76`、同一创建时间
  `2026-08-03 17:39:34 +0800`，状态 `healthy`，未重启。

## 失败关闭与修复留痕

1. 首个镜像候选误嵌入错误完整 Git 提交，身份门在 scope、批准、预检和效果读取前拒绝；候选镜像
   `sha256:cc14f656...6cbbb`、失败回执和候选清单保留在 Git 忽略证据目录。
2. 干净镜像测试发现协议元数据加载和 release 文档纯组装函数隐式依赖宿主数据；现已分离纯元数据、
   显式已验证哈希和真实挂载校验。干净镜像不挂宿主数据仍可运行全部合成测试。
3. 首次真实键级预检先后在 4 GiB 临时空间和缺少冻结 R3F 谱系证据处失败；两次均发生在分数值或
   收益读取前。资源与精确只读挂载修复后，相同报告幂等 GO。

上述失败均未创建 approval、release scope 或正式效果产物，未消费三个效果尝试，也不构成策略失败。

## 验证

- 本机定向 TS/协议/架构测试 26 PASS；全仓 `1371 PASS`，仅 17 条已知第三方/旧 M7 warning。
- 最终镜像在断网、只读、无宿主数据挂载条件下 23 PASS。
- Ruff、compileall、pip check、Compose 解析、diff-check、受控文件清单和宿主/镜像哈希门全部 PASS。
- R3G-2 生产模块按输入、订单、执行、费用、指标、产物、控制、runner、auditor 和 fixture 分层；13 个
  `effect_*.py` 模块共 2,634 行，最大文件 400 行，没有新增不可维护单体。

## 下一合法动作

只有用户逐字批准 scope
`961b62f288f61a6ae19f88ef04c0697f93f27bf52390ddb48b7c49064e19db75` 后，才可运行一次动作
`TS_R3G2_BREAKOUT_RETEST_EFFECT_ONCE_WITH_DISCOVERY_FIREWALL_REPLAY_AND_INDEPENDENT_AUDIT`。
同 scope 不得重跑；发现期失败不得读取留出期；不授权外网、2026、额外参数点、重新训练、模拟仓、
Web 或生产。
