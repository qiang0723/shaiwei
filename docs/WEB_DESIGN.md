# 筛微专业只读 Web 设计草案

> 状态：v0.6，2026-07-22。模拟仓首批只读查询已实现；当前继续冻结信息架构与原型，不启动 Web 服务、不改变生产口径。

## 1. 产品任务

Web 首版不追求“展示很多数据”，而是让使用者依次回答三个问题：

1. **数据可信吗**：最新有效交易日、采集状态、门禁、异常、覆盖与证据链是否完整。
2. **策略有效吗**：相对基准、扣除成本后的收益、回撤、稳定性和当前模型版本是否成立。
3. **今天要行动吗**：是否调仓、最新信号、可成交性、风险与告警是什么。

## 2. 信息架构

### 总览

- 顶部结论条：系统状态、数据截止日、最新成功任务、当前告警数量。
- 今日行动卡：是否调仓、信号生成时间、目标持仓数量、次日执行提示。
- 组合结果：策略与基准净值、扣费超额、回撤、换手；收益不得脱离基准和成本单独展示。
- 证据摘要：当前代码/数据/模型/信号哈希与验收进度，可进入对应详情。

### 股票池与信号

- 当前股票池、入选/排除原因、排名、目标权重、当前权重和预计换手。
- 流动性、停牌、涨跌停、ST、上市状态及可成交性提示。
- 因子贡献仅展示已有确定性归因；无法可靠归因时明确显示“暂无可审计贡献分解”。

### 模型与回测

- 实验版本、数据窗口、训练/验证/OOS 划分、参数、模型与产物哈希。
- 策略/基准净值、成本情景、年度结果、最大回撤、换手及多窗口稳定性。
- PASS/REJECT/NOT_READY 结论与失败原因，不隐藏失败实验。

### 模拟仓

- 分列模型目标权重、实际成交持仓、现金和未成交订单，不用目标权重冒充当前持仓。
- 展示扣费净值、中证800净超额、最大回撤、换手、成交率、开盘偏差、现金拖累和个股贡献。
- 从组合结果下钻到信号、订单、成交、行情批次、费用、持仓变化和净值账本。
- 首版仅展示 `model_baseline`；未来 `llm_judgment` 必须作为独立账户并排比较，禁止合并业绩。

### 数据质量

- 数据源、最新批次、覆盖率、缺失、重复键、异常、发布时间和 `.BJ` 排除状态。
- 从门禁结果下钻到批次、账本行、文件路径和 SHA-256；页面只读，不提供修数入口。

### 系统运行

- Docker 服务、scheduler 最近运行、日增量/影子步骤、飞书投递和异常恢复记录。
- 日志默认结构化摘要；原始日志按需下钻，并对密钥、token、Webhook 和签名做硬脱敏。

## 3. 首批指标草案

| 决策区 | 指标 | 必须同时展示 | 证据来源候选 |
|---|---|---|---|
| 数据可信 | 最新有效交易日 | 任务状态、门禁状态、数据快照哈希 | 日运行账本、哨兵报告 |
| 数据可信 | 原始批次数/行数 | `.BJ` 行数、重哈希结果 | ingest ledger、原始批次 |
| 策略有效 | 扣成本净超额 | 基准、成本、最大回撤、窗口 | 实验账本、回测产物 |
| 策略有效 | 当前模型状态 | 代码/数据/模型哈希、生成时间 | 影子运行账本、manifest |
| 今日行动 | 是否调仓 | 上次/下次调仓日、目标持仓数 | 信号 manifest |
| 今日行动 | 次日可成交率 | 换手、开盘偏差、预计成本 | 对账账本 |
| 模拟结果 | 扣成本净超额 | 中证800、净值、最大回撤、现金和时间范围 | 模拟仓净值/持仓账本 |
| 模拟执行 | 实际成交率 | 订单数、未成交原因、开盘偏差、费用和现金拖累 | 模拟仓订单/成交账本 |
| 系统运行 | 最近一次完整周期 | 开始/完成、耗时、失败步骤 | 日运行账本、通知记录 |

所有指标在代码施工前还须补齐：机器名、业务定义、时间口径、单位、空值语义、状态枚举、来源字段和下钻主键。

### 真实证据核对后的口径约束

2026-07-20 首次用真实非调仓日对账复核页面口径，冻结以下设计约束：

- **正式验收进度不能直接显示 `forward_report.json` 的 `reconciled_trade_days` 或 `trial_ready`**。汇总包含不同代码快照的先导证据；页面必须以 `signal_sha256` 关联影子运行与对账账本，再按受控 `code_snapshot_sha256` 和预定交易日对过滤。
- **次日可成交率的分母只包含 `delta_weight != 0` 的实际交易腿**。非调仓日 `trade_count=0` 时显示 `NOT_APPLICABLE`，不得把行级行情可观察或历史累计比率显示为“当日 100% 可成交”。
- 非调仓日仍展示目标持仓的平均绝对开盘偏差，并紧邻展示定义 `official_next_open / signal_day_close - 1`；换手和预计成本正确显示为 0。
- “整日任务批次”和“当日新增原始批次”分开命名。前者只统计市场补采计划，后者还可能包含 `stock_basic` 刷新，禁止用同一个“批次数”标签混用。
- 首页状态只能来自完整证据连接：日运行 PASS → 同数据/代码快照门禁 → qlib/模型/信号哈希 → 次日对账；任一连接缺失都不能显示整体 PASS。
- 任务状态与通知投递状态必须分列。核心任务 PASS 但飞书投递失败时，任务仍按冻结语义 PASS，页面整体至少显示 `WARN`，并保留失败事件、错误类型和后续恢复证据。
- `recovery_count` 只代表其机器定义覆盖的任务恢复，不能代替通知通道恢复判断；通知恢复须按同一通道后续 PASS 事件单独呈现。

### 状态机草案

| 状态 | 机器含义 | 首页表现 | 是否需要行动 |
|---|---|---|---|
| `FAIL` | 必需步骤失败、哈希不一致或证据损坏 | 红色结论条并显示首个失败步骤 | 是 |
| `STALE` | 已过数据时钟截止但没有最新完整快照 | 橙色结论条，显示滞后交易日 | 是 |
| `WARN` | 主流程可用但存在非阻断异常或通知失败 | 黄色提示，分列任务 PASS 与通道异常 | 视情况 |
| `NOT_READY` | 观察期、窗口或正式验收次数未满 | 中性进度状态，不冒充失败 | 否 |
| `NOT_APPLICABLE` | 当前场景没有分母或该检查不适用 | 显示“不适用”及原因 | 否 |
| `NO_DATA` | 查询范围没有登记证据 | 显示空状态，禁止补零 | 视数据时钟 |
| `PASS` | 同一快照的全部必需证据完整且通过 | 绿色但克制，展示截止时间和哈希入口 | 否 |

状态优先级为 `FAIL > STALE > WARN > NOT_READY > PASS`；单项 `NOT_APPLICABLE`/`NO_DATA` 是否影响整体状态，由该指标是否属于当前场景的必需项决定，不能由前端自行推断。

## 4. 查询契约草案

首版只读接口按页面任务拆分，不让前端直接扫描 Parquet 或自由拼接生产 SQL：

- `overview_snapshot`：指定 `as_of` 的首页一致性快照。
- `latest_signal`：最新信号、调仓状态和持仓明细引用。
- `shadow_reconciliation`：信号对应的次日成交与成本对账。
- `paper_portfolio_snapshot`：指定账户和 `as_of` 的实际持仓、现金、净资产、累计成本与证据身份。
- `paper_orders_fills`：信号对应的订单、成交、未成交原因、价格、数量、费用和持仓变化。
- `paper_nav_series`：模拟仓、中证800和扣费净超额时间序列，以及回撤、换手和现金拖累。
- `experiment_summary`：指定实验的版本、窗口、指标、判决和产物引用。
- `data_quality_summary`：指定交易日的批次、覆盖、异常和门禁。
- `system_run_summary`：任务、步骤、通知和恢复记录。
- `notification_delivery_summary`：按 `message_id` 聚合同一逻辑消息的逐次投递，展示尝试次数、最终状态、错误类型、是否重试恢复、重复风险与通道健康度。

每个响应都应携带 `as_of`、`generated_at`、`source_refs`、`evidence_hashes` 和 `freshness_status`；若来源之间不一致，接口失败或显式返回不一致状态，不静默拼成一个数字。

`overview_snapshot` 还必须携带 `controlled_code_snapshot` 和 `acceptance_scope`；`shadow_reconciliation` 必须同时返回 `trade_count`、可成交率分子/分母及 `metric_status`，不能只返回一个容易误读的百分比。

`system_run_summary` 中的 `task_status` 与 `notification_status` 不得合并为同一字段；一次通知失败后，只能由同一 `message_id` 的后续 PASS 尝试或后续真实事件形成恢复证据，不能覆盖原失败记录，也不能把人工补发伪装成原始事件成功。通知明细至少返回 `message_id`、`attempt`、`max_attempts`、`retryable`、`recovered`；飞书无服务端恰好一次承诺，超时重试产生的同 ID 重复消息必须显式标注风险，不能由前端静默折叠为“只投递一次”。

模拟仓三组接口必须携带 `account_id`、`execution_policy_version` 和相关信号/行情/账本哈希。`paper_portfolio_snapshot` 的当前持仓只能由已登记成交与公司行为重放得出；`paper_nav_series` 不得用目标权重或回测净值填补正式模拟仓缺口。

2026-07-22 实现状态：`src/shaiwei/paper/query.py` 已提供 `snapshot`、`orders`、`nav` 三个只读入口，对应上述 `paper_portfolio_snapshot`、`paper_orders_fills`、`paper_nav_series`。Docker 回放验证其账户、策略版本、来源引用、证据哈希、新鲜度和 `BACKFILL/FORWARD` 状态可稳定返回；其余六组页面查询仍为草案，前端不得绕过这些入口直接扫描账本或 Parquet。

## 5. 总览低保真线框

```text
┌ 系统结论：PASS / STALE / FAIL ─ 数据截至：YYYY-MM-DD HH:mm ─ 告警：N ┐
├ 今日行动 ───────────┬ 当前验收 ───────────┬ 证据身份 ─────────────┤
│ 是否调仓 / 目标数量 │ 当前版本 1/3 / 下一节点 │ 代码·数据·模型·信号哈希 │
├ 策略 vs 基准（含成本）──────────────────┬ 风险与回撤 ──────────┤
│ 净值、净超额、成本情景、时间范围          │ 最大回撤、换手、集中度 │
├ 最新信号与次日对账 ─────────────────────┴──────────────────────┤
│ 信号时间｜调仓状态｜交易腿｜可成交率/不适用｜开盘偏差｜预计成本 │
├ 数据质量与运行时间线 ─────────────────────────────────────────┤
│ 批次/行数/.BJ/门禁 → qlib → 模型 → 信号 → 飞书状态 → 次日对账   │
└ 每张卡均可下钻到账本主键、不可变产物与 SHA-256 ───────────────┘
```

## 6. 视觉与交互原则

- 桌面优先、响应式查看；中性克制的专业金融风格。
- 首页先结论、异常和数据时点，再展示趋势；正常状态不喧宾夺主，异常必须可见。
- 颜色不作为唯一状态编码；PASS/FAIL/WARN/NOT_READY 同时使用文字与图标。
- 图表默认显示基准、成本口径和时间范围；缩放或筛选后仍保留口径提示。
- 参考 Grafana、MLflow、TradingView、Ant Design 的信息架构与交互，不复制品牌视觉。

## 7. 近期迭代顺序

1. [x] 核对现有账本和不可变产物能够支持哪些首页指标，删除或限定无法追溯的展示项。
2. [x] 形成首版指标口径约束和状态机草案，明确 PASS/FAIL/WARN/NOT_READY/NOT_APPLICABLE/STALE/NO_DATA。
3. [x] 形成总览低保真线框；已补任务/通知分离、恢复语义和模拟仓结果入口，下一步补股票池与信号、模拟仓，再补模型、数据质量和系统运行页。
4. [x] 冻结 P0.5 模拟仓首批查询契约和下钻主键，并以四日 BACKFILL 验证。
5. 评审技术栈、模拟仓/股票池页面线框和 Docker 部署边界；Web 施工另立目标，且不得影响 P0.5 首个自然 `FORWARD` 验收。

## 8. 设计协作与工具边界

Web 设计采用“一名口径裁判 + 四类专项能力”的协作方式，不让任何视觉工具反向定义量化指标：

| 角色/能力 | 负责 | 不负责 |
|---|---|---|
| 主线口径裁判 | 指标定义、数据时钟、状态机、证据链、验收结论 | 追求视觉效果而改统计口径 |
| Dashboard Architecture | 信息优先级、总览到下钻、筛选器、页面任务和指标分层 | 自行创造后端没有的指标 |
| Quant Visualization | 净值/基准/超额、回撤、RankIC/ICIR、成本、换手、暴露和稳定性图表 | 用装饰性图表替代可审计数字 |
| Figma UI/UX | 栅格、导航、组件、字体、色彩、间距、交互和高保真原型 | 保存真实行情、账本、密钥或运行证据 |
| Browser QA | 主流产品范式核对、响应式、空状态、异常状态、交互和视觉回归 | 绕过查询契约直接读取生产数据 |
| Frontend Implementation | 按冻结契约和原型实现只读页面 | P0.5 首批查询契约稳定前介入代码施工 |

### 固定工作流

1. 主线先冻结指标及证据契约。
2. Dashboard Architecture 形成页面任务、信息层级和低保真线框。
3. Quant Visualization 为每张图明确问题、数据、分母、基准、成本和空值语义。
4. Figma UI/UX 形成组件规范和高保真原型，交回主线复核口径。
5. P0 与查询契约闸门通过后，前端实现；Browser QA 对真实页面做验收。

### 前端候选工具

- Ant Design：导航、布局、表格、筛选、状态、时间线和详情组件。
- Ant Design Charts：净值、回撤、分布、热力图和常规对比；若定制要求超出能力，再评审 Apache ECharts，首版不同时引入两套通用图表引擎。
- TradingView Lightweight Charts：仅用于 K 线、价格、成交量和事件标记；公开使用时遵守其归属要求。
- Grafana、MLflow 和 TradingView 只作为信息架构与交互范式参考，不复制品牌视觉，也不以它们替代筛微证据契约。

Figma 等云端工具只允许使用无敏感信息的示例数据和结构稿；可迁移的设计说明、组件清单、导出图和最终决策必须回存 `docs/` 或后续仓库内设计目录，本地 Git 仍是设计真身。
