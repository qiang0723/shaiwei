# TS-1A-R4 回调状态与全收益基准预检协议

日期：2026-08-12（UTC+8）

状态：`RESULT_BLIND_PROTOCOL_FROZEN_NOT_EXECUTED`

## 1. 研究问题与边界

本节点只回答两个问题：现有不可变输入能否形成真正的“先回调触达、再恢复确认、次日合法开盘”事件；
项目内是否已经存在覆盖 2019—2024 的可审计中证800全收益指数 H00906。它不回答策略是否赚钱。

允许读取行情、PIT成员/行业、市值、交易日历、指数和 Alpha158 的事件键；只允许读取候选下一开盘来
判断订单是否合法。禁止读取入场后的收益、MAE/MFE、Alpha158分数/排名，禁止训练、预测、回测、
联网、读取密钥、创建模拟账户、修改 Web 或生产。

## 2. 唯一入场状态

每周首个官方开盘前，只用上一完整周及更早数据冻结计划：

- 周锚点为上一完整周复权 VWAP，回调线固定为锚点下 4%；
- 初始结构止损为上一完整周复权低点下浮 2%；
- 最近连续两个月高点提高，最近完整月低点不得低于前月低点 5%以上；
- 最近三根完整周K低点不下移，最新完整周收盘位于该周中点及以上；
- 最近两根完整周的日收益年化波动率各在 20%—120%，周振幅各不超过 30%；
- 总市值不低于200亿元、上一完整周成交额不低于50亿元；单日30亿元只作解释，不参与门槛或排序。

当日市场、所属板块和热点行业门通过，且调整价最低价触达周锚点下 4%，状态进入
`PULLBACK_TOUCHED`。同一完整日若同时满足收盘高于前一有效交易日高点且收盘高于开盘，可以直接
`RECOVERY_CONFIRMED`；否则只在同一周等待。确认前收盘不高于结构止损则本周永久失效。

每只股票每个周计划最多一次确认。确认后只检查唯一下一官方交易日：调整因子须与确认日相同，且
开盘在“结构止损以上、周锚点及以下”，止损距离严格小于15%。因子变化但缺少单独公司行为谱系、
停牌/缺开盘、跳空越界或止损距离等于/超过15%均拒绝；不得用后来一天重试。

## 3. 结果前样本门

为避免看到数量后降低标准，评估范围和最低门槛现在冻结：

- 只以 2019—2024 六个自然年作未来权威历史效果范围；
- 真正合法事件合计至少60条，至少覆盖40个不同信号日；
- 六个年度每年不少于3条，至少四个年度各不少于8条；
- 真正事件在冻结 Alpha158 OOS 缓存中的键覆盖必须为100%。

任何一项不足均为 `STOP_INSUFFICIENT_TRUE_EVENTS`，不得降低4%回调、放宽15%止损或删除年度门救回。
2016—2018和2025—2026只报告结构数量，不参与最低门或策略效果。

## 4. 全收益基准预检

未来主超额基准必须是可审计的中证800全收益指数 H00906，覆盖2019—2024。当前节点只检查项目内
既有 manifest 和不可变数据，不联网。若不存在或身份未绑定，返回 `BLOCKED_BENCHMARK_DATA`；
`000906.SH`价格指数不能替代，也不得在本地临时拼接股息代理。

事件门和基准门分开裁决：事件门通过只能说明状态机有足够样本；基准缺失仍会阻断真实效果，但不应
抹掉已经取得的合法事件证据。

## 5. 未来退出合同

本节点只为下一 synthetic 工程冻结退出语义，不执行退出：第一批真实成交价与初始结构止损之差为
`R0`；新一完整周形成后，止损更新为“当前止损与该周低点×0.98的较高者”，从下一官方交易日生效；
止盈固定为`min(第一批成交价+1.5×R0, 第一批成交价×1.20)`；第二批不重置止损、止盈或15日时钟。
所有退出继续服从T+1、停牌、跌停、容量和真实开盘语义。

## 6. 交付与停止条件

协议提交并推送后，才允许实现完全合成的序列对抗 fixture，并运行恰好一次断网真实匿名画像和一次
独立复算。真实报告不得包含证券代码、名称或任何效果字段。R4完成后仍不得直接回测；只有事件门通过
且 H00906 谱系闭合，才能另立完整 TS-1B 效果协议。

机器真身：`config/ts_v3_pullback_state_preflight_v1.yaml`。
