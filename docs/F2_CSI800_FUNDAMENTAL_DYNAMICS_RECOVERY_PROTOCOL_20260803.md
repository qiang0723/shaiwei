# F2-0R 中证800基本面动态数据门恢复协议（结果已知、运行前冻结）

冻结时间：2026-08-03 13:36:28（UTC+8）。机器真身为
`config/f2_csi800_fundamental_dynamics_recovery_v2.yaml`。

## 1. 不是盲预注册

F2-0 已知结果为权威 NO-GO：101,600 行中，质量期仅
`2018-05-31 / 000939.SZ` 没有截至形成日可得的严格连续年度三表共同对；六项总覆盖率和最差形成日
覆盖率均已通过结果前冻结的 85%/75% 门。F2-0 的协议、NO-GO、tracked/ignored manifest、质量报告
和特征面板永久保留，不删除、不重写，也不表述为 F2 效果失败。

## 2. 唯一恢复变更

F2-0R 只纠正一个裁决语义：

- 旧规则要求质量期“无连续年度对”的行数严格等于 0；
- 新规则要求这种合法不可估计行保持整行特征空值，禁止任何补造，然后由 F2-0 已经冻结的六项总覆盖
  85%和最差形成日覆盖75%判断数据是否足以进入下一阶段。

恢复协议不允许对已知证券或日期建立白名单，不允许把门槛写成“最多1行”，也不允许修改公式、PIT、
样本、日期、覆盖阈值或数据源。非法混期、跨年桥接、未来倒灌、当前成分回填、插值、非连续年度对、
`.BJ`、重复键和来源冲突继续零容忍。

## 3. 空值与配对硬门

每行只有两种合法状态：

1. 配对存在：当前期和前一期年末必须同时存在且精确相隔一年，所有组件公告可用日不晚于形成日；
2. 配对不存在：联合可用日必须为空，六项特征必须全部为空，不得从更晚公告、当前成分、相邻非连续
   年份或单表数据补值。

`quality_no_consecutive_pair_rows` 继续完整报告，但不再单独设置固定数量阈值。它对可用性的影响只能通过
原 85%/75% 覆盖门体现。

## 4. 不变边界

六项公式、方向登记、分母规则、中证800 PIT 成员、2016-01-01—2026-07-31范围、2018-05-02质量
起点、月末形成和三表可用日规则全部逐字段复用 F2-0。仍禁止查看标签、IC、收益、回撤、排名、持仓，
禁止训练、回测、DeepSeek、信号、模拟仓、Web或生产接入。

F1-1 六项静态水平候选的 REJECT 和 6 次尝试继续绑定。F2-0/F2-0R 都不计效果尝试；只有未来真的读取
F2 六项效果时才新增 6 次，并与 F1 合并披露累计 12 次。

## 5. 终态与证据

全部恢复门通过只能裁为 `GO_F2_FUNDAMENTAL_DYNAMICS_RECOVERY_DATA_FEATURE_GATE_ONLY`，仅授权
以后另立 F2-1 结果前效果协议；任一门失败即
`NO_GO_F2_FUNDAMENTAL_DYNAMICS_RECOVERY_DATA_FEATURE_GATE`。两者均保持
`strategy_effective=NOT_EVALUATED`、生产授权 `none`。

实现与运行必须使用独立输出目录，先提交推送实现身份，再以断网、只读根、唯一 F2-0R 可写目录的
Docker 镜像串行双跑。两遍报告、面板和 manifest 必须哈希一致，且 F2-0 五项绑定证据和 F1-1 tracked
manifest 哈希均不得变化。
