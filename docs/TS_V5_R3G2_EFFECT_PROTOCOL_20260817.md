# TS-v5 R3G-2 突破回踩真实效果协议

日期：2026-08-17（UTC+8）

状态：`RESULT_BLIND_EFFECT_PROTOCOL_FROZEN_PENDING_ENGINEERING_RELEASE`

配置真身：`config/ts_v5_r3g2_effect_v1.yaml`
配置 SHA-256：`c3aa5a2bef199d8745b6e0399085dcf5a60d9f28f29a5426ea0104831f3572bf`

策略效果：`NOT_EVALUATED`
生产授权：`none`

## 1. 本节点回答什么

R3G-1 已在不读取收益的前提下，从六种机制、431 个参数点中只保留事件密度足够的“突破后回踩”，
并机械锁定一个主点和两个相邻敏感性点；H00906 中证800全收益基准谱系也已闭合。现在把下一次历史
效果检验的输入、执行、成本、风险和停止规则全部写死，避免看完结果再修改。

本节点不训练 W7、不读取 Alpha158 数值或收益、不运行回测。即使未来历史门通过，也只得到
`GO_TS_V5_R3G2_HISTORICAL_REVIEW_ONLY`，还要另立自然前瞻协议，不能直接进入模拟仓或生产。

## 2. 唯一三个效果点

- 主点：4周突破回看、1.5 ATR回踩容忍、最多等待10日、1日确认；哈希`81833a47...9784`。
- 邻居一：只把确认改为2日；哈希`09bceb50...2ea5`。
- 邻居二：只把回踩容忍改为0.8 ATR；哈希`35592634...8076`。

三个点各自运行独立账户，首次读取任何效果即合计登记3次策略效果尝试。年度、成本场景和指标不是
额外尝试；稀疏、阻断或失败也不能从尝试数中删除。邻居只能证明局部稳定性，不能替换主点成为赢家，
不得新增第四点、第三邻居、其他机制或事后调参。

## 3. 时间角色与防泄漏

- 2021-01-04—2023-12-29：发现效果期。R3G-1已用这里的事件密度选点，但从未看收益。
- 2024-01-02—2025-12-31：条件留出期。只有发现期全部门通过后才能读取，账户重新以50万元开始。
- 2026-01-05—2026-08-11：部分年度监控。没有事先冻结的W8模型谱系，因此不读分数、不读收益，
  不选参、不裁决，也不允许把W7分数沿用到2026。
- 2019—2020只作为模型训练/前置周分数和基准兼容历史，不重新进入本次TS选择或效果结论。

每个分区尾部先删除计划最迟退出会越界的信号；如果停牌或跌停导致合法卖出仍跨越分区，则整个效果
节点失败关闭，不能借下一区间价格补完上一分区收益。

## 4. Alpha158排序谱系

旧P1缓存`46a24aad...c38a`产生于M6标签成熟度纠错之前，虽覆盖2019—2024，但不再是干净比较真身，
本协议明确禁止使用。新口径为：

1. 复用M6已按t+11标签成熟度清洗并独立审计的`clean_lgbm_control_v1` W2—W6预测；
2. 2025只新增W7，训练2022-01-01—2024-06-30、验证2024-07-01—2024-12-31，分别剔除末端11个
   信号日，handler只拟合到2024-06-13；模型、Alpha158处理器、seed和LightGBM参数逐项继承M6；
3. W7谱系阶段只生成分数与覆盖，不计算RankIC、模型优劣或组合收益；必须双遍物理一致；
4. 每个信号只用其ISO周周一之前最后一个官方交易日的分数，降序排列，同分按代码升序；缺分即剔除，
   不用当日分数、当前模型或其他窗口补洞；每个参数点、每个效果分区覆盖至少95%。

这样既保留Alpha158“合格事件后的排序”职责，也不把已知标签泄漏或历史回填带入TS。

## 5. 账户与买卖规则

三个账户各以50万元开始，最多7只、单票买入后不超过10%、总买入后不超过70%、行业不超过30%，
不够合格票就持有现金。每票最多两批、每批最多5%；首批风险不超过权益0.25%，两批合计不超过0.5%，
组合新开仓后总止损风险不超过3%。买入按100股向下取整，单边成交额不得超过此前20日中位成交额5%。

具体时钟：

1. 确认信号在收盘后形成；通过排序、现金、行业、风险、容量和交易门后，只在下一合法开盘买首批，
   失败不追单。
2. 第二批只在首批盈利、已完成收盘价同时高于冻结突破线和首批价格时获得一次机会；机会只存在于
   首批后5个交易日，下一开盘仍须通过全部门，失败不重试，禁止越跌越补。
3. 初始止损为事件建臂时最近完整周低点下浮2%；随后只取“原止损”和“最新完整周低点下浮2%”的
   较高者。收盘触发后在下一可卖开盘退出，停牌/跌停则保留卖单直到合法成交。
4. 止盈为“首批价上方1.5倍初始风险距离”和“首批价上方20%”两者较近者；高开越过目标按开盘卖，
   日内触达且满足T+1则按预置目标卖。
5. 首批日年龄记0，在第15个后续官方交易日开盘强制退出；风险/时间退出优先，其后才处理加仓和新仓。

原始OHLC、成交量、涨跌停和tick只负责可成交、容量、份额与费用；结构判断和持有期经济收益使用复权
等价价格，避免除权除息制造假亏损。该结果是研究经济NAV，不冒充券商逐笔法定现金账；公司行动重叠
交易必须单独披露，未来仍需自然模拟账户验证。

## 6. 成本、指标与裁决

基础成本逐笔应用佣金双边3bp且最低5元、过户费按2022-04-29前后0.2/0.1bp、卖出印花税按
2023-08-28前后10/5bp。另跑两套压力：全部佣金/最低佣金/法定费用2倍，以及基础成本外买卖各增加
10bp不利滑点。任何场景都必须走各自现金和整数股路径，不用结果后线性扣减。

主点发现期硬门：不少于30笔闭合交易、每年不少于5笔，合并净收益和H00906净超额均为正，三年至少
两年净超额为正，最大回撤不超过20%，两套成本压力下净收益不为负；再以三个点的日净主动收益作为
同一族，试验数固定3，复用G1的DSR实现且主点概率至少0.95。两个邻居各须不少于15笔、每年不少于
3笔，并同时通过正收益、正超额、20%回撤和两套成本门。任一失败即
`REJECT_TS_V5_R3G2_DISCOVERY`，留出收益保持未读。

发现期通过后才看留出期：主点不少于15笔且每年不少于5笔，合并净收益和净超额为正，两年至少一年
净超额为正、单年净收益不低于-5%、回撤不超过20%、两套成本压力不为负；至少一个邻居还须通过较轻
的10笔/每年3笔及相同收益、超额、成本、回撤门。失败为`REJECT_TS_V5_R3G2_HOLDOUT`；全部通过才是
历史复核GO。

## 7. 下一施工边界

下一节点只施工断网、短生命周期的W7分数谱系与效果执行/审计工程：输入只读、输出write-once、runner
与auditor分离、一次first pass加一次确定性replay，scheduler不得重启或改变。完成无收益fixture和
身份门后，必须形成绑定Git、镜像、输入和输出空目录的唯一release scope，并由用户明确批准，才可首次
读取三个点的真实效果。
