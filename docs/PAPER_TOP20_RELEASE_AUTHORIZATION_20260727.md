# Top20 模拟账户生产调度授权

> 授权日：2026-07-27（Asia/Shanghai）
>
> 授权范围：`PAPER_ONLY_AUTHORIZED`
>
> 策略有效性：`NOT_EVALUATED`

## 1. 用户裁决

2026-07-27，用户确认继续保留现有 Tushare 与飞书凭据，并明确授权发布 Top20 模拟账户。此前完整
Docker 元数据曾在交互式工具输出中包含容器环境变量；没有证据表明凭据进入 Git、项目文档、账本、
运行日志或第三方公开位置。用户知悉该来源与 GitHub 仓库是否私有不是同一风险面，并接受不轮换的
剩余风险。

本裁决只解除 `PAPER_TOP20_ENGINEERING_ACCEPTANCE_20260726.md` 中的凭据轮换阻断，不降低以下
安全要求：

- 凭据继续只存在于被 Git 忽略的项目 `.env`；
- 禁止在提交、日志、文档、告警或工具输出中打印明文凭据；
- Docker 身份检查只请求镜像、只读根、挂载和运行时代码身份等定向非敏感字段；
- 不再请求或输出完整容器元数据，也不请求容器环境变量。

## 2. 发布边界

授权文件为 `config/paper_top20_release_v1.yaml`，必须绑定冻结的
`config/paper_top20_v1.yaml` SHA-256：
`d09a767ce75ce8ad95c5e2350ab410501cbd8a4d790498731553cadaefbca37e`。

发布后 scheduler 仍先运行 `model_baseline`，再串行运行 `model_top20`，两个账户分别执行模拟仓周期、
独立重放和前瞻验收。Top20 只消费同一份不可变 Top30 信号的排名前 20 投影；不改变模型、股票池、
调仓日、Top30 账户、真实交易或券商连接。

本授权不代表 Top20 优于 Top30，不授权同图绩效比较、策略有效性、实盘交易或真实委托。自然
`FORWARD` 证据不足时必须继续显示 `NOT_READY/OBSERVING`。

## 3. 切换门

候选镜像可以在今日跑批完成后构建和离线验证，但生产容器是否立即切换必须由
`release_start_readiness` 裁决。若当前版本已完成最新模拟仓且没有更新交易日，跨快照启动必须失败
关闭；不得为了立即重启而覆盖今日产物、回写代码身份或绕过门禁。候选镜像与发布审计保留，待出现
更新的合格交易日后再执行受控启动。
