# M4-0 科创50基准残差因子数据与预执行门验收

> 日期：2026-08-04（Asia/Shanghai）
>
> 裁决：`GO_M4_STAR50_RESIDUAL_DATA_PREEXECUTION_ONLY`
>
> 策略有效性：`NOT_EVALUATED`
>
> 生产授权：`none`

## 1. 裁决边界

结果前协议提交 `dd01843`、实现提交 `e9f195c`、一次性执行 release 提交 `3177e4b` 均已在第一份
真实候选特征值生成前推送。旧 M1/M3 批继续权威停止；本任务没有补发或回救旧候选，也没有调整
P2-2C 已拒绝的模型或组合。

本裁决只证明三个固定候选可在科创50官方 PIT 股票池上按冻结时钟构造并通过结构数据门。没有读取
标签、RankIC、收益、回撤、2023—2025封存验证、压力期或 G1，没有调用 DeepSeek/Tushare 等外部
API，没有读取 `.env`，没有训练模型、回测、选股、生成信号或接生产。

## 2. 真实输入与覆盖

发现信号期固定为 2020-08-03 至 2022-12-15。输入只读过滤至 2022-12-15，实际包含：

| 指标 | 结果 |
|---|---:|
| 官方信号交易日 | 577 |
| 官方成员日 | 28,850 |
| 发现期及预热股票行情行 | 54,412 |
| 发现期及预热科创50基准行 | 637 |
| 冻结覆盖分母 | 28,838 |
| 三候选有限特征行 | 28,838 |
| 三候选各自分母覆盖率 | 100% |
| 全部官方成员日结构覆盖率 | 99.9584% |
| 每日最少有限候选数 | 49 |

未进入分母的 12 个成员日全部是已归因的全天停牌：当日无市场 bar，且同日 `total_mv` 不可用；
行业缺失为 0。协议禁止用前值、0或扩大窗口补齐，因此这 12 行如实排除。它们不是因子计算失败或
按效果选择性删样本。

三个固定候选均通过：

- `residual_momentum_35_skip5`
- `residual_reversal_5`
- `negative_idiosyncratic_volatility_40`

重复特征主键、持久化非有限值和 `.BJ` 行均为 0。每个特征窗口结束日都等于信号日，独立审计确认
不存在未来窗口。

## 3. 确定性与不可变证据

一次性研究镜像为
`sha256:6cfc8b9363679dc30f0dacf8dbde57aac9987403905d257e295f319d66f48df5`，断网、无 `.env`、
非 root、只读根，只读挂载 P2-1 输入并只给 M4 专属结果目录写权限。

| 证据 | SHA-256 |
|---|---|
| 协议 | `a404e32a581cb80534cd8859b4909d275d5fefb8977bcd920051c8ab2da527ab` |
| 执行 release | `4e6bc62d9103aed383f66d69731e1a1e708945bb8d9b5a0c47a29a1faceda634` |
| 代码束 | `a4a5bd4ab5bf2fa3b9a22b181bf9ee524e19c1e554aefe782536f4e9703d2b3a` |
| 特征 Parquet | `de108fc22ca747a50376cd789f23e34d49b8fcaa00bfbe37bf549f09fe44a377` |
| 质量报告 | `0b3111686c21d9e5535cd66458816086d775731db79f640179c45b807a0dda58` |
| 独立审计规范哈希 | `37325de0000b2f52ffdfac044e89571c2ab6229ba6ae615ed5f25539bf2f1dbb` |
| 脱敏 manifest | `0d972c6daa68dbf4beb101c460cb235bd6d62c5df33bfa9e65ceedb2f7e0c8dc` |

第二遍完整复跑返回 `artifact_reused=true / quality_report_reused=true`，两项物理哈希完全不变；独立
只读审计同时通过哈希、行数、主键、有限值、`.BJ=0`、窗口不未来和结果盲标识七项检查。

## 4. 工程与生产隔离

新增代码按 contract、compute、evidence、audit 和 run 拆分，最大文件 258 行；没有向现有热点文件
增加职责。宿主全仓 576 项测试通过，Ruff、compileall、pip check 和 `git diff --check` 通过。

施工前后 scheduler 均为容器 `183b8c6c5edd`、生产镜像内容 ID
`sha256:722f63de15932cb2698e82db7c0140f02921f27f126e1c0235ca62e0a1213b76`、代码快照
`4e5244b6...82708`，保持 `running/healthy`，未重建、未重启。一次性 M4 镜像没有 promote 或进入
发布审计。

## 5. 后续允许动作

M4-0 GO 不是因子有效或策略有效。下一目标若继续，必须另立并先推送 M4-1 结果协议，固定三个原式、
次日开盘至 T+11 开盘标签、PIT 行业/市值中性化、Alpha158 增量比较、成本、压力期、G1和多重检验。
三个候选方向、40/60窗口和缺失规则不得因本次覆盖或未来效果修改；相关价量尝试背景按 273 次披露。
