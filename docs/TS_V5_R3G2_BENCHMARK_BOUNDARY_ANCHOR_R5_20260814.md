# TS-v5 R3G-2 H00906 起点锚语义澄清 R5

日期：2026-08-14（UTC+8）

状态：`RESULT_UNKNOWN_BOUNDARY_ANCHOR_SEMANTICS_CLARIFIED`

机器真身：`config/ts_v5_r3g2_benchmark_boundary_anchor_r5.yaml`

R4 在任何实现、派生写入或策略效果读取前被发现存在字面矛盾：一方面要求边界锚“OHLC 全空”，
另一方面要求 close 有限且为正。两项不能同时成立，因此 R4 不直接进入施工，也不被静默改写。

R5 只澄清字段集合：允许的唯一边界锚必须位于精确请求起点 `20190101`，不属于 SSE 开市日，代码为
`H00906`，open/high/low 三列全空，close 有限且为正。它只留在原始证据中并从派生日表排除；排除后
日期集合必须与官方开市日完全相等。第二条非开市日、其他日期、缺失开市日或身份异常继续失败关闭。

本澄清不改变数据、门槛、来源或时间角色，不发新请求，不读取候选收益、Alpha158 数值或任何策略
结果。一次断网评价恢复与一次独立审计仍是唯一授权；GO 只代表 H00906 谱系数据门通过。
