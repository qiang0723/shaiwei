# TS-1A-R4 结果盲状态操作化附录

日期：2026-08-12（UTC+8）

本附录只澄清已冻结协议如何落成确定性日线序列，不改变4%回调、2%结构止损、15%最大止损距离、
2019—2024范围或最低样本门，也不扩展任何收益权限。

- 每个计划周以SSE交易日历所在自然周标识；所有周/月特征严格取观察日之前已完整结束的周期。
- 200亿元市值门取上一完整周最后一个有效PIT市值，周内不随价格变动重新判断。
- 触达日和确认日均须仍为PIT中证800成员、非ST、行业唯一，并通过当日市场、所属上市板块与热点行业
  门。触达状态只在同一计划周保留，多次触达仍只形成一个事件。
- 从计划周首个有效bar起，只要收盘不高于冻结结构止损，本周即永久失效；失效发生在确认日或之前均
  不得确认。每票每周只认首次合法确认，事件日期就是确认收盘日，也是Alpha158键日期。
- 下一开盘只认确认后紧邻的下一SSE开市日，不得跳到股票后来恢复交易的日期。该日仍须为PIT成员、
  非ST，raw OHLC/成交量有效，且与确认日复权因子完全相同；否则拒绝而非引入尚未冻结的公司行为适配。
- H00906只在R3已绑定manifest的源元数据中寻找显式全收益身份；不得从`000906.SH`价格序列内容推断
  或临时合成。缺失就如实输出`BLOCKED_BENCHMARK_DATA`。

真实含证券身份的事件中间件、匿名日报、报告与audit全部保存在Git忽略区；报告只含聚合计数和哈希，
不含入场后路径。机器真身：`config/ts_v3_pullback_state_operationalization_v1.yaml`。
