# TS v3 数据契约恢复 R2（结果前冻结）

日期：2026-08-12（UTC+8）

状态：`FROZEN_BEFORE_IMPLEMENTATION_COMPLETION_AND_RESULTS`

机器真身：`config/ts_v3_data_gate_recovery_r2_v1.yaml`

## 为什么不能直接执行 R1

R1 尚未生成 release、未请求用户授权、未创建 claim、未读取密钥、未调用 provider，也未消费画像
尝试。执行器联调前的只读键级覆盖复核发现：既有科创50指数历史只到 2026-07-24，而研究截止日为
2026-08-11。首发后的指数缺口不能被 `NOT_EVALUABLE` 掩盖；否则创业板历史补齐后，科创板成员仍会
在没有合法板块状态的情况下进入候选漏斗。

因此 R1 原件永久保留并记 `SUPERSEDED_BEFORE_EXECUTION_BY_R2`，不改写、不冒充失败尝试。R2 继承
R1 和操作化附录的全部阈值、PIT、生命周期、可交易性、匿名报告与结果防火墙，只扩大必要的数据恢复
请求，不改变策略。

## 两个且仅两个请求

1. `399006.SZ` 创业板指，2016-01-01 至 2026-08-11，全历史；
2. `000688.SH` 科创50，2026-07-25 至 2026-08-11，只补首发后缺失尾段。

字段均固定为 `ts_code,trade_date,open,high,low,close,pre_close,change,pct_chg,vol,amount`。
两份响应必须都通过代码、日期、唯一键、官方 SSE 日历、OHLC、行数上限与 `.BJ=0` 校验，才允许
分别写入不可变 raw batch 与追加式 ingest ledger。每个逻辑请求最多 6 次有界传输尝试；claim 后同
scope 不得重跑。密钥仅能在 claim 落盘后从项目内 Git 忽略 `.env` 读取，禁止输出或提交。

## 完整性硬门

- 中证800：2016-01-04 至 2026-08-11 每个官方开市日均有唯一日线；
- 创业板指：2016-01-04 至 2026-08-11 每个官方开市日均有唯一日线；
- 科创50：2019-12-31 至 2026-08-11 每个官方开市日均有唯一日线；
- 缺失、重复、冲突均必须为 0；科创50首发前的科创板成员继续明确不可评估，禁止使用其他指数回填。

## 不授权事项

R2 仍不授权候选后的收益、MAE/MFE、模型训练、预测生成、回测、效果评价、DeepSeek、模拟仓、Web
或生产。恢复完成只允许回答“数据合同能否支持一次结果盲候选画像”，不等于策略有效。
