# 结果导向路线图（2026-07-16 冻结；2026-08-09 第二次阶段复盘）

> 本路线图记录模拟组合与前瞻绩效、资金流候选、科创独立策略臂、多股票池因子研究、专业只读
> Web，以及长期 LLM 多源因子研究。
> 领先不是验收标准；只有可复现、扣除成本、通过预注册门槛的增量结果才算完成。

## 2026-08-17 TS-v5 R3G-2效果预执行工程完成

结果盲runner、独立auditor、合成夹具、不可变镜像、键级预检和唯一release scope均已完成。最终镜像
`sha256:dbf006ec...8a6053`与提交`3b43b6b...e725bf`、930个受控文件一致；干净镜像23项、全仓1371项
均PASS。键级预检报告`3cc735ee...114d0`幂等GO，真实分数/收益/H00906值未读、效果尝试0。
唯一未授权scope为`961b62f2...e19db75`；当前只到
`READY_FOR_EXACT_USER_APPROVAL_NOT_EXECUTED`。用户逐字批准前不得执行；发现期失败时留出期保持物理
未读，同scope重跑、外网、2026、额外参数、模拟仓、Web和生产继续禁止。见
`docs/TS_V5_R3G2_EFFECT_PREEXECUTION_ENGINEERING_20260817.md`。

## 2026-08-17 TS-v5 R3G-2 W7入口恢复数据门GO

recovery scope`f61a2365...44b5`唯一runner完成固定W7 first/replay，无Qlib独立auditor裁决
`GO_W7_SCORE_LINEAGE_DATA_ONLY`；194,329行分数谱系覆盖2025年，两遍模型/预测/summary逐内容一致。
原scope未重跑，RankIC、收益、H00906、组合和策略效果均未读，效果尝试0、策略NOT_EVALUATED、生产
none。本scope关闭；下一步只能另立TS效果release并重新取得用户精确批准。见
`docs/TS_V5_R3G2_W7_LINEAGE_RECOVERY_ACCEPTANCE_20260817.md`。

## 2026-08-17 TS-v5 R3G-2 W7入口恢复发布准备

新恢复协议仅修复runner/auditor CLI参数映射，并绑定原scope、原批准、失败回执和两个空输出根；使用新
镜像、新scope、新批准schema和新输出目录，原scope永久禁止重跑。准备提交`8c22834`、镜像
`sha256:39a5fa...1398`、911文件host/image门和断网fixture已PASS；唯一未授权scope为
`f61a2365...44b5`。真实Qlib/W7/RankIC/收益/效果仍为0，下一步必须再次取得用户逐字批准；恢复GO也不
自动授权TS效果、模拟仓或生产。见
`docs/TS_V5_R3G2_W7_ENTRYPOINT_RECOVERY_PREPARATION_20260817.md`。

## 2026-08-17 TS-v5 R3G-2 W7入口失败与恢复边界

原scope`5d238942...38ad`唯一runner在进入真实执行函数前因CLI参数名错配失败；runner次数已消费且不得
重跑，auditor/真实Qlib/W7模型与分数/策略效果均为0。两个CLI入口已最小修复并加入直接main回归；
下一步只允许另立绑定原scope、approval和失败receipt的新recovery scope，重新取得精确批准。见
`docs/TS_V5_R3G2_W7_ENTRYPOINT_FAILURE_20260817.md`。

## 2026-08-17 TS-v5 R3G-2 W7谱系预执行工程

2025 W7已完成分层工程入口、双跑、write-once、精确批准和无Qlib独立审计，但真实训练与分数仍为0，
TS效果尝试仍为0。最终镜像已通过断网fixture与906个host/image受控文件逐项哈希核验；未授权scope
`5d238942...38ad`已生成。用户精确批准后才可训练一次W7。W7数据GO也不授权TS收益、模拟仓或生产。见
`docs/TS_V5_R3G2_W7_PREEXECUTION_ENGINEERING_20260817.md`。

## 2026-08-17 TS-v5 R3G-2真实效果协议冻结

H00906数据门通过后，结果前效果合同已冻结：只运行R3G-1机械锁定的一个突破回踩主点和两个邻居，
2021—2023发现门通过后才允许读取2024—2025条件留出，2026无W8只监控不评价。旧P1 Alpha158缓存
因早于M6标签成熟度纠错被禁用；2021—2024复用干净W2—W6，2025按同口径新建W7。账户、买卖、
税费、容量、H00906超额、DSR和停止门均已锁定，历史GO不授权模拟仓或生产。下一节点是断网W7谱系
与效果执行/审计工程，首次收益读取仍须独立release scope和用户明确批准。见
`docs/TS_V5_R3G2_EFFECT_PROTOCOL_20260817.md`。

H00906前置谱系数据门已于2026-08-14完成：1845/1845个SSE开市日全覆盖，唯一官方起点锚只留raw，
独立audit 15/15 PASS。裁决仅为`GO_H00906_LINEAGE_DATA_GATE_ONLY`，策略尚未评价。下一节点另立
R3G-2结果前效果协议，且只允许R3G-1机械锁定的3个突破回踩点进入。见
`docs/TS_V5_R3G2_BENCHMARK_LINEAGE_ACCEPTANCE_20260814.md`。

## 2026-08-14 TS-v5 R3G-2前置基准谱系门

H00906中证800全收益基准数据门已在任何候选收益读取前冻结。现行R3G时间仍为2021—2023发现选择、
2024—2025冻结稳定性、2026截至8月11日只观察；采集2019—2026仅为同时满足旧TS-v3基准合同，不能
把2019—2020重新用于选择。权威源只认中证指数官方事实表和H00906历史接口，禁止价格指数或本地股息
代理。下一步只执行一次官方不可变采集、完整性评价和独立audit；GO后仍须另立R3G-2结果前效果协议。
见`docs/TS_V5_R3G2_BENCHMARK_LINEAGE_PROTOCOL_20260814.md`。

首次网络容器在事实表TLS握手阶段失败，未取得完整响应、未发历史请求、未写产物；该scope关闭且不是
数据质量结论。R1已在结果未知时冻结：宿主只串行传输固定三份官方bytes，Docker断网完成原门评价与
独立audit；不引入secret、替代源或效果权限。见
`docs/TS_V5_R3G2_BENCHMARK_TRANSPORT_RECOVERY_R1_20260814.md`。

R4起点锚文字在实施前被发现同时要求“OHLC全空”与“close为正”，因内部矛盾不直接施工。R5仅澄清为
open/high/low全空、close有限且为正；不改数据、来源、门槛、时间角色或网络次数。见
`docs/TS_V5_R3G2_BENCHMARK_BOUNDARY_ANCHOR_R5_20260814.md`。

## 2026-08-13 TS-v5持续演化研究路线

R3G-1近期密度投影已完成R2权威恢复：现行时间口径覆盖2021—2026，但严格分为2021—2023发现选择、
2024—2025冻结验证和2026截至8月11日只观察不改判。首轮431点全零因公共`raw_open`缺失和`has_bar`
接线错误失效，原产物永久保留；R2单点修复后形成5,683行无收益事件，独立audit 21/21 PASS。六机制
仅突破后回踩通过：81点中41点过发现密度门，机械锚点及两个邻居均过冻结验证；其余五机制本批停止。
权威`PARTIAL_GO_DENSE_MECHANISMS_ONLY`，不是策略有效或生产授权。下一步先补H00906全收益谱系，再另立
R3G-2结果前效果协议，且只允许这3个冻结点进入。见
`docs/TS_V5_R3G1_RECENT_DENSITY_RECOVERY_R2_ACCEPTANCE_20260813.md`。

R3G-0六机制可执行语义工程门已完成：六份R3F候选从不可变content重新编译并登记，fingerprint与语义
签名完全一致；价格/相对强度量纲和六类确认公式在实现前分别加注。整数网格去重后六候选有效点为
81/75/81/81/32/81，共431；六条正常合成执行路径与16类对抗路径全PASS。断网镜像、独立audit和幂等
复跑均PASS，权威`GO_R3G_DENSITY_SCOPE_PROPOSAL_ONLY`；行情/密度/收益/Alpha158分数/模型/回测均未
读取或运行，TS-v5效果尝试仍为0。其当时的2019—2021下一步文案已由上方R3G-1结果前冻结的2021—2026
三段角色显式覆盖；未来效果前仍须闭合H00906全收益谱系，不得直接模拟仓或生产。见
`docs/TS_V5_R3G_EXECUTABLE_SEMANTICS_ENGINEERING_ACCEPTANCE_20260813.md`。

R3F本地绑定proposal真实合同金丝雀已完成：结果前scope、实现镜像和release依次推送后，唯一短命容器
串行取得六机制各1份完成响应；外部请求6、完成6、每席仅sequence 1，重试/递补/第7次调用/计费不确定
均为0，实际费用0.010779967美元。六份均通过proposal v3 Schema、本地authority编译和原候选validator，
且语义签名互不重复；断网独立audit与零调用幂等复核PASS。权威
`GO_BOUND_PROPOSAL_CANARY_ONLY`，只说明候选生成合同可用，策略效果仍`NOT_EVALUATED`。下一步须另立
R3G发现期评价scope，先登记/去重并冻结发现期、事件密度、参数预算、成本和多重检验；不得直接寻找
最高收益、模拟仓或生产。见`docs/TS_V5_R3F_LLM_CANARY_ACCEPTANCE_20260813.md`。

R3E零API工程已关闭R3D的本地授权缺陷：proposal v3响应不再拥有lineage或搜索点数；批准的独立席位
通过request中的本地authority绑定，编译器唯一注入候选lineage，证据mode从候选派生。1—5槽搜索点
机械分配并始终不超过196。六机制合成编译、48个对抗样例、R3C六份封存回答0录取、独立audit和幂等
均PASS，旧v2与旧裁决保持不变。当前仅`GO_R3F_LIVE_CANARY_SCOPE_PROPOSAL_ONLY`；如继续，必须另立
小批R3F scope/release并获用户明确批准，不自动调用DeepSeek，也不进入参数搜索/回测。见
`docs/TS_V5_R3E_BOUND_PROPOSAL_ENGINEERING_ACCEPTANCE_20260813.md`。

R3D零API离线诊断发现R3C存在本地授权绑定缺陷：批准的六席均为`INDEPENDENT`，但请求没有绑定
`assigned_attempt_mode`且Schema允许反方模式；六份回答均选`ADVERSARIAL_REVISION`，runner却统一按
独立席位记账。权威`STOP_LOCAL_IMPLEMENTATION_DEFECT`；字段层另有搜索积超196占5/6、文本安全2/6、
父哈希2/6和缺字段1/6。下一步只能另立R3E零API工程，把mode确定性绑定到request/schema/compiler/账本
并机械约束搜索积；不放宽validator或196上限，不自动开启新调用。见
`docs/TS_V5_R3D_OFFLINE_PROPOSAL_DIAGNOSTIC_ACCEPTANCE_20260813.md`。

R3C六机制合同金丝雀已完成唯一真实批次：六机制各1份、恰好6/6个完成响应，无重试、递补、第7次
调用或计费不确定；实际费用0.012032535美元。六份均为HTTP 200、可解析JSON，但机制专属proposal
Schema或确定性编译合同6/6 FAIL，有效候选0，权威`STOP_NO_VALID_CANDIDATES`。断网独立audit与零调用
幂等复跑PASS，scheduler未变化。本scope关闭，不进入参数搜索/回测；下一步只宜另立零API、零行情的
R3D离线匿名失败诊断，任何新调用仍须新scope和明确批准。见
`docs/TS_V5_R3C_LLM_CANARY_ACCEPTANCE_20260813.md`。

R3B机制专属合同投影工程门已完成：六机制各自拥有proposal Schema和精确参数projection，LLM只提供
研究语义和边界内选择；本地编译器从冻结常量补齐reference/measure、强制取消规则与必需features，
并继续通过原冻结候选validator。六机制全部编译，42/42对抗样例fail closed，独立audit和幂等复跑
PASS；外部调用、secret、行情/效果和回测均为0。当前最多
`GO_NEW_LIVE_CANARY_SCOPE_PROPOSAL_ONLY`，下一步仍须单独冻结小批scope/release并获用户明确批准，
不得直接进入参数搜索、回测、模拟仓或生产。见
`docs/TS_V5_R3B_CONTRACT_PROJECTION_ENGINEERING_ACCEPTANCE_20260813.md`。

R2四响应合同金丝雀已完成零调用预执行门：固定四个独立机制席位、无反方/递补/第五调用，v2请求束
SHA-256为`0068357f...e489b`，最坏费用0.034104美元、硬上限0.10美元。全仓与断网只读镜像预检均
`GO_PREEXECUTION_ONLY`；新增代码仅为157行scope/请求束预检，没有复制live runner或扩张传输热点。
当前仍没有API调用、secret读取、行情/效果、参数搜索或回测。下一步必须由用户批准唯一scope哈希
`e2d7218f...a1a8ef`，之后才能另立执行release；“继续”本身不构成付费授权。见
`docs/TS_V5_R2_LLM_CANARY_PREEXECUTION_ACCEPTANCE_20260813.md`。

首批DeepSeek机制研究已完成12/12响应，实际费用0.03012781美元；但12份全部因provider结束原因非
`stop`而无效，合法候选0。权威裁决`STOP_NO_VALID_CANDIDATES`，不进入参数搜索/回测，不补发或复用
本approval。原始报告的`GO_CANDIDATES_ONLY`遗漏合法候选门，保持原文并由独立断网audit追加纠正。
TS策略族继续；未来若恢复LLM候选生成，须先另立结束原因兼容与语义门协议和新scope。见
`docs/TS_V5_LLM_FIRST_BATCH_ACCEPTANCE_20260813.md`。

TS-v5-R1已完成零API响应合同恢复工程门：不可变证据确认12份响应均因思考阶段用满1800 tokens而
`length`停止，最终content为空。v2只把结构化候选请求改为显式非思考模式，候选Schema、提示、六机制、
max_tokens和失败关闭门不变；不把reasoning正文当候选，也不把`length`兼容成成功。断网独立镜像复核
PASS，新增调用和费用均为0。下一节点不是自动补发，而是另立小批live scope并再次取得用户批准；在此
之前不得参数搜索、回测、模拟仓、Web或生产。见
`docs/TS_V5_LLM_RESPONSE_CONTRACT_RECOVERY_ACCEPTANCE_20260813.md`。

2026-08-13新增执行授权：用户明确批准v1 scope哈希`9947e1...a88`，首批恰好12个完成响应、串行、
0.50美元单批熔断；此批准晚于预算v2，故v1控制本批，5美元总预算不扩大本批且不自动授权后续批次。
先完成最小编排/证据/独立审计工程和不可变镜像，再冻结绑定release后调用；不授权行情/效果、参数
搜索、回测、模拟仓、Web或生产。代码新增遵守四职责拆分与400行软上限。见
`docs/TS_V5_LLM_EXECUTION_PROTOCOL_20260813.md`。

TS升级为长期策略研究族：具体版本可以停止或淘汰，策略族只由用户明确退役。探索与正式验证拆成双速
通道；发现期允许利用匿名反馈持续提出和修改逻辑，但候选、参数评价、重复和失败全部计数。DeepSeek
只负责机制假设、定向改版、失败诊断和语义反审，本地确定性程序负责参数搜索、成本/可成交回测、
多重检验、去重、选择和独立裁决。首轮只研究六个不同机制，不继续扩大固定百分比网格。

治理、严格候选合同、六机制枚举、参数/复杂度边界、语义去重、受限提示、TS专用窄传输适配和离线
CLI均已完成工程验收；
离线preflight形成六个不同独立请求并停在`READY_FOR_BOUNDED_LLM_RESEARCH_APPROVAL`。用户已将TS-v5
DeepSeek持续研究总费用硬上限提高到5.00美元，但首批仍为12个响应/0.50美元单批熔断，余额不是自动
执行授权，后续每批仍须冻结scope并明确批准。真实调用仍须以精确发送内容和v2 scope再次取得用户
批准；未授权行情/收益、回测、模拟仓、Web或生产。见`docs/ADR_0006_TS_EVOLUTIONARY_RESEARCH_LANE.md`、
`docs/TS_V5_EVOLUTIONARY_RESEARCH_PROTOCOL_20260813.md`和
`docs/TS_V5_EVOLUTIONARY_RESEARCH_ENGINEERING_ACCEPTANCE_20260813.md`和
`docs/TS_V5_LLM_RESEARCH_BUDGET_ADDENDUM_20260813.md`。

## 2026-08-12 TS-v4参数研究继续路线

2026-08-13 更新：v4B首次写出前失败后，R1按结果未知时冻结的单点序列化修复完成唯一断网画像与
唯一独立审计。1.5%/2.5%/3.5%/4.0%四臂在2019—2021分别仅有5/5/4/4个合法事件，2019年均为0，
没有任何臂或相邻参数对通过预注册密度门；Alpha158键覆盖100%，但未读取分数或收益。权威裁决
`STOP_NO_DENSE_PARAMETER_REGION`、audit 18/18 PASS、策略效果仍`NOT_EVALUATED`。因此固定百分比
参数区域关闭，不扩网格、不降门槛；若继续TS须另立具有新研究含义的波动自适应或周结构分位版本，
并重新经过结果盲密度门。任何效果前仍须闭合H00906全收益谱系。见
`docs/TS_V4_DENSITY_PREFLIGHT_RECOVERY_R1_ACCEPTANCE_20260813.md`。

TS方向不因v3的4%回调样本不足而关闭。用户授权继续研究合理参数，但不得把历史最高收益当结果。
v4A只改变回调深度，预注册1.5%/2.5%/3.5%/4.0%四臂，其余机制和组合执行规则不变；先用2019—2021
做结果盲密度门和发现选择，再把唯一锁定参数依次交给2022—2023验证和2024锁定历史测试。选择必须
来自相邻参数平台、全部四臂计入多重检验且DSR>=0.95；验证/测试失败不得换第二名。2024不冒充完全
未知样本外，历史GO也不直接授权模拟仓或生产。任何效果前还须补齐可审计H00906全收益基准。当前只
冻结协议；TS-v4B一次性结果盲密度预检release也已在真实运行前冻结，只允许四臂发现期计数、Alpha
事件键覆盖与独立audit，不读取收益或Alpha分数。下一节点为合成状态机/结果防火墙验收后唯一一次断网
真实画像。见`docs/TS_V4_PARAMETER_RESEARCH_PROTOCOL_20260812.md`和
`docs/TS_V4_DENSITY_PREFLIGHT_RELEASE_PROTOCOL_20260812.md`。

首次v4B画像在任何产物或结果写出前因DuckDB COPY参数绑定顺序失败，原scope已停止且不重跑。R1仅
修复序列化写法并使用独立输出目录，研究含义、输入、四臂和门槛不变；下一节点为写出fixture和镜像
身份通过后唯一一次R1断网恢复画像。见
`docs/TS_V4_DENSITY_PREFLIGHT_RECOVERY_R1_PROTOCOL_20260812.md`。

## 2026-08-12 TS重点旁线（v3方向冻结）

TS现升级为“强板块—强个股—右侧波段”方向：先要求大盘与主要市场不继续创新低，再选择相对走强的
热点板块；板块内以市值不低于200亿元、完整周成交额不低于50亿元作基础门，连续两个月高点提高、
三根完整周K线低点不下移确认右侧结构，日线负责回调后转强买点。两批各最多5%、单票10%、正常3—7只、
总仓位70%和允许长期空仓继续继承；初始止损暂定上周低点下浮2%且只准上移。统一中证800/SMA20许可、
SMA6月线许可、`VWAP-1R`唯一入口和固定ATR止损被v3方向覆盖。板块映射/强度、连续两周波动和放量
仍须结果盲量化。TS-1A v1 的`BLOCKED_MARKET_RULE + BLOCKED_DATA`永久保留；随后R3在新冻结合同和
用户精确授权下补齐创业板全历史、科创50尾段与中证800单日缺口。三指数官方日历、PIT成员日行情/
独立不交易解释、行业、市值与Alpha158事件键门已通过，断网画像裁决`GO_TS_V3_FREEZE`、独立audit
`PASS`；策略效果仍为`NOT_EVALUATED`。随后TS-1B冻结前复核发现，R3的832行只证明日线恢复形态，
尚未证明事前回调线已经触达；2019—2024止损距离小于15%的154行只能视为上界，不能作为权威样本门。
同时真实效果要求的H00906全收益基准尚未绑定。TS-1B因此`STOP_BEFORE_FREEZE`，不是策略失败或R3
数据门翻案。下一合法节点收窄为结果盲TS-1A-R4，先闭合回调状态序列和H00906谱系，再冻结效果协议；
不得直接进入回测、模拟仓、Web或生产。见`docs/TS_V3_DATA_GATE_RECOVERY_R3_ACCEPTANCE_20260812.md`
和`docs/TS_V3_EFFECT_PREFREEZE_REVIEW_20260812.md`。

R4结果盲协议先行冻结后已完成唯一画像与独立audit：23个确认事件中10个合法、13个下一开盘高于周
锚点；2019—2024仅8个合法事件、5个信号日，远低于预冻结的60/40门且年度分布门也失败，故权威
`STOP_INSUFFICIENT_TRUE_EVENTS`。Alpha158事件键8/8覆盖，但H00906仍未绑定并独立
`BLOCKED_BENCHMARK_DATA`。策略效果尝试仍为0、`NOT_EVALUATED`，不得为救回样本放宽4%回调、15%
止损距离或年度门。当前精确基线停止；若未来继续TS，须提出具有新研究含义的新版本并登记新尝试，
不能同scope重跑。见`docs/TS_V3_PULLBACK_STATE_PREFLIGHT_ACCEPTANCE_20260812.md`。

最终交付路线固定为后台原子快照→TS独立模拟账户→Web只读页面：页面展示具体中文简称/代码、热点板块、
状态、第一/第二批计划买点、结构止损、盈利/最晚退出、仓位和证据；无合格股票明确显示空仓。每项生成
稳定反馈编号，用户反馈进入下一版研究提案，不能直接修改当天信号。见
`docs/TS_V3_STRATEGY_PRODUCT_PLAN_20260812.md`。

以下v2.1记录作为v3继承来源保留，待正式量化协议形成后整理为单一无冲突规则：

新增`TS`月线趋势—周线回撤策略作为重点旁线，优先级低于生产/R2-1主线、高于RF和普通研究扩张。
TS按“已完成月线决定方向、上一完整周决定回撤锚点、日线只负责真实执行”分层，避免使用未结束周/月
数据。v2在读取收益前退役固定3%—5%买点、5%—10%止盈及触线即成交，优化为完整状态机：市场与个股
月线SMA6许可 → 周VWAP下方1R只进入回撤观察 → 日线收复前日高点且大盘站上SMA20确认 → 次日合法
开盘买入（周末顺延到下一官方交易日但不重算周计划）。同期OOS Alpha158只负责候选排序，仓位按
单笔/组合风险、容量和行业约束。v2.1进一步冻结集中分批账户：每票最多两批、合计不超过10%，第二批
只在第一批盈利且再次确认趋势后执行；正常3—7只、硬上限7只、总仓位不超过70%，信号不足保留现金。
每周约5日刷新候选，第5/10日只复核，第15交易日硬退出；退出采用预置1.5R止盈和收盘跌破1R后次日
真实退出，二者可早于5日。缩量、10日涨幅随后逐个做增量；所有结果、
账户和生产身份与中证800主策略隔离。

上述v2.1状态现为历史设计记录，不再是现行候选。v3 TS-1A v1结果盲量化曾按双阻断停止，R3现已在
不读效果的前提下解除数据合同阻断并允许另立冻结协议；以下是原冻结范围，保留用于审计：冻结唯一
市场/板块映射，并画像板块强度、波动、放量、月周日结构，同时核验既有Alpha158 OOS分数在TS事件日
的覆盖、成熟时钟和10日标签适配。Alpha158足以
启动首版排序但未被认定最终充分；若后续挖掘TS原生因子，先做缩量和10日过热，每次一个机制、最多
三个低复杂度候选，并证明相对Alpha158非重复，不得用新因子混合救回失败的v2.1。不得读取收益/
MAE/MFE、跑参数网格、训练回测或创建模拟账户。完整边界见
`docs/TS_0_TREND_SWING_STRATEGY_DESIGN_BASELINE_20260812.md`。

## 2026-08-12 进度与收紧的平衡口径

R2-1自然前瞻仍是唯一主裁决线，20个live-dual日和2次自然调仓门不降低。为避免等待期完全停工，
允许旁路推进只读元数据盘点、外部一手研究、字段/PIT适用性、规范去重、协议和对抗fixture准备；这些
工作不得读取效果、生成候选、调用LLM、训练回测或改变生产。

首个准备节点记为`RF-0A`（Research Factory，避免与既有R3 Web编号混淆），选择中证800“隔夜信息
吸收与日内反转状态”作为唯一机制问题，裁决为`GO_PREP_ONLY_NOT_EXECUTION_AUTHORIZED`。下一旁路
节点只允许RF-0B open/前收盘数据画像与跨账本/Alpha158身份预检；正式8响应小批仍须等R2-1检查点
和用户复核后另立协议。见`docs/RF_0A_CSI800_SINGLE_MECHANISM_PREPARATION_20260812.md`。

## 2026-08-09 第二次阶段路线收紧

裁决`COURSE_CORRECTION_AND_OBSERVE`：平台整体没有跑偏，但8月6日后恢复工程和历史复算入口再次快于
权威效果产出。M6形成`PORTFOLIO_CONVERSION_BOTTLENECK_INDICATED`后，Top20历史效果因约`1e-16`
复现差异停在效果前；M7投入18,632行tracked文本后仍停在数据门，候选/效果/尝试增量均为0。

因此关闭M6-R4和M7-R4/R5，暂停新股票池、因子、LLM批、通用控制面和无目标重构。下一主目标改为
`R2-1 FORWARD_EVIDENCE_CHECKPOINT`。同日合同补正把协议FORWARD 10日/1次分为受控追赶5日/1次与
live-dual自然5日/0次；主门只认后者。只靠生产自然增长，预计20260814核验第一次live-dual调仓，
最早20260828同时核验20日/2次调仓门。未到门槛且无异常时不制造开发任务。
完整依据见`docs/PLATFORM_ROUTE_REVIEW_20260809.md`。

## 2026-08-10 LLM研究工厂长期治理基线

用户确认长期采用“LLM研究员 + 确定性裁判 + 独立审计 + 多模拟账户”的研究工厂模式。因子研究与
策略研究是共享治理底座上的并行分支；中证800可作为首个研究池，但任何候选使用独立身份、产物、账本
和模拟账户，不覆盖主策略。首批默认8个完成响应、最多3个候选、机械Top1/Top2，连续最多3个小批后
强制复盘；未来高配置电脑只作为隔离research-worker，不替换生产scheduler。

该基线状态为`FROZEN_DESIGN_BASELINE_NOT_EXECUTION_AUTHORIZED`。R2-1到期前仍不写代码、不调用
DeepSeek、不新增候选/队列/Worker/Web执行能力；具体供应商、模型、预算、封存效果和模拟账户继续逐批
授权。完整基线见`docs/LLM_RESEARCH_FACTORY_GOVERNANCE_BASELINE_20260810.md`。

## 2026-08-06 阶段路线重排

阶段复盘裁决为`REORDER`，不是整体暂停：生产和自然前瞻继续；PIT、预注册、独立audit与Docker隔离
继续；M5历史动态基本面因23/23冲突均无权威历史版本链而暂停，Registry v1和现有控制面冻结。下一阶段
不再扩候选数量、队列或Worker。Web最新权威只读投影与M6-0中证800模型/组合归因小批协议均已冻结；
M6-1结果盲工程门已`GO_ENGINEERING_ONLY`；M6-2唯一真实runner与内部双跑完成后，原auditor因CLI
绑定错误在读结果前停止，M6-2R随后以新scope完成唯一断网auditor-only恢复。独立audit PASS，权威
归因终态为`PORTFOLIO_CONVERSION_BOTTLENECK_INDICATED`：两个替代臂都改善RankIC，但固定Top30组合
均未形成更好的扣费后结果；生产授权仍为none。M6-3A已冻结唯一变量`TopK 30→20`，M6-3B纯合成
工程门已`GO_ENGINEERING_ONLY`并由独立audit复算PASS；M6-3C唯一真实runner获精确批准后，在首个
W1控制臂Top30重建日报与封存规范日报逐内容比对时失败关闭为`BLOCKED_PRE_EFFECT`。Top20尝试消费0、
Top20效果仍为0，独立audit因runner前置失败未启动；原scope不得重跑。M6-3C-R1因Compose tmpfs
编排错误在容器前失败；R2修复后完成6次Top30诊断及独立audit。三路各自确定，但原M6镜像、失败镜像
和封存规范日报形成不满足任何单因分类的差异模式；R3已证明规范生产环境身份完整，但线程/资源、
进程路径和整体镜像差异并存，权威`PRODUCER_ENVIRONMENT_IDENTIFIED_NOT_CAUSALLY_PROVEN`。Top20
继续禁止，建议关闭M6-3C连续诊断并先进入A1-0；未来R4须另立单变量协议和授权。
科创三池独立机制批次仍须单独排期。
完整依据、资源比例和停止条件见
`docs/PLATFORM_ROUTE_REVIEW_20260806.md`和
`docs/M6_CSI800_TOPK20_CONVERSION_ENGINEERING_ACCEPTANCE_20260807.md`。

## 依赖顺序

1. 先完成当前锁竞争修复后代码版本连续三次“信号 → 下一交易日开盘对账”的前瞻影子验收。
2. 验收期间不修改中证800生产基线、调仓规则、数据口径或门禁。
3. 三次完整闭环验收和健壮性复核通过后，先施工 P0.5“模拟组合与前瞻绩效闭环”，把信号、真实成交约束和累计结果连接起来；P0.5 不反向改变模型或调仓规则。
4. P0.5 通过后，依次施工“资金流候选验证”和“科创50独立基线”；二者不得同时写入生产基线，确保结果可归因。
5. 后台结果线始终优先。P0 验收期间可旁路优化 Web 和 P0.5 的信息架构、指标草案、查询契约草案和低保真线框，但不写代码、不启动服务、不读取未登记的临时数据。

6. P0.5 首批后台指标、账本和查询契约稳定后，才施工 Web 代码；Web 首版只读，不连接券商、不提供下单或在线改参，且不得阻塞 P1/P2。

## 横向工程治理

自 2026-08-05 起，所有阶段共同遵守 `docs/ARCHITECTURE_CONSTITUTION.md`。新股票池、因子家族、
模型、查询或页面优先继承注册表、PIT、成本/执行、门禁、账本和只读投影，只把合法差异放入窄配置或
插件；不得复制研究流水线或在前端形成第二套口径。重大跨层变化先写 ADR，代码交付必须通过
`make architecture-check`。现有超大模块按机器清单冻结上限并小步拆分，不启动无目标的全盘返工。

2026-08-07 增加受控整理与瘦身里程碑。首次完整检查点在 M6 系列关闭后、下一项重大策略池或 Web
能力前；若跳过 M6 后续，则在下一重大能力前触发。`A1-0` 只读审计先建立入口/依赖/重复/无引用候选
和保护清单，不删文件；用户复核逐文件证据后，`A1-1` 才能小批删除或拆分。不可变证据、冻结协议、
失败历史、迁移、发布回滚和唯一复算实现不属于普通废代码。见
`docs/CODEBASE_CONSOLIDATION_PLAN_20260807.md`。

`A1-0`已于2026-08-07完成：当前121,353行已跟踪代码中没有满足全部删除门的安全删除文件；确认一处
`src -> tools`反向依赖和两个Python循环依赖。A1-1建议严格分成三个可独立回滚的包：先把P2-2C/M4
共享的科创50纠错执行内核提升到`src`，再分别拆D1 transport/llm_factor和M3 data/release循环。
Web、D1、config大热点继续受no-growth棘轮约束，不做全盘重写。A1-1须用户先复核清单后另行授权，见
`docs/A1_CODEBASE_INVENTORY_20260807.md`。

A1-1A前置检查随后确认上述执行内核无法原地无损迁移：M4 v1写死旧executor路径/物理SHA，M4 recovery
release和P2-2C又分别绑定代码身份。为架构整齐改wrapper或切换import会破坏历史复算，故A1-C01改为
`DEFERRED_FROZEN_IDENTITY_CONFLICT`并保持唯一债务机器隔离；待版本化successor或归档ADR后再评审。
下一安全整理包改为A1-1B D1解环，仍须单独继续指令。见
`docs/A1_STAR50_EXECUTION_MIGRATION_DECISION_20260807.md`。

A1-1B已于2026-08-07完成：新增纯D1 typed contract/request planning seam，旧公共导入兼容，
`llm_factor.py`由1,254行降至944行，`deepseek_client.py`保持808行；全仓Python循环由2降至1，D1请求
身份、mock transport、恢复与敏感输出语义不变，且未调用LLM或研究。下一安全整理包为A1-1C M3
data/release解环，必须另收继续指令，不自动施工。见
`docs/A1_D1_TRANSPORT_CONTRACT_EXTRACTION_ACCEPTANCE_20260807.md`。

A1-1C已于2026-08-08完成：保留具体`M3DiscoveryIdentity`原模块与字段形状，只在内层M3合同增加
结构端口，使release不再反向依赖data实现；全仓Python循环由1降至0。M3协议、release、输入、CLI和
研究证据均未改变。至此A1首次整理检查点关闭，唯一`src -> tools`债务继续按A1-1A冻结裁决隔离；
后续不自动继续无目标重构。见`docs/A1_M3_DISCOVERY_IDENTITY_CONTRACT_ACCEPTANCE_20260808.md`。

截至 2026-07-23，P0 三次闭环和验收后通知健壮性复核均已完成：飞书瞬时网络、HTTP 408/425/429/5xx 与响应解码异常采用最多 3 次的有界退避，同一逻辑消息固定 `message_id`，每次尝试追加留痕；通知仍不改变核心任务退出语义。P0.5 已冻结 500,000 RMB 和 `paper-v1`，工程、Docker 接入、四日 BACKFILL 及首个自然 `FORWARD` 验收全部 PASS；完整证据见 `docs/P05_FORWARD_ACCEPTANCE_20260723.md`。

## R0.5 · 模拟组合与前瞻绩效闭环

### 目标

把不可变信号、真实下一交易日开盘约束和持续组合会计连接起来，回答“正式信号在真实可成交约束和成本后取得了什么结果”，而不是只统计目标权重或单日可成交性。

### 当前边界

- 正式模型主基线只运行 `model_baseline`；另有 `model_top20` 作为同一 Top30 信号排名前20的独立
  模拟比较账户，不是新模型。Top20 六日 BACKFILL 工程已 PASS，但自然 FORWARD 为0、生产自动日更
  未启用；不接券商、不生成真实订单。
- 正式买卖时点固定为信号后下一交易日官方开盘。止盈、止损、限价区间或 LLM 动态择时不进入首版；若后续研究，必须作为独立、预注册的执行策略臂。
- 实际成交、持仓和现金是组合真身；涨跌停、停牌或行情缺失导致未成交时，不得用目标持仓覆盖实际状态，也不得事后追价补成交。
- 未来对话或 LLM 研判只能进入独立 `llm_judgment` 账户，不能污染模型基准仓或生产信号。

### 施工输出

- 版本化执行策略，以及追加式订单、成交、持仓、现金、公司行为和每日净值账本。
- 每笔订单到信号哈希、行情批次、成交判定、费用和持仓变化的完整证据链。
- 扣成本净值、中证800净超额、最大回撤、换手、成交率、开盘偏差、现金拖累和个股贡献。
- `paper_portfolio_snapshot`、`paper_orders_fills`、`paper_nav_series` 三组首批只读查询契约。
- Docker 日任务、重复运行幂等、异常恢复、飞书守护和脱敏后的运行证据。

### 通过条件

- 无未来数据；成交只能使用执行时点已知的信号、交易状态、官方开盘和事前费用规则。
- 每日满足“净资产 = 现金 + 持仓市值”，订单 → 成交 → 持仓 → 现金 → 净值可逐项重算并通过哈希验证。
- 同一输入重复运行产生相同结果且账本不重复追加；`.BJ` 为 0，失败不覆盖旧证据。
- 公司行为、不同板块交易单位、最低费用和无法成交语义均有实测 fixture 与边界测试。
- 首版只读、无券商连接、无在线改参；Web 只能查询已登记结果。

### 禁止事项

- 不把当前目标权重当成已成交持仓，不以复权价格伪造真实现金流。
- 不观察后续行情后补写买卖点、成交价、止盈止损或未成交处理。
- 不混合模型基准仓、未来因子研究仓和 LLM 主观研判仓的资金、成交或业绩。
- 不以绝对盈利替代相对中证800、扣成本、含回撤的结果判断。

## R1 · 每日主力资金流候选

### 目标

判断数据商口径的主力/大中小单资金流，在现有 Alpha158 量价特征之外是否提供稳定、可交易的独立信息；不以“成功采集”或“单因子历史收益为正”作为成果。

### 当前边界

- 当前正式 Alpha158 使用价格、成交量、VWAP 及滚动量价关系，不包含 `moneyflow_ths`/`moneyflow_dc` 等显式主力资金流特征。
- 已有 `amount`、`turnover_rate`、`volume_ratio`、市值等字段属于量价/流动性信息，不等同于数据商定义的“主力净流入”。
- 北向资金继续服从 `DATA_SPEC.md` 冻结口径：2024-08-19 起无连续日度披露，不作因子。

### 施工输出

- Tushare 资金流原始批次、冻结字段 schema、请求参数、内容 SHA-256 和追加式采集账本。
- 发布时点实测报告：明确当日 19:30 是否稳定可用；若不能证明，则统一按下一交易日可用处理。
- 覆盖率、缺失率、重复键、修订、极值、停牌日行为及北交所排除报告。
- 对行业、市值、成交额/换手及现有 Alpha158 暴露残差化后的候选特征和证据。
- 与现有基线同窗口、同成本、同调仓条件下的增量 RankIC/ICIR、净超额、回撤、换手和稳定性报告。

### 通过条件

- PIT/shift、完整性和幂等门禁全部通过；来源不可用或发布时间不确定时失败即停。
- 残差化后仍有跨窗口稳定信息，并使组合扣成本结果相对 Alpha158 基线产生可复现增量。
- 通过冻结的 G1 准入规则后才允许进入正式因子库；普通 REJECT 保留证据，不调整门槛追结果。

### 禁止事项

- 不把数据商的“主力”标签当成交易所统一事实。
- 不因原始相关性、单窗口 IC 或回测收益好看而直接入模。
- 不用回测期内观察到的发布时间为同日可用性背书。

## R2 · 科创50独立策略臂

### 目标

验证科创50能否在中证800主策略之外形成经成本和风险约束后仍成立的独立结果；不预设指数名称不同就代表有效分散。

### 当前边界

- 指数代码固定为 `000688.SH`；PIT 股票池以官方首批名单和历史调整事件构建，未来独立研究按冻结的等权口径，不使用 Tushare 历史权重数值替代官方成员事实。
- 当前科创板 20% 涨跌停识别、全市场行情/复权/基本面采集和通用 qlib 构建能力可以复用。
- 中证800仍为主策略池；科创50使用独立配置、数据集标识、基准、模型、信号、实验账本和准入结论。
- 2026-07-24 P2-0 已按 `p2-star50-protocol-v1` 完成数据/PIT 门禁并裁决 NO-GO：源采集层
  PASS，但首份权重按 T+1 仅从 2020-08-03 可用，冻结起点缺 7 个交易日；Tushare 又不提供历史
  版本或修订字段。未授权构建 qlib、模型、回测或信号，证据见
  `docs/P2_STAR50_DATA_FEASIBILITY_ACCEPTANCE_20260724.md`。
- v1 NO-GO 永久保留且不代表 P2 整体失败。主控随后在任何策略效果查看前冻结并推送
  `p2-star50-protocol-v2`，只施工官方成员谱系门：官方首批 50 只和 24 期调整公告可追溯，82 对
  替换事件按公告生效时点重建后，2020-07-23~2026-07-24 每个交易日均为 50 只，与 72/72 个
  Tushare 月度集合精确一致，`.BJ=0`。v2 数据门 GO；Tushare 只作二级集合核验，未使用权重数值。
  当前仍为 `engineering_complete=false`、`strategy_results_inspected=false`、
  `production_authorization=none`，证据见
  `docs/P2_STAR50_OFFICIAL_LINEAGE_ACCEPTANCE_20260724.md`。
- P2-1 已在真实效果查看前冻结并推送 `p2-star50-engineering-v1`，显式枚举 72 个预期月份并以
  缺月/双快照对抗 fixture 补强输入门。official daily membership 唯一驱动 72,800 个成员日、独立
  数据集和动态 qlib instruments；真实 qlib 结构、覆盖、哈希和幂等 PASS，完全 synthetic 的
  Alpha158/LightGBM/TopK/backtest 六阶段 PASS。机器结论 `engineering_complete=true`，
  `strategy_results_inspected=false`、`strategy_effective=NOT_EVALUATED`、
  `production_authorization=none`；证据见
  `docs/P2_STAR50_ENGINEERING_ACCEPTANCE_20260724.md`。
- P2-2 已在任何真实 handler/model/backtest 前冻结并推送 `p2-star50-effect-v1`，原三窗/压力/成本
  数值和 54/54 确定性产物仍可复算；但模型标签末 11 个信号日未 purge，valid early stopping 使用
  test 期价格，执行又以当日收盘 flags 判开盘且卖单未执行冻结 5% 容量。故原报告永久标记
  `original_p2_2_model_valid=false`、`original_p2_2_execution_valid=false`；旧 `NO_GO/REJECT` 不是权威
  历史效果裁决，旧提交/报告/账本/产物不得删除或重写。失效说明见
  `docs/P2_STAR50_EFFECT_INVALIDATION_ADDENDUM_20260725.md`。
- P2-2C 已在纠错结果前提交并推送 `c6fbbaf`，且披露旧结果和主控敏感性已可见，明确不是盲预注册
  或新变体。纠错范围严格只有 train/valid 标签 t+11 成熟 purge、执行日 raw open/pre_close/tick 与
  prior-close 时钟、买卖双向信号日中位 amount 5% 容量；其余模型、窗口、test、组合、成本与门槛
  逐字段不变。三窗基础净超额 -8.51%/-19.25%/-23.87%，pooled 基础/2x/额外滑点 -52.97%/
  -56.19%/-56.02%，三测试窗和 microcap_2024 回撤超过 20%；分散化仍 `NOT_EVALUABLE`。两遍 54
  份可比产物逐哈希一致，权威结论 `NO_GO/REJECT`、生产授权 none；本基线停止。证据见
  `docs/P2_STAR50_EFFECT_CORRECTION_ACCEPTANCE_20260725.md`。

### 施工输出

- `000688.SH` 的 index_daily、index_weight 历史批次与完整性报告。
- 独立 `star50` qlib instruments；不得覆盖 `csi800` 或阶段0冻结缓存。
- 在首次回测前冻结适合50只股票池的 TopK/n_drop、流动性、行业和单股风险约束；Top10/Top15只能作为事前候选，不能看完结果再选。
- 按科创板真实历史长度重新设计训练/验证/OOS窗口，并显式声明其证据不能与2016年起的中证800六窗口等同。
- 成本、涨跌停、极端波动、持仓集中度及与中证800实际持仓重叠的压力报告。

### 通过条件

- 历史成分 PIT、20%涨跌停、停牌、ST、复权、量纲、北交所排除和幂等门禁通过。
- 多窗口净结果、回撤和稳定性达到事前冻结门槛。
- 与中证800联合配置前，必须证明持仓重叠、行业/风格暴露和风险贡献可接受；未通过时只保留研究结果，不配置资金。

### 禁止事项

- 不照搬中证800 Top30（30/50会显著削弱截面选择强度）。
- 不用2016年以前/科创板成立前的伪历史补足窗口。
- 不把两个股票池混合后再回头解释收益来源。

## R4 · 科创100独立策略臂

### 目标

在不干扰中证800生产主线、也不覆写科创50失败证据的前提下，判断科创100能否形成可审计、可归因
的独立研究与策略臂。官方数据可行、工程可运行和策略有效必须分层裁决。

### 当前边界

- 用户已决定把拟议的自建“科创300”改为官方科创100；指数固定为 `000698.SH`。
- P4-0 `p4-star100-data-protocol-v1` 已于2026-07-26在真实采集和任何效果查看前冻结并完成。官方
  首批名单、规则版本、指数日线和Tushare月度集合源均PASS，但12期历史调整的官方成员对真身未能
  从当前公开归档闭合，PIT数据门权威NO-GO；该结论不评价策略效果。
- 指数2019-12-31基日不等于当时已有可知成分；最早可能研究可用日固定为正式发布日2023-08-07。
- 2025-03-17起新股纳入时间由满6个月改为超过12个月，并采用新老样本划断；规则版本必须按时点
  记录，不得用现行规则回写历史。
- 科创50的Top10、`n_drop=2`、10日调仓、窗口和模型只属于已REJECT的P2具体基线，不自动继承。
- 中证800继续是唯一生产主策略；P4使用独立目录、配置、数据集、账本、模型、信号和准入结论。

### 分阶段输出

1. **P4-0 数据门（已完成，NO-GO）**：官方首批100只与规则版本PASS；718/718指数日线、35/35月度
   集合和幂等PASS；12期季度调整的官方历史成员对缺失，使逐日PIT成员与完整交叉核验不可执行。
2. **P4-1 工程门（阻断）**：仅在P4-0经新协议和合格官方历史源恢复为GO后，建设独立数据集/qlib
   与完全合成的端到端通路；不读取
   真实策略效果。
3. **P4-2 效果门**：仅在P4-1 GO后结果前冻结模型、窗口、组合、成本、容量、风险和比较门槛，再作
   唯一真实运行。
4. **前瞻/生产**：只有历史门通过后才有资格另议，不能因数据或工程GO自动进入。

### P4-0通过条件

- 官方首批名单和规则版本完整，公告扫描越过发布日前边界，定期/临时调整无未解释缺口。
- 2023-08-07起每个官方交易日恰好100只，重复、未来公告回填、当前成分回填和`.BJ`均为0。
- 指数日线官方交易日覆盖100%；35个月度Tushare成员集合与官方逐日谱系精确一致。
- 原始/派生证据不可覆盖、逐哈希可重算；相同入口复跑零新请求、零改写。

### 禁止事项

- P4-0期间不构建qlib，不计算特征/IC/收益，不训练、回测、排名、选参或生成信号。
- 不以2019基日、当前成分、ETF持仓或第三方成分库补造官方发布前历史。
- 不把科创50失败解释成科创板或科创100无效，也不把科创100数据GO包装成策略有效。
- 不修改中证800生产代码/配置/模型/门禁，不重建或重启scheduler。

## M1 · 多股票池因子研究底座

### 目标

把中证800、科创50、科创100、科创200、科创综指和规则型科创板研究池置于统一但不混淆的身份、
PIT数据门与因子裁决合同下。因子定义可跨池复用，但每个股票池独立评价、独立准入，不能用某一池
的历史结果替代另一池，也不能把数据或工程GO包装成策略有效。

### 当前边界

- M1-0已登记8个股票池身份。中证800保持唯一既有生产池；科创50官方PIT数据和工程证据完整，允许
  未来另立新因子协议，但旧P2具体基线REJECT继续有效。
- 科创100官方历史谱系仍为NO-GO；科创200 M2-0R2已补齐2026-07二级快照并使24个月Tushare源门
  PASS，但公开官方归档没有历史成员对，24个月只有1个月能与无调整首批集合一致，PIT数据门仍权威
  NO-GO。科创综指尚未完成数据门。
- 三类自建科创板PIT池的数据与规则门已GO：从2021-01-04起连续67个形成月可构造，全市场及市值
  三等分中盘/小盘身份稳定；这只允许另立因子协议，策略仍`NOT_EVALUATED`、生产授权none。
- 自建全市场/中盘/小盘池必须标`CUSTOM_RULE_BASED`，不得冒充官方科创100、科创200或不存在的
  官方“科创300指数”。
- 因子准入键必须包含股票池、基准、标签、预测期限、中性化、窗口、成本和裁判版本；生成尝试与跨池
  评价单元分开记数。

### 后续顺序

1. M1-1已完成科创50新价量因子发现批：40/40个DeepSeek响应中14条完成发现评价、26条规则拒绝，
   机械Top2已锁定；不调旧P2模型和组合参数，2023—2025仍封存，策略仍`NOT_EVALUATED`。
2. M1-2已在第1/8份响应因自由文本语义合同失败权威停止；schema虽PASS，但候选裁决权为空。本批
   不补发、不续跑、不修式、不递补，策略仍`NOT_EVALUATED`。
3. M1-3因M1-2未通过而不获授权；2023—2025验证/G1继续封存。未来若重启须用户另立全新结果前
   研究批次，不能回救本批。
4. M2-0R2已证明二级快照补齐仍不能替代官方PIT谱系，公开归档盲扫路线停止；只有取得带发布/生效/
   版本/修订语义的授权历史成分源后才能另立恢复协议。在此之前禁止计算科创200因子，可把研究资源
   转向科创50或三类已有合法PIT的自建科创池。M3-0与M3-1已GO；M3-2完成24/24个真实响应，15条
   发现评价、9条规则拒绝，三池8条
   合格并机械锁定序号3和4为Top2，实际费用`$0.053605862`。策略仍`NOT_EVALUATED`，验证/G1/模型/
   生产继续封存；如继续只能另立M3-3审查固定Top2，不递补或追加候选。
5. 只有因子在指定股票池独立通过PIT、覆盖、稳定性、成本、容量、压力和多重检验，才可另议模型与
   独立模拟账户；任何历史GO不自动进入前瞻或生产。
6. M4-0另立科创50基准残差因子家族，固定残差动量、残差反转和低特异波动三个候选；数据/预执行门
   已GO。M4-1已结果前冻结2023—2025六个半年窗、PIT中性化、Alpha158增量、成本、压力与统计全门，
   当前仍未读取效果；科创50短历史不能冒充中证800固定窗口的正式G1-v1。

完整机器合同和禁区见`docs/M1_MULTI_UNIVERSE_FOUNDATION_PROTOCOL_20260801.md`。

## M5 · 多股票池策略工厂

### 目标

把股票池身份、研究家族、尝试数、证据等级、权威裁决和下一合法动作统一成可审计的平台合同，并通过
Web持续展示“哪些池可研究、做过什么、为什么失败、当前能做什么”。平台化不等于自动试参：每个真实
研究仍须是有界批次，终态不自动派生下一轮。

### M5-0完成边界

- 首版只建立M1八池身份的追加式状态覆盖、既有P1/P2/F1/F2/M1/M3/M4工作包目录、正式准入账本
  核验、内容寻址write-once投影和`GET/HEAD`查询；Web提供研究地图、家族矩阵、历史证据和本地临时
  草案。
- 当前事实固定为登记8、可建立草案5、数据/PIT阻断3、既有生产1、正式准入0、活跃授权任务0；
  科创50的研究REJECT不等于股票池失败，科创100/200/综指的阻断不等于策略REJECT，三个规则池必须
  常驻`CUSTOM_RULE_BASED`。
- 本轮不建立SQLite控制库、不增加写HTTP、不启动Worker、不调用DeepSeek/Tushare、不读取效果、
  不训练或回测、不改变模拟仓/生产/scheduler。浏览器草案始终是
  `DRAFT_NOT_SUBMITTED / NOT_FROZEN / NOT_RUN`。
- 未来写能力须使用独立`research-control`，按草案、复核、批准冻结、独立批准执行分步；真实研究还须
  单独批准外部调用和封存效果，历史GO不能自动进入前瞻或生产。

完整协议见`docs/M5_STRATEGY_FACTORY_PROTOCOL_20260805.md`；研究治理、后台与Web专项设计分别见
`docs/M5_MULTI_POOL_RESEARCH_GOVERNANCE_20260805.md`、
`docs/M5_MULTI_POOL_BACKEND_ARCHITECTURE_20260805.md`和
`docs/M5_MULTI_POOL_WEB_PRODUCT_20260805.md`。M5-0只读工程已GO，验收见
`docs/M5_STRATEGY_FACTORY_ACCEPTANCE_20260805.md`。

### M5-1完成边界

- 已建立独立`research-control`与SQLite v1三表操作态，只持久化`NON_AUTHORITATIVE_PROPOSAL`、状态事件
  和幂等回执；只开放create、submit-review、cancel，状态上限为`REVIEW_REQUIRED`。
- 所有提案严格绑定M5 v2快照、M1股票池注册表、五个可选池、五个研究家族及补正后的primary/
  sensitivity multiplicity；确定性意向固定零调用/零费用，LLM意向只登记上限，不产生调用授权。
- 全库双向重建proposal、event与receipt；孤儿、缺失、重复、错配或伪响应均使health/read/write失败关闭，
  写入错误在同一事务提交前回滚。Web写入另受loopback Origin、短会话、CSRF、限流、稳定幂等键和内部
  Docker secret约束。
- 控制服务无宿主端口、只在内部网络、只读挂载冻结证据，唯一窄写路径为本机控制库；Web仍只绑定
  `127.0.0.1:8080`，生产scheduler、行情、标签、研究结果和研究账本均未接入。
- M5-1的GO只代表“可建立并提交人工复核的提案控制面”。freeze、approve、release、enqueue、run、
  Worker、DeepSeek、效果读取、前瞻和生产全部物理不存在；M5-2必须另立ADR、结果前协议并重新获得用户
  授权，不能从`REVIEW_REQUIRED`自动推导。

协议、补正与架构决策见`docs/M5_RESEARCH_PROPOSAL_CONTROL_PROTOCOL_20260805.md`、
`docs/M5_RESEARCH_PROPOSAL_CONTROL_CORRECTION_PROTOCOL_20260805.md`和
`docs/ADR_0001_M5_PROPOSAL_CONTROL_PLANE.md`；验收见
`docs/M5_RESEARCH_PROPOSAL_CONTROL_ACCEPTANCE_20260805.md`。

### M5-2A当前边界

- 首份动态基本面提案已另立ADR和结果前协议：科创50为home，科创板中盘/小盘为迁移池；八个固定
  公式、方向、PIT/分母/548日陈旧度、十个半年数据段、未来六窗/压力期和24单元均已冻结。
- 协议冻结后八个公式正式计入研究尝试：动态基本面`N=14`、基本面联合敏感性`N=20`；效果测试仍为0。
  候选数据失败不退回N，不补候选，不因三池相关性折减N。
- M5-2使用独立gate registry；M5-1零迁移。protocol scope与DATA/ENGINEERING两个release scope分离，
  两个门分别批准；没有常驻Worker、队列、外部调用、标签/效果、模型、回测或生产权限。
- 数据门可产生FULL/PARTIAL/NO-GO：候选必须三个池全过才可进入未来验证；PARTIAL只允许全部且仅
  DATA_GO候选未来统一验证，不能按覆盖高低挑选。任何数据GO都不是因子有效。
- 当前只完成M5-2A protocol-only冻结。下一步先施工并推送M5-2B数据门实现/release；只有用户对精确
  data release scope另行批准后，才可在断网短命Docker中读取既有真实数据。工程门仍须DATA_GO后另批。
- 冻结提交为`98f2d10b...1198b6`，protocol scope为`ab8c3396...b17557`；scope本身不包含任何门执行
  权限，完整验收见`docs/M5_DYNAMIC_FUNDAMENTAL_CROSS_POOL_PROTOCOL_FREEZE_ACCEPTANCE_20260805.md`。

协议与架构见`docs/M5_DYNAMIC_FUNDAMENTAL_CROSS_POOL_PROTOCOL_20260805.md`、
`docs/ADR_0002_M5_2_GATE_REGISTRY.md`；机器真身为
`config/m5_dynamic_fundamental_cross_pool_v1.yaml`。

### M5-2B施工边界

- 先冻结construction-only合同，再施工独立registry、M5候选级PIT/公式/门阵、独立auditor和离线
  Docker；真实测试只用合成fixture，正式registry和门事件保持不存在。
- 月末形成固定映射下一SSE开市日成员；M3还须校验源`formation_date`。候选只连接自身所需报表，
  不复用F2三表共同期；有效形成月固定为有效横截面达到池门槛。
- 实现commit、镜像、输入身份manifest和挂载/资源全部内容寻址后，才形成data release scope并停止；
  用户批准精确scope前不读真实财务行、不计算真实候选、不运行data gate。
- 完整施工合同见`docs/M5_DYNAMIC_FUNDAMENTAL_DATA_GATE_BUILD_PROTOCOL_20260805.md`；数据门GO仍只表示
  数据可用，不表示因子有效，synthetic工程门和未来效果门继续分别授权。
- 独立registry、候选级PIT/八式/24单元门阵、write-once runner、独立auditor、元数据清单合同和
  专属断网Docker现已完成纯合成工程验证。v1 release因未绑定`/registry`作废；v2获批后因输入包漏
  build contract在协议加载阶段失败，正式event 6已留痕`semantic_rows_read=false`并恢复协议冻结态。
  v3绑定推送实现`a796066...ab0da`、镜像`sha256:738b9d7f...80d0f`、同一metadata-only输入和跨release
  write-once bundle合同；scope为`49fdc6e7...e05830`，提案于`2026-08-12 18:48:16 UTC+8`到期。
  用户已批准并运行唯一真实断网runner；语义读取后发现`balancesheet`相同冻结来源身份的业务字段冲突，
  v3以exit 2失败且零输出/audit，event 10已`STOPPED`。这不是DATA NO-GO或策略REJECT；旧case/release
  不得重跑。M5-2B-R1现已冻结superseding protocol/build v2：新scope `6f99c0df...0dc49`派生新case
  `a2539149...72068`，不改研究语义或registry schema；冲突必须封存脱敏全局NO-GO并经独立audit。
  恢复实现已由提交`18e7502...276a7`推送，六类分类、三类冲突全局封存、独立审计、旧case隔离和
  幂等均经两次断网纯合成fixture通过；release v4 scope为`8858912f...d6f65`，绑定新metadata-only
  输入与镜像`sha256:acb7c6c2...d1ea7`。用户随后已批准并完成唯一真实断网数据门：23个源身份冲突组
  被全局失败关闭，24单元全FAIL，独立audit PASS，新case进入`BLOCKED_DATA`；效果测试仍为0，策略
  `NOT_EVALUATED`。M5-2B-R2版本谱系恢复协议现已结果前冻结：scope `96c4f996...e463ccd`派生新case
  `6b6c849f...f7be16`，强制分开公告日、权威修订生效时间和本地观察时间，禁止用latest/VIP/update
  flag/多数值或删样本解冲突。用户批准精确release `b01058b5...cf155`后，唯一真实断网runner在读取
  anchor行后因`anchor conflict identity changed`以exit 2失败，零输出、零audit并进入`STOPPED`；不是
  lineage NO-GO或策略REJECT。根因是R1冲突基线限定年报且report type 1/5，R2 reader锚定前未复用同一
  行域。本release不得重跑。恢复协议与实现现已按单变量完成：reader在Observation前复用年报+type
  1/5范围，季度/其他类型对抗fixture通过；新镜像`sha256:5dd12995...12d1a`、metadata-only清单
  `bda3f6b8...35d0df`和新release `f7904929...6e2d5`均已绑定。用户批准后唯一断网runner正常封存，
  独立audit PASS：23组全部仅有本地观察版本，历史PIT可恢复0组，权威
  `NO_GO_M5_2_SOURCE_LINEAGE_PREEXECUTION / BLOCKED_DATA`；这不是策略REJECT。release已消费且不得
  重跑，按用户指令先做路线复盘，再裁定是否另立权威版本证据恢复协议。
  缺权威版本证据时的联网采集仍必须另立协议，禁止与DATA_GATE混跑。

## R3 · 专业只读 Web

### 目标

让使用者更快判断“数据是否可信、策略是否有效、今天是否需要行动”，而不是把上线页面本身当成果。

### 双轨边界（2026-07-19 更新）

- **后台主线**：P0/P0.5/P1/P2 的运行、证据、数据与研究结论优先，Web 不得反向要求修改冻结口径。
- **设计旁路**：近期连续打磨页面任务模型、信息架构、指标定义、查询契约和线框，产物只落文档。
- **代码闸门**：P3-0 至 P3-4B 均已验收 GO，Web 1.0 七类本机只读页面完成。新增指标、远程部署或写能力仍须另立目标，且不得改变冻结后台口径。
- **模块治理**：2026-07-27 第一阶段已把查询、前端校验与样式三个最高风险单体拆为职责模块，并以
  600行上限和依赖方向测试形成增量闸门；API与CSS产物等价。其余大型文件按真实维护需求另立目标，
  不连续开展无业务收益的全面重构。
- **真实性闸门**：线框可使用明确标注的示例值；正式页面的每个数字必须能追溯至账本、不可变产物或哈希证据。

### 参考范式

- Grafana：运行状态、时间序列、告警和下钻。
- MLflow：实验、参数、指标、模型与产物追溯。
- TradingView：K线、指标叠加、缩放和标的比较交互。
- Ant Design：中文企业应用的信息层级、导航、筛选、表格和详情页。

参考其信息架构和交互范式，不复制品牌视觉；整体采用中性、克制、桌面优先且可响应式查看的专业金融风格。

### 页面进度

1. **总览（P3-1 已完成）**：数据截止时间、核心/通知状态、最新信号、FORWARD 观察和证据下钻。
2. **模拟组合（P3-1 + 双账户切换 + 中文简称已完成）**：BACKFILL/FORWARD 分层、实际持仓、会计
   事实和账户日证据；持仓以PIT中文简称优先、代码保留为审计标识；Top30默认，Top20独立切换且当前
   0前瞻，不做跨观察类型绩效比较。
3. **股票池与信号（P3-1 已完成）**：排名、目标/实际权重、计划差、执行证据和哈希身份；当前后台未提供因子贡献时不生成前端解释。
4. **因子工厂（P3-3C 已完成）**：因子目录、tear sheet、严格可比分析和追加式准入/拒绝历史已接入
   真实只读投影；正式库 0、当前权威 REJECT 8、仅历史 2 如实展示，四类未统一登记指标固定
   `NOT_EVALUATED`，不由 Web 补算。
5. **模型与回测（P3-4B 已完成）**：真实 783 条实验记录按 evidence tier、authority、lifecycle 与
   outcome 分层；目录使用后台精确筛选/分页，详情展示 G1 十五门和 P2 类型化窗口证据，原 P2-2
   明确失效并链接权威 P2-2C。页面不扫描账本、不补算指标、不做表现排序。
6. **数据质量（P3-2B 已完成）**：数据 PASS 与证据 WARN 分列，展示 S1—S10、批次、来源、哈希链、原始重验边界与 `.BJ` 三层门。
7. **系统运行（P3-2B 已完成）**：核心/通知状态分列，展示调度阶段、失败恢复、脱敏通知、release 与 recorded-only heartbeat。
8. **策略工厂（M5 Web权威投影v3已完成）**：8池/8工作包和非权威草案控制保留；最新M5来源谱系门
   以独立终态展示`BLOCKED_DATA`、23组仅当前观察版本、0组历史PIT可恢复和策略`NOT_EVALUATED`。
   旧v2不可变保留，v3缺失或证据漂移时失败关闭，不回退旧结论。

### 通过条件

- 先冻结指标字典、状态机、API/查询契约和低保真线框，再写前端代码。
- 首页优先显示结论、异常和数据时点；所有收益同时展示基准、成本与回撤。
- 页面中的数字可下钻到运行、信号、实验或原始批次证据。
- 不显示 token、Webhook、签名或其他秘密；首版不写生产数据、不下单、不在线改参。

## G8-DATA · 法定产品证据链

### 目标

从冻结日起持续保留六只产品的法定份额净值、累计净值、分红/除息说明和实际申赎费率有效期，形成
G8 三年裁决可逐字复核的输入证据；采集成功不代表产品或策略效果，也不能提前输出 G8 方向。

### 当前边界

- G8-0 已确认证监会基金电子披露站对六只产品 `2026-07-15~2026-07-24` 各有 8 个唯一可用 A 类
  估值日，法定文件和分红备注入口均存在，机器裁决 `GO_G8_1_PRIMARY_CAPTURE_ONLY`。
- 监管站机器入口为 HTTP，HTTPS TLS 探针失败；监管主源未和基金管理人 HTTPS 逐值核对前，只能标
  `PRIMARY_CAPTURED_UNAUTHENTICATED`。
- G8-1 原 Docker 执行的空体 502 和错误完整 SHA 已永久留证；G8-1R 只改变执行环境后，主源采集
  首遍追加 54、次遍追加 0、断网 Docker 独立复核 PASS。账本保留 1 条原失败和 54 条恢复正常证据。
- 主源采集器、不可覆盖账本和本批原文已完成；管理人 HTTPS 交叉核验、费率文档下载/有效期谱系、
  总收益构造和 G8 裁决账本仍未实现。
- G8-2 已完成首轮证据门并权威 `NO_GO_G8_2`：管理人 HTTPS 八日逐值 1/6、完整费率有效期谱系
  0/6；G8 继续 `NOT_READY`，禁止进入 G8-3。若未来恢复须另立协议、永久保留 19 条失败/通过证据，
  且不得用 HTTP、CNINFO 或聚合站替代管理人 HTTPS。

## L1 · LLM 持续因子研究线（D1-3A 已 STOP；语义恢复工程门 GO）

### 长期定位

LLM 因子挖掘是筛微未来的主要研究工作，但不是每日生产策略的控制者。输入范围不局限于观象，
还包括筛微本地结构化数据、论文/规则/研报/网页、开源代码、后续经授权的数据源，以及成功与
失败实验记忆。LLM 负责检索、假设、代码和变异；确定性代码、G1 和真实影子结果负责裁决。

### D1-2A 当前边界

- 2026-07-25 已裁决采用筛微自建窄控制面、首轮受限量价 DSL、固定知识包、正负实验记忆和不变
  `g1-v1`；外部框架不接管生产管线。完整裁决见
  `docs/D1_LLM_FACTOR_RESEARCH_ARCHITECTURE_20260725.md`。
- D1-1 已实现 40 次确定性排程、严格 JSON schema、追加式尝试账本、实验总账一一对应、mock provider、
  DSL 沙箱和断网只读 Docker fixture；不调用 API、不生成真实候选、不启动常设研究服务。验收见
  `docs/D1_LLM_FACTOR_ENGINEERING_ACCEPTANCE_20260725.md`。
- D1-2A 已基于官方合同冻结 `deepseek-v4-pro` thinking/high、价格、system prompt、五主题模板、
  同主题全历史反馈序列和 10 条知识 manifest；受限客户端只在后续授权后读取 secret/建真实 transport，
  已有请求前累计费用熔断、完成响应 write-once 恢复和 `BILLING_UNCERTAIN` 停机账本。全程零真实
  API、零行情、零 G1，验收见 `docs/D1_LLM_FACTOR_PREEXECUTION_ACCEPTANCE_20260725.md`。
- 公开的 Alpha-GPT、AlphaAgent、RD-Agent-Quant、Chain-of-Alpha、CogAlpha、QuantaAlpha、
  FactorEngine 和 AlphaQT-Bench 均作为组件与测试思想来源，不继承其作者回测结论。
- 观象只可能作为多源输入之一；是否建立单向只读特征桥，须同时处理其现有“平台完全独立”
  决策和短历史/PIT问题，不能直接连接数据库或把观象判断当事实标签。
- 完整定位、已调研范式、触发条件和待议问题见 `docs/LLM_RESEARCH.md`。

### D1-2B 终态与 D1-3 边界

D1-2B 已按结果前冻结完成 `deepseek-v4-pro` thinking/high 的恰好 40 个响应：36 个完成发现期评价，
2 个重复 AST 和 2 个沙箱拒绝按协议计 N；实际费用 `$0.076626207`，低于 `$1` 单批熔断。首次运行在第
1 份后暴露控制流缺陷，恢复附录先行锁定既有响应/账本/产物并只修独立反馈和连续恢复；序号 1 未重发，
其余 39 份连续完成。断网、无密钥、只读重放 0 外部调用且证据哈希不变。机械 Top2 已锁定；D1-3 的
人工经济解释/泄漏审查和 `g1-v1` 必须另立目标，不通过不递补、不调门槛、不追加候选。当前
`strategy_effective=NOT_EVALUATED`、`production_authorization=none`。

D1-3A 已按结果前原则完成 Top2 对抗复核：固定两条表达式，四角色×两候选恰好 8/8 份 DeepSeek
响应均通过结构 schema，实际费用 `$0.01472214`。但后续自由文本语义审计发现 3 份正文建议替换公式/
构造变体，同时结构字段声明没有提出变体，违反冻结 prompt。按“8/8 有效、无效不替换”规则，原
`GO_INDEPENDENT_HUMAN_GATE` 已权威改判为 `STOP_SEMANTIC_CONTRACT_VIOLATION`。本批不进独立人工闸、
不递补、不改式、不读 W1—W6/压力期、不运行 G1；未来只能另立新指令和结果前协议。

2026-07-26 已先冻结并完成 `d1-review-semantic-gate-v1`：确定性门在结构 schema 之外检查自由文本的
公式/窗口/变体建议、完整 DSL、回看期、业绩/准入声称，并把非英文、不完整 DSL 和模糊修改语言统一
转为人工停止。旧 8 份证据离线双跑精确复现 5 PASS/3 FAIL；工程裁决
`GO_SEMANTIC_GATE_ENGINEERING_ONLY`，零 provider 调用、零新增费用。它只解除“未来新批缺少语义门”
这项工程欠账，不解除旧批 STOP，也不自动授权新批、人工闸或 G1。

## 执行状态

| 顺序 | 工作包 | 当前状态 | 启动条件 |
|---|---|---|---|
| P0 | 当前版本三次“信号 → 次日开盘对账”验收 | 3/3 完成；核心 PASS、通知 WARN | 2026-07-22 已通过 |
| P0-R | 验收后健壮性复核 | 已完成；有界重试、稳定消息 ID、逐次留痕和恢复标识通过 | 2026-07-22 已通过 |
| P0-E | 16:00日增量早探测 | v1/静默补正工程GO；20260804发布未派发、自然整链PASS_WITH_NOTIFICATION_WARN；20260805恢复守护预执行GO，候选0640574b仍未提升，旧release healthy | 仅在20260805 16:05—19:00实时门全过时执行一次；STARTED后验新生产、完成时点、两账户、通知、幂等与零人工修数 |
| P0.5 | R0.5 模拟组合与前瞻绩效闭环 | 已完成；四日 BACKFILL + 首个自然 FORWARD PASS | 2026-07-23 已通过 |
| P0.5-T20 | Top20 同信号模拟比较账户 | 工程/六日BACKFILL/Web/发布均GO；20260803首个自然生产FORWARD核心PASS、切换期通知WARN；持续OBSERVING | 继续自然日更，不以一个自然日比较优劣；后续积累可比账户日并保持独立重放、通知与幂等门 |
| P1 | R1 主力资金流候选 | 已完成；六候选同预算 G1 全部 REJECT，正式库 0 插入 | 不追加变体、不调门槛；证据见 `P1_MONEYFLOW_EXPERIMENT_ACCEPTANCE_20260724.md` |
| F1-0 | 中证800基本面 PIT 数据与特征门 | v1：127个形成日、101,600行与六项覆盖门PASS；1行三表最新年度报告期不一致触发权威NO-GO | v1协议、产物和NO-GO永久保留，不以F1-0R覆盖 |
| F1-0R | 基本面最新共同报告期恢复门 | 质量期1个披露错峰合法回退；混期/未来/无共同期均0，六项覆盖PASS；数据门GO | 已作为F1-1冻结输入；数据GO不被效果REJECT覆盖 |
| F1-1 | 中证800基本面六候选效果门 | 固定经济方向6/6 PASS；G1 0/6 PASS；六候选净超额均低于Alpha158基线；REJECT、正式库0插入 | 本家族停止，不调方向/权重/窗口/成本、不追加变体；独立新机制须新协议并计入本次N=6 |
| F2-0 | 中证800连续年度基本面动态数据/特征门 | 101,600行、六项覆盖门及PIT门大部PASS；质量期1行无严格连续年度对，权威NO-GO；策略NOT_EVALUATED | v1永久保留；如继续另立F2-0R，以合法空值+原85%/75%覆盖门裁决，不按已知1行定制门槛，恢复前不看效果 |
| F2-0R | 基本面动态合法不可估计空值恢复门 | 全样本12,325行无配对均保持全空；恢复面板与v1同SHA，六项覆盖及全部PIT门PASS；数据门GO | 已作为F2-1冻结输入；数据GO不被效果REJECT覆盖 |
| F2-1 | 中证800基本面动态六候选效果门 | 固定方向5/6 PASS；五项进入G1但0/5 PASS；累计基本面尝试N=12，正式库0插入；REJECT、生产授权none | 本家族停止，不翻方向、不调权重/窗口/成本、不追加变体；独立新机制须新协议并保留既有N=12 |
| P2 | R2 科创50独立策略臂 | P2-0 v1 永久 NO-GO；v2 官方谱系数据门 GO；P2-1 工程门 GO；原 P2-2 方法失效；P2-2C 权威历史 NO-GO/REJECT | 基线停止；不调参、不追加变体、不进入前瞻或生产；中证800仍是唯一生产主策略 |
| P3 | R3 专业只读 Web | P3-0 至 P3-4B 均 GO；Web 1.0 七类页面、策略工厂v3权威真身、双账户与PIT中文简称完成 | 保持本机只读、证据优先；不开放远程、交易或写能力，新指标先冻结后台契约 |
| P4 | R4 科创100独立策略臂 | P4-0 源采集PASS、官方历史谱系NO-GO；策略NOT_EVALUATED | 停在P4-1前；取得带发布/版本证据的官方历史成员源后另立恢复协议，禁止用当前成分或月末集合补造 |
| M1-0 | 多股票池因子研究底座 | 严格注册表、PIT用途门和跨池评价身份已GO；M3规则池另有后续readiness证据，零新生产授权 | 科创50与M3规则池可分别另立新因子协议；其余股票池按各自状态先过数据或恢复门 |
| M1-1 | 科创50独立价量因子发现批 | 40/40响应完成；14条发现评价、26条规则拒绝；$0.071831434；机械Top2已锁定；GO_DISCOVERY_TOP2_LOCKED，策略NOT_EVALUATED | 下一步另立M1-2，仅审查固定Top2的经济含义/泄漏/稳定性；通过后才可另立验证/G1协议，不递补、不接生产 |
| M1-2 | 科创50机械Top2结果盲独立审查 | 第1/8份响应schema PASS但语义合同FAIL；STOP_M1_2_REVIEW_CONTRACT，candidate_decisions为空，策略NOT_EVALUATED | 本批结束；不补发、不续跑、不递补，M1-3验证/G1不获授权 |
| M3-0 | 科创板全市场/中盘/小盘规则型PIT池 | 数据与规则门GO；2021-01-04起67个连续形成月，策略NOT_EVALUATED、零生产授权 | 作为M3-1只读输入真身；不得把规则池冒充官方指数或直接进入模型/生产 |
| M3-1 | 三自建池价量因子研究预执行门 | 工程GO；断网双跑同哈希，发现474日、封存727日、未来24响应且相关域N=270；零真实结果/零API | 另立M3-2 live release并显式授权24响应与0.50 USD熔断；验证/G1/模型/生产继续封存 |
| M3-2 | 三自建池真实价量因子发现批 | 24/24响应完成；15条发现评价、9条规则拒绝；三池8条合格，序号3/4机械Top2锁定；`$0.053605862`；GO_M3_2_DISCOVERY_TOP2_LOCKED，策略NOT_EVALUATED | 如继续另立M3-3，仅审查固定Top2的经济含义/泄漏/稳定性；不递补、不追加响应，验证/G1/模型/生产继续封存 |
| M3-3 | 三自建池机械Top2结果盲独立审查 | 第1/8份响应服务端结束状态不符合合同；schema/语义未评价、candidate_decisions为空；`$0.00320073`；STOP_M3_3_REVIEW_CONTRACT，策略NOT_EVALUATED | 本批结束；不补发、不续跑、不改提示或替换候选，M3-4验证/G1不获授权 |
| M4-0 | 科创50基准残差因子数据与预执行门 | 28,838个合法成员日三候选覆盖100%、每日最少49只；幂等/独立审计PASS；GO仅限数据预执行，策略NOT_EVALUATED | 如继续另立M4-1结果前效果协议；固定三式/方向/40-60窗口，冻结标签、中性化、Alpha158增量、成本、压力和G1后才可看效果 |
| M4-1 | 科创50固定残差因子历史效果门 | 终版独立审计PASS；方向2/3、适配效果门0/3，REJECT；正式G1未运行、正式库0、生产none；14件证据幂等零改写 | 本批关闭，不翻方向、不调公式/门槛、不追加变体；未来仅以独立经济机制新批继续科创50研究 |
| M5-0 | 多股票池策略工厂合同与只读工程门 | GO；v3已补M5权威`BLOCKED_DATA`，8池/8工作包、GET/HEAD、本机Web、五视口/axe/真实部署及scheduler隔离PASS；旧v2保留 | 已由M5-1继承只读真身；历史草案口径不改写，证据漂移失败关闭 |
| M5-1 | 多股票池非权威研究提案控制面 | GO；create/submit-review/cancel、SQLite证据链、全库双向完整性、本机Web与隔离Docker均PASS；交付验收时空库，研究尝试增量0 | 只允许人工建立/复核/取消提案；M5-2冻结、批准和执行必须另立ADR/协议并重新授权 |
| M5-1A | 科创50/中盘/小盘动态基本面提案 | `REVIEW_REQUIRED`；8个确定性候选、最多24个跨池评价单元、零provider调用/费用；实际研究尝试增量0，2026-08-12 18:48:16到期 | 停在人工复核；若继续须另立M5-2冻结/批准协议并获得用户明确授权，不自动排队或运行 |
| M5-1B | 三自建科创池资金流提案 | `REVIEW_REQUIRED`；全市场为主池、中盘/小盘为迁移池，最多8个确定性候选/24个评价单元；历史资金流N=18、计划背景N=26，实际尝试增量0，2026-08-15 12:05:02到期 | 已由M7-0协议继承；仍不代表批准、排队或执行，提案身份漂移/过期必须失败关闭 |
| M5-2A | 科创三池动态基本面数据/预执行协议 | protocol-only冻结；8式/3池/24单元、PIT/覆盖/陈旧度/未来窗与N=14/20已锁定；效果测试0、门批准0 | 先施工并推送M5-2B数据release；用户批准精确scope前不得读取真实数据，工程门须DATA_GO后另批 |
| M5-2B | 动态基本面数据门与R2版本谱系恢复 | v4冲突`BLOCKED_DATA`、旧R2 `STOPPED`均永久保留；行域恢复后新release唯一运行且audit PASS，23/23仅本地观察、历史PIT可恢复0，权威谱系`NO-GO/BLOCKED_DATA`，策略`NOT_EVALUATED` | 路线复盘已裁`PAUSE`且Web v3已同步；本release不得重跑，外网补证须另立有界协议，M5-2C/效果/生产仍禁止 |
| M7-0 | 三自建科创池资金流键级兼容门 | 权威`NO_GO_M7_0_DATA_COMPATIBILITY`：14门12 PASS/2 FAIL，4个早期池×半年覆盖低于99%；另有SH-only源正则共享缺陷但不改NO-GO；独立audit PASS | scope关闭不得重跑，不进入八候选；策略仍NOT_EVALUATED。未来若继续须另立早期半年缺口谱系恢复协议和新scope，不得降门槛或复用本approval |
| M7-0R1 | M7资金流恢复前工程门 | `GO_ENGINEERING_PREREQUISITES_ONLY`；全A源`.SH/.SZ`与科创成员`.SH`域已分离，pre-read一次性消费原语合成对抗PASS，v1三组哈希不变 | 尚未形成真实successor入口或数据release；下一步另立缺口谱系协议/新scope，保持旧门槛，精确批准前真实键和缺口读取0 |
| M7-0R2 | M7资金流早期缺口谱系 | 权威`NO_GO_M7_GAP_LINEAGE_INCOMPLETE`：2,615条缺口完整分区、冲突0，但908条只有主源全天停牌而缺独立确认；另有541条daily存在但moneyflow缺键；独立audit PASS | scope关闭不得重跑，原M7 NO-GO不变且不进入候选；未来若继续须新协议补独立交易状态证据并处理已确认交易缺键，不得算调整覆盖率或复用本approval |
| M7-0R3 | M7资金流双轨证据恢复 | 权威`NO_GO_M7_EVIDENCE_RECOVERY_INCOMPLETE`：轨A 527/527独立确认未交易；轨B 541键在全市场与单票两形态均缺失、恢复0；断网双算与独立audit PASS | scope关闭不得重跑；按路线复盘硬停止规则不立R4/R5、不算调整覆盖率、不进入八候选。策略仍NOT_EVALUATED，下一主线转A1-2只读清单 |
| M6-0 | 中证800模型/组合归因小批协议 | 三臂、六窗、11日purge、Top30/3/10日、三档成本及NW(10)+Holm已结果前冻结；真实结果0 | 已由M6-1工程门和M6-2真实release继承，不得增加第三臂、网格、多seed、新因子或组合变体 |
| M6-1 | 中证800模型归因结果盲工程门 | `GO_ENGINEERING_ONLY`；五终态、十二失败关闭、Qlib模型工厂、统计裁决和独立audit均PASS；真实拟合/效果0 | 已作为M6-2冻结上游；工程GO不代表策略有效，也不自动授权真实运行 |
| M6-2 | 中证800模型归因真实release | 唯一runner及内部双跑完成、2次尝试已消费；原auditor入口故障永久保留，正式结果已由M6-2R原算法独立恢复审计 | 原runner/scope不得重跑；效果只作M6归因，不写生产授权 |
| M6-2R | M6独立审计入口恢复 | 唯一断网恢复audit PASS；新增尝试0、effect前后同SHA；权威`PORTFOLIO_CONVERSION_BOTTLENECK_INDICATED`、生产none | 本scope关闭不得重跑；下一批最多预冻结一个组合转换变量，不增加模型且须重新授权 |
| M6-3A | 中证800 Top20组合转换结果前协议 | 仅冻结`TopK 30→20`；复用三组封存预测，两个组合假设采用NW(10)+Holm差分中的差分；真实Top20效果0、生产none | 协议推送后停止；如继续另立M6-3B合成工程目标，真实效果须新release scope和用户精确授权 |
| M6-3B | 中证800 Top20组合转换结果盲工程门 | `GO_ENGINEERING_ONLY`；Top30兼容、Top20转换、四终态、15类失败关闭、物理双跑和独立audit均PASS；真实effect/Qlib/拟合/预测/回测0 | 工程scope关闭；真实Top20效果须另立M6-3C精确release scope并由用户明确授权，生产仍为none |
| M6-3C | 中证800 Top20组合转换真实release | 唯一runner在首个W1控制臂Top30规范日报逐内容门失败关闭；`BLOCKED_PRE_EFFECT`，Top20尝试0、Top20回测0、audit未启动、生产none | 原scope永久不得重跑；如继续另立结果盲Top30兼容诊断/恢复协议，先定位差异且不得放宽门或读取Top20，新真实执行须新scope和精确授权 |
| M6-3C-R1 | M6-3C Top30兼容差异诊断 | scope `cad1928c...1fc23`获批后，original入口因tmpfs YAML被拆分而在容器创建前失败；Top30诊断0/6、Top20 0、分类NOT_EVALUATED、生产none | 本scope永久不得重跑；如继续另立M6-3C-R2结果盲编排恢复，fixture通过后重建镜像/新scope并再次精确授权，不得改变矩阵或比较口径 |
| M6-3C-R2 | M6-3C Top30兼容诊断编排恢复 | 6次Top30及独立audit完成；三路内部确定，失败镜像两适配器相同，但原M6/失败镜像/规范日报三者不等，权威`MIXED_UNRESOLVED`；Top20 0、生产none | 本scope关闭不得重跑；如继续另立零新回测R3数值谱系复原，不加容差、不读Top20，否则关闭M6-3路线 |
| M6-3C-R3 | M6-3C Top30数值生产谱系复原 | 双镜像probe、collector、独立audit均唯一完成；环境身份完整但多项竞争差异使因果未证，audit PASS；新回测0 | scope关闭；Top20历史效果继续禁止。20260809复盘裁M6-R4停止，转自然FORWARD观察 |
| A1 | 代码架构整理与受控瘦身 | A1-3A本地恢复PASS：M7终局tag已推远端，冻结镜像tar与3,480份执行证据在本机逐哈希闭合，断网只读fixture PASS；删除0 | 远端耐久性仍未就绪：镜像/忽略证据无异机副本且未做全新引擎恢复，A1-3B继续阻断。待用户指定远程服务器或私有registry后另立异机恢复门，再逐文件授权清理 |
| R2-1 | Top30/Top20自然前瞻证据检查点 | 协议FORWARD 10日/1次；其中受控追赶5日/1次，live-dual自然证据5日/0次；生产日增量17/17 PASS、次日对账16/16 PASS | 不新增代码；预计20260814核验第一次live-dual调仓，最早20260828同时核验20日/2次门。只作描述诊断，不以短样本切换生产 |
| L2-0 | 未来新因子批紧凑审查合同 v2 | 工程GO；non-thinking JSON、最大合法2655 bytes/硬限4096、12类对抗PASS、零API；不重开M1/M3 | 仅供未来独立新批引用；真实候选与调用仍须另立结果前协议、release及用户授权 |
| TS-v5-R2 | 四响应合同金丝雀 | 恰好4个独立完成响应、费用$0.006332411；四份JSON均严格Schema FAIL，有效候选0，断网独立audit PASS；权威`STOP_NO_VALID_CANDIDATES` | scope关闭不得递补或第五次调用；先离线匿名分析失败机制，新调用须新scope和用户批准；效果/回测/生产仍禁止 |
| TS-v5-R3A | 四响应离线合同诊断 | 四份共同主因是LLM侧合同投影不完整；Schema/提示缺口4/4，可见规则不服从1/4，validator缺陷0；独立audit PASS | 仅允许另立R3B工程目标，构建机制专属同源投影与确定性编译器；不放宽validator、不修补旧候选，外部调用仍须新批准 |
| TS-v5-R3B | 机制专属合同投影工程门 | 六机制proposal Schema/精确projection、确定性编译器、42/42对抗fail closed与独立audit均PASS；外部调用/效果0 | 仅可另提小批live canary scope；调用次数、费用、release须结果前冻结并由用户明确批准，参数搜索/回测/生产仍禁止 |
| TS-v5-R3C | 六机制合同金丝雀 | 6/6完成响应、六机制各1；费用$0.012032535；六份JSON均在proposal Schema或确定性编译合同FAIL，有效候选0；断网独立audit PASS；权威`STOP_NO_VALID_CANDIDATES` | scope关闭，不递补/第7次调用；如继续先另立零API/零行情R3D离线匿名失败诊断并保持validator不变；效果/回测/生产仍禁止 |
| TS-v5-R3D | 六响应离线proposal诊断 | 六份均暴露批准独立席位未绑定进request/schema、响应模式与runner记账错配；另有搜索积5/6等字段错误；audit PASS；权威`STOP_LOCAL_IMPLEMENTATION_DEFECT` | 仅可另立R3E零API合同恢复，绑定mode并机械约束搜索积；不自动授权新调用，效果/回测/生产仍禁止 |
| TS-v5-R3E | 本地绑定proposal合同工程门 | v3移除响应lineage/搜索点，编译器绑定独立authority并机械分配搜索积；六机制、48对抗、旧六回答0录取、audit/幂等PASS | `GO_R3F_LIVE_CANARY_SCOPE_PROPOSAL_ONLY`；新调用须另立小批scope/release并获明确批准，参数搜索/回测/生产仍禁止 |
| TS-v5-R3F | 本地绑定proposal真实合同金丝雀 | 六机制各1份，6请求/6完成/0重试；v3 Schema、本地编译、原validator和语义唯一6/6 PASS；费用$0.010779967，断网audit/幂等PASS | `GO_BOUND_PROPOSAL_CANARY_ONLY`；scope关闭。下一步另立R3G发现期评价协议，结果前冻结去重、发现期、密度、参数预算、成本与多重检验；仍不授权模拟仓/生产 |
| TS-v5-R3G-0 | 六机制可执行语义工程门 | 六候选原指纹/签名重编译一致；431有效参数点、六正常路径、16对抗、独立audit与幂等PASS；行情/效果0 | `GO_R3G_DENSITY_SCOPE_PROPOSAL_ONLY`；下一步另立R3G-1无收益密度scope，只看2019—2021事件分布；效果/模拟仓/生产仍禁止 |
| G8-0 | 法定产品证据源可行性 | 主源覆盖与结构 PASS；HTTP 传输身份 WARN；GO 仅限 G8-1 主源采集 | 先冻结不可覆盖采集协议；管理人 HTTPS/费率谱系完成前不得 VERIFIED 或计算 G8 |
| G8-1R | 监管主源不可覆盖采集 | 1 条原 Docker 失败永久保留；恢复 54 条主证据、二遍幂等与断网复核 PASS；G8 仍 NOT_READY | 只允许另立 G8-2 管理人 HTTPS 交叉核验与费率有效期谱系协议 |
| G8-2 | 管理人 HTTPS 逐值与费率有效期谱系 | 19 个逻辑请求留痕；八日逐值 1/6、费率谱系 0/6，权威 NO-GO；不是策略 REJECT | 禁止 G8-3；未来只能另立恢复协议处理传输/解析、华商 HTTPS 与法律文件有效期 |
| D1 | L1 LLM 持续因子研究 | D1-3A 3/8 语义 FAIL、旧批权威 STOP；未来新批语义一致性工程门已 GO；D1累计$0.091348347，策略未评价、零生产授权 | 本批仍结束；未来重启须新指令和新协议，并强制复用语义门；不得补发或递补旧批 |
