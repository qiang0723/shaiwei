# TS-v5 R3G-1 时间角色边界加注（结果前冻结）

日期：2026-08-13（UTC+8）

本加注绑定主 scope SHA-256 `3b83d8a5…0381`，只消除一个执行歧义：等待中的 episode 不得从
发现段跨入稳定性段，也不得从稳定性段跨入 2026 部分年度段。

特征可以使用 2016-01-01 起的连续历史做滞后预热；但每段开始时活动状态清空，触发、确认与下一
官方开盘裁决必须全部落在同一时间角色内。段尾按该候选最大等待日数再加一个下一开盘日清除未成熟
信号。该清除不改变密度阈值，只防止后段信息参与前段事件裁决。

加注发生在首次真实密度或任何策略效果读取之前；不修改候选、参数、门槛或主协议原文。
