# RF-0B 字段与身份结果盲预检协议（2026-08-19，待用户批准）

- 机器真身：`config/rf_0b_field_identity_preflight_v1.yaml`，SHA-256
  `9c9c58c0edac374957c35c78e92f53d06070740b9eccfdea28656781c2e2556c`
- 状态：`RESULT_BLIND_DATA_AND_IDENTITY_PREFLIGHT_FROZEN_PENDING_USER_APPROVAL`
- 上位授权：RF-0A 裁决（`GO_PREP_ONLY_NOT_EXECUTION_AUTHORIZED`，RF-0B 可在等待 R2-1 期间
  施工）+ 用户 2026-08-19 选择 RF 为下一方法家族

## 1. 机制定位（继承 RF-0A，不冻结公式）

中证800"隔夜信息吸收与日内反转状态"：收盘时分解隔夜跳空（`open_t/close_{t-1}-1`）与日内
分量（`close_t/open_t-1`）的方向、强度或吸收/反转关系，检验其联合状态能否在 Alpha158、行业
和市值暴露之外提供稳定扣成本的增量信息。RF-0A 的账本元数据扫描发现 `open` 字段从未出现在
既有 104 次 LLM 尝试中——这是身份缺口，不是有效性证据。

## 2. 本节点只做两件事（零效果）

**A. open/前收盘字段链画像**（PIT 中证800 成员日 × 2019-01-02—2025-12-31）：只输出计数、
覆盖率、失败分类（缺 bar、除权变动日、涨停开盘、上市初期、跨非交易日引用、`.BJ` 必须为 0），
不计算任何候选值、分布或收益。冻结门：open 覆盖率 ≥99%、前收盘可连接率 ≥99%、未分类缺失
= 0。门在画像前冻结，不达标即 `BLOCKED_DATA`。

**B. 规范身份注册表**：跨三份 LLM 尝试账本（D1-v2 40 次、M1 科创50 40 次、M3 多池 24 次，
共 104 次）、G1 准入账本 21 个身份、Alpha158 158 个表达式族，建立规范 AST 哈希注册表——
账本只准读身份列（尝试 ID、主题、规范表达式、哈希、token/AST 数），**禁止读
`discovery_rank_ic`、`result_json`、准入裁决等任何效果字段**。注册表给出计数与哈希集合，
未来候选命中即 `REJECT_DUPLICATE`。

## 3. 对抗 fixture（全合成）

停牌缺 bar、信号与成交日间除权、上市首日无前收盘、有收盘缺开盘、跨非交易日前收盘引用、
`.BJ` 拒绝、同义 AST 去重——全部通过才可进入真实画像。

## 4. 边界与裁决

- 不调 DeepSeek/任何 LLM、不训练、不回测、不读封存效果、不碰模拟仓/Web/scheduler/生产；
  断网、write-once、一次画像、一次独立审计、同 scope 不重跑。
- `GO_FORMAL_PROTOCOL` 只授权起草正式单机制协议（RF-0A 规定：8 个完成响应、至多 3 个合格
  候选、机械 Top1/Top2、重复/失败计入预算不补位），且正式批仍须 R2-1 检查点 + 另立协议 +
  你再次批准。
- `REJECT_DUPLICATE` / `BLOCKED_DATA`：本机制关闭，不重试。

## 5. 待你裁决

批准即进入工程实现；fixture 与真实画像之间照例会停一次等你放行。若对覆盖率门（99%）或
画像期间（2019—2025，对齐可评价分数链）有异议，现在改最便宜。
