# Web 1.1.2 前瞻分层与当前路线修正验收

- 协议 ID：`web-1.1.2-truth-correction-v1`
- 验收日期：2026-08-09（UTC+8）
- 工程裁决：`GO_LOCAL_READ_ONLY`
- R2-1 状态：`NOT_DUE`
- 策略有效性：`NOT_EVALUATED`
- 生产授权：`none`

## 1. 修正结果

Web 已停止把所有 `mode=FORWARD` 账户日统称为“自然前瞻”。后端现在依据冻结合同和不可变证据统一
完成分层，前端只消费类型化结果：

- 协议 FORWARD：10 日 / 1 次调仓；
- 受控追赶 FORWARD：5 日 / 1 次调仓；
- Top30 / Top20 同日自然运行：5 日 / 0 次调仓；
- R2-1 到期门槛：20 个同日自然账户日且至少 2 次自然调仓；
- 当前状态：`NOT_DUE`，预计日期只作为计划，不替代未来账本和不可变产物。

Top30 已有的 12 个单账户自然观察日仍作为独立事实展示，不再与双账户共同检查点混为一个计数。Top20
账户日明确标出 `CONTROLLED_CATCHUP_FORWARD` 或 `SAME_DAY_FORWARD`；当前 5 个同日自然观察日只展示
逐日精确表，不绘制趋势，也不输出优胜、效果 GO/REJECT、年化、Sharpe、信息比率或生产切换建议。

## 2. 当前路线投影

策略工厂保留不可变 `strategy_factory_v3` 历史快照，并在其上叠加内容寻址的当前路线：

- 当前裁决：`COURSE_CORRECTION_AND_OBSERVE`；
- 当前唯一主目标：`R2-1_FORWARD_EVIDENCE_CHECKPOINT`；
- 新股票池、新因子、LLM 批次、通用控制面和无目标重构均暂停；
- M7 终态仍是证据恢复不完整，候选 0、效果读取 0、策略未评价；
- “具备研究条件”与“当前获得施工授权”在页面上明确分开。

路线配置同时绑定旧策略工厂指针、快照的精确相对路径和 SHA-256，并逐项校验四份权威文档。任何路径
越界、符号链接、文件缺失或哈希漂移均返回 `EVIDENCE_MISMATCH`，不回退到旧路线。

## 3. 页面与架构边界

- 总览新增独立“双账户自然检查点”，同时保留 Top30 单账户事实；
- 模拟组合新增前瞻分层摘要、5 日双账户明细和账户日证据分层；
- 策略工厂首屏改为当前路线，M5 只作为历史支线；
- 新增计算、查询、校验和三个展示组件均按职责拆分；新增最大生产模块 399 行，并纳入 Web 架构棘轮；
- `types.ts` 保持既有 719 行上限，没有继续增长；
- Web 镜像只复制三份白名单配置，四份权威文档以单文件只读挂载进入查询容器；
- Web 无写 API、无 `.env`、无 Docker socket、无项目根挂载；`web-ui` 仍只监听
  `127.0.0.1:8080`，`web-query` 仅内部暴露。

## 4. 验收证据

- `make architecture-check`：13 PASS；
- 全仓 `make test`：1,069 PASS，17 条为既有第三方弃用/未来行为提示；
- Web 单元：33 PASS；
- 五视口 fixture（1440 / 1024 / 768 / 390 / 320）：69 PASS、11 条按适用场景跳过；
- 真实只读部署：桌面 7 PASS、移动 7 PASS；
- 手工真实页面复核覆盖总览、Top20 前瞻分层和策略工厂，浏览器控制台错误 0；
- Ruff、compileall、pip check、Compose 渲染和 `git diff --check` 均 PASS。

最终 Web 镜像：

- `sha256:30ee550b4cf8723d55a53a86a2c24c0f177b6a996fd98826d2b7e501d170b421`
- `web-query`：`b1e4300219dd...`，healthy；
- `web-ui`：`90997995c1b...`，healthy。

隔离复核：

- 生产 scheduler 仍为原容器 `183b8c6c5edd...`、原镜像 `722f63de...13b76`、创建时间
  `2026-08-03T09:39:34Z`，healthy，未重启；
- research-control 仍为原容器 `5b489771357f...`、原镜像 `95c00fb5...22e2`、创建时间
  `2026-08-05T10:34:56Z`，healthy，未重启；
- 未修改模型、信号、执行规则、生产配置或自然增长账本；七个自然账本保持未暂存。

## 5. 裁决与后续

本次 `GO_LOCAL_READ_ONLY` 只表示 Web 事实修正和本机只读展示验收通过，不表示 R2-1 已到期、Top20
优于 Top30、任何策略有效或获准进入生产。主线继续等待自然证据增长；预计节点必须到时依据新的账本和
不可变产物重新计算，不能引用本页计划日期提前裁决。
