# TS-v5-R3F 本地绑定 proposal 真实合同金丝雀协议

日期：2026-08-13（UTC+8）

状态：`USER_APPROVED_RESULT_BEFORE_SCOPE_FROZEN`

唯一 scope SHA-256：
`1a45898caa3c4fac0c7a1b8a301271d48c3b0a666e947d7b988bd50d7e7aee61`

## 用户授权的有界解释

用户明确要求“继续下一节点，批准调用DeepSeek”。结合R3E验收中已经结果前建议的下一节点，本次只
授权一个R3F小批真实合同金丝雀：六种机制各一个`INDEPENDENT`席位，串行取得恰好6个完成响应；每个
席位最多发起1次外部请求，整批外部请求最多6次，无传输重试、无递补和第7次调用；费用硬上限0.15
美元。任何失败都按已消费席位留痕，不以程序修补或额外调用替代。

本授权不扩展到参数搜索、回测、行情/收益读取、模拟仓、Web或生产。R3F只回答“v3绑定合同是否能让
六类机制各产生严格合法且唯一的结构化proposal”，不回答候选是否赚钱。

## 结果前发送内容

每个请求只发送：v3冻结system prompt、该机制专属proposal Schema与mechanism projection、本地批准的
独立attempt authority、冻结产品约束、公开方法摘要、匿名TS v3/v4失败记忆及attempt身份。

明确禁止发送：证券代码/名称/清单、逐行或原始行情、持仓/订单/信号、任何发现收益或风险指标、封存
验证/锁定测试/前瞻结果、旧LLM回答或reasoning、绝对路径、Tushare/飞书/DeepSeek密钥及其他凭据。

R3E已经把lineage和搜索点数从响应移到本地权威：请求会显示批准的`INDEPENDENT`模式，但响应Schema
不接受lineage或`search_points_maximum`；编译器注入空父谱系并机械分配合法搜索点。

## 次数、失败和费用

- 机制顺序固定为：波动自适应、周结构分位、突破回踩、均线恢复、收缩扩张、相对强度回调。
- 完成响应目标恰好6，独立席位6，反方席位0。
- 每席最多1次外部请求，整批最多6次；任一连接、HTTP、解析或计费不确定错误立即停止整批，不重试。
- 完成但候选无效仍计一次、计费并占用席位；不得补发。
- 官方价格于2026-08-13再次核对：V4-Pro缓存命中/未命中输入与输出分别为每百万token
  0.003625/0.435/0.87美元。按每席最多16000输入、1800输出、全部未命中计算，六席最坏0.051156
  美元，低于0.15美元硬熔断。5美元TS总预算不自动扩大本批。

## 工程与执行顺序

1. 本scope和协议先提交、推送；此时不读secret、不调用API。
2. 在新版本模块复用既有transport、append-only账本、费用计算与证据链，补v3分类、release和独立audit；
   R3C旧代码、账本、响应和裁决保持字节不变。
3. 实现测试通过并推送后构建独立不可变镜像，先在`network=none`、无secret下完成请求束、安全扫描、
   mock六响应、结果门和独立预检audit。
4. 冻结包含准确Git、镜像、代码快照、请求束、两个空专用账本及用户授权的execution release并推送。
5. release完整通过后，才从项目内`.env`定向提取DeepSeek key到Git忽略的专用只读文件；不输出、记录或
   提交密钥。短生命周期容器仅注入`DEEPSEEK_API_KEY`，串行执行唯一批次。
6. 结束后断网独立重算请求、响应哈希、账本、费用、候选分类和权威门；再做一次全只读幂等复跑，新增
   API调用必须为0。

## 结果前裁决

- 6/6完成且6个合法唯一候选：`GO_BOUND_PROPOSAL_CANARY_ONLY`。
- 4—5个合法唯一候选：`STOP_PARTIAL_BOUND_CONTRACT_COMPLIANCE`。
- 1—3个合法唯一候选：`STOP_WEAK_BOUND_CONTRACT_COMPLIANCE`。
- 0个合法唯一候选：`STOP_NO_VALID_CANDIDATES`。
- 未取得恰好6个完成响应：`STOP_INCOMPLETE_BATCH`。

无论结果为何，均不自动进入参数搜索或回测。只有6/6合同金丝雀通过，才允许主控另立结果前候选去重/
尝试登记与小规模发现期评价协议；仍须单独授权。
