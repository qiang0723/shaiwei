# RF-0C 字段与身份结果盲预检验收（2026-08-19）

权威裁决：`GO_FORMAL_PROTOCOL`

策略有效性：`NOT_EVALUATED`

生产授权：`none`

## 1. 权威结论

RF-0C 唯一一次结果盲画像完成（R2 恢复 scope，fixture 门先行通过），确定性复算一致，独立
审计 8/8 PASS。全部冻结门通过：open/前收盘覆盖率 99.8665%、`.BJ`=0、未解释缺失 0（原 5 个
缺口全部由独立 Baostock 层确认为真实停牌并归位）；身份注册表与 RF-0B 封存注册表逐字节一致。

本 GO 只授权一件事：起草正式单机制协议（RF-0A 冻结形状：8 个完成响应、至多 3 个合格候选、
机械 Top1/Top2、重复/失败计入预算不补位），且正式批仍须 R2-1 检查点 + 用户再次批准 +
DeepSeek 逐批授权。不授权候选生成、训练、回测、模拟仓或生产。

## 2. 画像结果

| 指标 | 结果 | 冻结门 | 通过 |
|---|---:|---:|---|
| 成员日总数 | 1,359,127 | — | — |
| open 覆盖率 | 99.8665% | ≥99% | ✓ |
| 前收盘可连接率 | 99.8665% | ≥99% | ✓ |
| `.BJ` 行 | 0 | 0 | ✓ |
| 未解释无 bar 缺失 | 0（5 个由 Baostock 独立层确认） | ≤0 | ✓ |
| 停牌证据分层 | suspend_d 主层 1,809 + Baostock 独立 5 | — | — |

注册表：D1-v2 40 次/38 式/2 重复簇、M1 40 次/14 式、M3 24 次/18 式、G1 29 身份、
Alpha158 158 式；效果列零读取。

## 3. 工程留痕（全部零消耗）

- v1 scope：命令链管道吞掉 fixture 非零退出（fixture 服务缺账本/注册表挂载），画像在
  fixture 门未通过时执行——执行顺序违规，v1 关闭留痕（标记 `a1ac4bbb…`，画像 `af8e5544…`）。
- R2：补挂载、fixture 先绿（6/6）、新输出根唯一画像 + 独立审计 8/8 PASS。

## 4. 证据与身份

- 协议 SHA-256：`28c8524b96f726968a507d42ef661b435ca000746d218634d76b768d5cd5cc66`；
  R2 scope：`b992c90c…f83d`。
- 运行 Git：`5f09257f53e93489b566e3137312e1a2e876332b`；镜像
  `sha256:33267a5a287236aa47dd8c6352378d95f6eb7a24da991ae4108143c562a12bfa`。
- 画像 SHA-256：`7984847b4404f3a5421f6df75a667fc2b2e56c723a4737e2dbdeeaf09e9b2bb0`；
  manifest：`697fa25a…2919`；注册表：`ab97bf51…e323`；字段画像：`fdd84076…5b05`；
  审计：`2efe154f…fe07`；标记：`b5210878…e2b6`。

## 5. 生产隔离

scheduler 原容器 `183b8c6c5edd` healthy、重启 0 次，全程未触碰。

## 6. 下一边界

起草 RF-1 正式单机制协议草案给你审；协议内冻结：DeepSeek 模型与费用上限、8 响应小批、
候选合同（含五层去重：语义/AST/暴露/IC 增量/组合增量）、发现期口径与预算。批准前不调用
任何 LLM。
