# M2-0R2 科创200官方谱系与2026年7月快照恢复协议

> 冻结日期：2026-08-04（Asia/Shanghai）
>
> 恢复 ID：`m2-star200-data-recovery-v2`
>
> 原协议：`m2-star200-data-protocol-v1`（永久保留）
>
> 因子/策略结果读取：`false`
>
> 生产授权：`none`

## 1. 已知背景

M2-0 的首轮官方公告候选发现曾遗漏仅含“临时调整”的标题。该工程缺陷已按
`m2-star200-discovery-recovery-v1` 修复并完整复跑；原首轮裁决降级为 provisional，恢复后裁决成为
权威 `NO_GO_M2_STAR200_DATA_GATE`。恢复后仍存在两个独立阻断：

1. 2024-08 至 2026-06 的二级月度集合显示 9 个成员变化区间，但公开官方材料没有提供可解析的
   科创200历史调入/调出成员对，官方谱系事件仍为 0；
2. 2026-07 的 Tushare `index_weight` 精确请求于 2026-08-01 即时双查均为 0 行，使冻结的 24 个月
   二级集合门只完成 23 个月。

本次恢复发生在上述结果已知之后，不是盲预注册，也不改变原协议、首轮恢复单、报告、manifest 或
NO-GO。它只回答“截至2026-08-04，这两个外部数据缺口是否已经发生可审计变化”。

## 2. 冻结输入

- 原协议配置 SHA-256：`584d0eab2405b65cd27ea9994e68c6419e81679f654d6fd1ef169bc9d4266250`；
- 原 collection report SHA-256：`40c4c71a843b770d76217c52a88fbaa33ce8e89366dd9cde19a41243565f9bd5`；
- 恢复后官方 discovery report SHA-256：
  `85daceb5fba7fdc41899cf6c6880216f0695463831587b34d1055afcdef77cf4`；
- 恢复后 quality report SHA-256：
  `affe9bc2b41ed5e4b3c1891dbc455feae52088c50ade9b21cd284854068e58a1`；
- 恢复后 tracked manifest SHA-256：
  `aa6c6ba40296a8a1a074c3437085abf80bf7a6698cc04ac2aa9a40f3307b399f`；
- 原 2026-07 零行批次：`33aa80a00744`，文件 SHA-256
  `4d83ccdf175fc9ed59d5e06d441fa58da65728622c80e3456bcf9caa9d7c899c`。

## 3. 唯一外部查询授权

### 3.1 Tushare

只允许对以下同一请求串行即时双查一次，不重查其他 26 个分区：

- API：`index_weight`；
- `index_code=000699.SH`；
- `start_date=20260701`；
- `end_date=20260731`；
- fields：`index_code,con_code,trade_date,weight`。

双响应必须 canonical SHA-256 一致、行数小于源端上限、代码和日期不越界、`.BJ=0`，然后作为新的
不可变原始批次追加到项目 `data/raw/` 和 `ledger/ingest_batches.csv`。允许再次得到 0 行；不得删除或
覆盖原 0 行批次。其他 23 个月和三段指数日线只复用原 collection 的即时双查证据，并重新核验其
文件哈希。

### 3.2 官方一手材料

- 官方域名、HTTPS、串行限速、最大文件、重试、跳转域和内容寻址策略完全继承原协议；
- 使用全新的 `data/research/star200/m2-star200-data-recovery-v2/official_sources/` 忽略目录，避免
  旧缓存隐藏页面更新；
- 从当前归档页扫描至越过 2024-07-21 首发边界，候选规则继续同时覆盖定期调整和临时调整；
- 截止日更新为 2026-08-04；不得增加第三方、ETF PCF、基金持仓、当前成分或人工成员表。

## 4. 确定性生成与裁决

恢复入口必须从原协议和本恢复配置生成一个不可变 effective protocol，只改变：冻结时间、恢复范围、
官方截止日、新输出路径和恢复文档身份。指数、首发/生效日、规则谱系、24个月、200只、T+1/PIT、
质量门、来源角色及全部禁止事项逐字段不变。

恢复 collection report 必须分列：26个复用请求、1个新查询、原/新2026-07批次身份、双查哈希和
完整27请求最新证据。官方 discovery、quality、derived artifacts 和 tracked manifest 均写新路径，
不得覆盖旧证据；相同完成入口复跑必须零新查询、零写入且哈希不变。

只有同时满足以下条件才可 `GO_OFFICIAL_LINEAGE_AND_SOURCE_DATA_ONLY`：

1. 2026-07 月度快照恰好一个日期、200行、200个唯一成员；
2. 官方首批、V1.0/V1.1规则、全部定期/临时调整成员对和公告/生效时点完整；
3. 2024-08 至 2026-07 共24个月与官方逐日200只谱系逐月精确一致；
4. 重复、未知代码、未来回填、当前成分回填、无法解释变化和`.BJ`均为0。

任一条件失败仍为 `NO_GO_M2_STAR200_DATA_GATE`。即使二级快照从0变为200，只要官方成员谱系仍不
闭合，仍必须NO-GO；不得用月末集合反推官方PIT历史。

## 5. 施工边界与停止条件

允许新增一个不超过400行的研究恢复编排模块、独立schema/fixture、恢复配置和新验收文档；通用解析
或门禁只有发现真实缺陷时才允许最小修复。先在断网fixture证明旧证据绑定、单分区查询、官方新目录、
双查不一致、`.BJ`、路径逃逸、幂等和NO-GO/GO裁决，再执行联网查询。

禁止 DeepSeek、因子、IC、收益、qlib、模型、回测、排名、信号、模拟仓、Web和生产变更。不得修改
中证800、科创50、科创100、规则型科创板池、scheduler镜像或明日P0-E发布守护。若最终仍NO-GO，
公开数据路线停止；下一步只能取得带发布/生效/版本/修订语义的授权历史成分产品，或转向另立的
科创50/自建PIT池因子研究，不能继续盲扫同一归档。
