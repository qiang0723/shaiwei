# G8-1R 监管主源证据采集验收（2026-07-26）

> 权威协议：`g8-fund-primary-capture-recovery-v1`
>
> 主源采集门：`GO_G8_2_CROSSCHECK_AND_FEE_LINEAGE_ONLY`
>
> G8 状态：`NOT_READY`
>
> 生产授权：`none`

## 1. 结论

六只冻结产品在 `2026-07-15~2026-07-24` 的监管集中披露主源证据已经按恢复协议完成不可覆盖
采集。恢复协议独立得到 6 条净值区间证据、48 条逐公告分红备注证据和 48 条唯一 A 类估值记录；
首遍追加 54、第二遍追加 0，断网 Docker 独立复核与宿主结果完全一致。

本结论只证明本批监管 HTTP 原文、解析结果和哈希链可复核。监管入口仍是未认证 HTTP，尚未完成
基金管理人 HTTPS 逐值交叉核验和申赎费率有效期谱系，因此不得标 `VERIFIED`、不得构造产品总收益、
不得运行 G8，也不得接入 scheduler 或生产策略。

## 2. 原失败永久保留

原协议 `g8-fund-primary-capture-v1` 的首次 Docker 真实执行在第一个净值逻辑请求收到两次空体
`502` 后按协议停止，并追加一条 `QUARANTINED_HTTP_STATUS`。同一镜像的 Docker 默认网络和 host
网络探针均复现空体 502；此外首次构建时操作层传入的完整 Git SHA 与仓库真实提交不一致。

该失败不计入恢复验收，但未被删除或改写：

- evidence ID：`d169e0b5af4aab1bb28263f39f50b9c0139c4c7860beb29259ecb0a67bdce46f`；
- bundle SHA-256：`736637e2ec2afa9c959c5d3c3f4f9e941e3d224d47aee5b66796d5c8bff67bf7`；
- 冻结账本前缀 SHA-256：`15934f0879ff4128b6324b88b578e406605cf56ee5250fdb86cd5e9107db3f64`；
- 两次 HTTP 状态：`502/502`；状态：`QUARANTINED_HTTP_STATUS`。

恢复采集器在发出任何网络请求前强制核验上述账本前缀、失败行和证据包；断网终验返回
`recovery_failure_preserved=true`。

## 3. 冻结身份

| 项目 | 冻结值 |
|---|---|
| 恢复协议 SHA-256 | `cc0e5ab4bee5ac5016b613c0736b25c16da302e0b13421ee49ae6c6facb64bde` |
| 执行 Git HEAD | `542fb214c9f0d96f12f208c2a20c576bdce285e2` |
| 代码快照 SHA-256 | `7e0d39f53ac619fac01e68cb3cf72b7952c3f1963da6ab30e33c776472617497` |
| 恢复镜像内容 ID | `sha256:05e2a2ff1fb1e5d20ef34dc0cc2e257687cb7d1975bb971dc5b65edc800b9cef` |
| 恢复证据 manifest SHA-256 | `915d013b109b88797dd154d911728b3496d173934d004810261d3e51bdb6fb2e` |
| 终版账本 SHA-256 | `0b05e285df85442d65b06cb441d9bc57b9382fbc68a9533ae126a54338a13b96` |
| 全部 55 个 bundle 路径+内容树 SHA-256 | `a580fed2ea154bd08352e794083a59af02f1904cc5cfdf00a815270a54b38bc5` |

全部原始响应和解析值只保存在 Git 忽略的项目内证据包。Git 跟踪账本只含相对路径、哈希、计数、
状态和执行身份，不含净值、分红金额、备注原文、绝对路径或凭据。

## 4. 三遍验收结果

| 门 | 首遍宿主采集 | 第二遍同命令 | 断网 Docker 独立复核 |
|---|---:|---:|---:|
| 逻辑请求 / HTTP 观察 | 54 / 108 | 54 / 108 | 不访问网络 |
| 新增 / 复用 | 54 / 0 | 0 / 54 | 只读 |
| 净值证据 / 可用行 | 6 / 48 | 6 / 48 | 6 / 48 |
| 分红备注证据 / 解析行 | 48 / 48 | 48 / 48 | 48 / 48 |
| 正常状态 | 54 | 54 | 54 |
| 旧失败证据保留 | PASS | PASS | PASS |
| 账本 SHA-256 | `0b05e285...13b96` | `0b05e285...13b96` | `0b05e285...13b96` |
| bundle manifest | `915d013b...fb2e` | `915d013b...fb2e` | `915d013b...fb2e` |

终版账本含 55 条数据行：原协议 1 条失败行、恢复协议 54 条正常行；证据目录含 55 个 bundle，合计
571,058 bytes。独立账本剖面确认 55 个 evidence ID、55 个 bundle 路径均唯一，54 个恢复 request ID
均唯一；每只产品恰有 1 条净值证据和 8 条分红证据，六只各覆盖 8 个估值日，分红父请求孤儿数为 0。
恢复双取没有 HTTP 失败、结构失败、内容不一致、修订冲突、绝对路径或重复追加。

## 5. 执行环境与安全边界

恢复唯一改变的变量是执行环境：从 Docker Desktop 出网改为项目 `.venv` 的一次性宿主进程。
进程由 `env -i` 启动，只保留 `HOME=/tmp` 和系统 `PATH`，不读取 `.env`，HTTP 客户端
`trust_env=False`、零自动重试。它不是常驻服务；服务和 scheduler 仍全部使用 Docker。

宿主采集完成后，由同一提交构建的不可变镜像在 `--network none`、根文件系统只读、无额外能力、
仅只读挂载项目 `data` 和单一 G8 账本的条件下独立验收。未访问或写入筛微项目之外的业务目录。

scheduler 在施工前后均保持原容器 `fd8e9615...a5adbb`、原镜像
`sha256:de87ec74...a0261`、原创建时间 `2026-07-24T12:25:27Z`，状态持续 `healthy`，未重建或重启。

## 6. 验证与已知边界

- 宿主全仓：318 PASS；Ruff、compileall、Compose、diff-check PASS；
- 新镜像正确工作目录和只读账本装配下 G8 专项：15 PASS；
- 首次镜像专项命令因未指定 `/workspace` 且未挂载账本，产生 19 个测试装配失败；修正装配后通过，
  不作为实现失败或真实采集证据；
- Git 脱敏检查未发现凭据；`data/g8/fund_evidence` 持续由 Git 忽略。

主源的即时双取只能证明本次响应一致，不能把 HTTP 传输升级为认证来源，也不能证明管理人材料和
费率历史已经闭环。下一步只能另立 G8-2 结果前协议，完成六只产品的管理人 HTTPS 交叉核验与费率
有效期谱系；在该门通过前，G8 保持 `NOT_READY`。
