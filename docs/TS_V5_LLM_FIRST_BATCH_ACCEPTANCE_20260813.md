# TS-v5 首批 DeepSeek 机制研究验收

日期：2026-08-13（UTC+8）

权威裁决：`STOP_NO_VALID_CANDIDATES`

## 结果

- 批次严格完成12个响应：6个独立候选席位、6个同机制反方改版席位；串行，外部调用12次，无第13次、
  无递补。相同入口终态复跑`external_api_calls_this_run=0`。
- 实际估算费用0.03012781美元，低于0.50美元单批硬上限；本批不能使用其余总预算。
- 12个响应全部以非`stop`结束，均按冻结合同计为`PROVIDER_FINISH_REASON_INVALID`；Schema合法候选0、
  唯一合法候选0。因此本批不允许进入参数搜索、回测或任何后续效果阶段。
- 独立断网审计PASS：12条尝试、24条传输事件、12个请求/原始响应/manifest/provider产物逐项关联，
  release、成本和证据束均一致；审计不读取密钥、不联网、不使用候选正文作效果判断。

## 口径纠正

原始终端报告SHA-256为`f2e445f236b717286869e60917b9c415ad2d2d1d9b6b831105a2654e0bc9a254`，
其中`GO_CANDIDATES_ONLY`只检查12个响应是否齐全，遗漏“至少一个Schema合法且非重复候选”的必要条件，
因此不是权威候选门。原报告保持不可变；本验收追加裁决为`STOP_NO_VALID_CANDIDATES`，不回写原证据。

专属尝试账本SHA-256：`200e0391460a678c3c78f8bfc9c09384a7830d29a9ee68ae733e5c9b9e4f84a8`。
专属传输账本SHA-256：`4df2806186cdd03d9caf04640df612989735f9141902f4f6fbe1a066a8ebbc7e`。
响应证据束SHA-256：`850350d2d552238e035de87c9fab336a2356f53ae110fecbaae01082b849907f`。
权威独立审计SHA-256：`c8c38140e7ece534e3b6a2e05eea0f2212299c9f48f97660981d5c0fcee947cc`。

## 边界

本批没有发送证券、行情、持仓、收益、封存结果或凭据；没有读取行情/效果，没有参数搜索、回测、
模拟仓、Web或生产变更。scheduler保持原镜像和原容器健康。TS策略族继续有效，本裁决只停止本次
LLM批次；任何后续调用必须先解释DeepSeek结束原因合同并另立新scope，不能复用本approval或补发。

## 代码结构

TS-v5新增生产模块按传输release、批次编排、证据、独立审计拆分，最大模块不超过400行。DeepSeek
公共类型已抽为28行轻量模块，旧D1热点文件净缩小且架构棘轮未放宽；专用镜像只安装13项最小依赖，
没有复制共享传输实现。
