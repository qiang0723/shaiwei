# TS-v6-3 排序子集效果协议（2026-08-18，待用户批准）

- 机器真身：`config/ts_v6_3_ranked_subset_effect_v1.yaml`，SHA-256
  `74a9111ae620f15072555188e48ba25ef22cf98daa81ad31a7f471412169b3f9`
- 状态：`RESULT_BLIND_EFFECT_PROTOCOL_FROZEN_PENDING_USER_APPROVAL_AND_ENGINEERING`
- 上位授权：TS-v6-1 权威 GO（`GO_TS_V6_1_RANKING_EFFECT_SCOPE_PROPOSAL_ONLY`，只授权起草本
  协议）+ 用户 2026-08-18 裁决（TS 支线效果预算 2，本协议消耗 1）
- 本协议只冻结口径；批准前不得施工，批准后也只准读取发现期（2021—2023）

## 1. 唯一研究问题

父版 R3G-2 主点（4 周突破、1.5 ATR 回踩容差、10 日等待、1 日确认）的完整入场/退出/仓位/风险/
执行语义逐字节不变，唯一变量是把交易事件集换成 TS-v6-1 冻结的入场质量排序 Top-94 子集。该子集
在发现期是否具有扣成本绝对价值与 H00906 相对价值？

这是**一次**效果尝试（无邻点、无网格、无替补），回答的是 R3G-3 诊断的问题一："入场是否制造了
过多随后止损的交易"。

## 2. 与父版协议的关系

除以下四项外，全部语义逐字继承 `config/ts_v5_r3g2_effect_v1.yaml`（SHA-256
`c3aa5a2b...f3572bf`）：

1. **候选集**：父版 188 条发现期事件 → v6-1 冻结 Top-94 键（`ranked_events.parquet` 中
   `role=selectable_discovery 且 selected=true`，SHA-256 `b356f01c...747e9f`）。执行器先从
   绑定 R3 输入重推父事件并与 R3G-1 冻结键精确对账，再与 94 键取交集；任何缺/多键
   `BLOCKED_PRE_EFFECT`。v6-1 留出期 74 行不得进入候选集。
2. **无敏感性邻点**：父版有 2 个邻点（共消耗 3 次尝试）；本协议只有 1 个点。
3. **新增主 KPI 门**：费前单笔期望必须严格为正（父版为 -88.80 元），直接针对 R3G-3 诊断的
   亏损根因；父版原有门禁（≥30 笔、每年 ≥5 笔、净收益>0、超额>0、≥2 个正超额年、MDD≤20%、
   双倍成本 ≥0、双边 +10bp ≥0、DSR≥0.95）全部保留不放宽。
4. **DSR 试验计数**：trial_count=4（R3G-2 三点 + 本候选），旧三点 Sharpe 只能从绑定的
   R3G-2 first-pass 产物树机械重算（报告 SHA-256 `515e891b...ffd528`），禁止手填。

## 3. 排序谱系（组合内资金优先级）

继承父版：优先级仍用 M6 干净谱系 W2—W6 的同期 Alpha158 OOS 分数（信号 ISO 周一之前最后一个
官方开盘日观察、降序、同分代码升序），覆盖率 <95% 即 `BLOCKED_PRE_EFFECT`，缺分数事件按冻结
规则排除且不允许回退。**不使用 W7 或任何 2024—2026 分数**（本协议不碰留出期）。

## 4. 时间角色与防火墙

- 发现期 2021-01-04—2023-12-29：唯一效果读取范围；信号计划退出跨区即清除（16 个交易日
  规则继承父版）。
- 留出期 2024—2025：**本协议物理不计算其收益**；只有发现期全部门通过、另立协议并再次获得
  用户批准后才可打开（v6-1 已为其备好 74 条条件密度合格事件）。
- 2026：保留不读。
- 禁止：外网、凭据、Alpha158 值检查、参数/门槛/候选集的任何读后修改、同 scope 重跑。

## 5. 必须额外报告的背景（继承 v6-0/v6-1 后继要求）

绝对现金净结果、完整 H00906 背景、仓位匹配的 H00906+现金比较、板块背景；按退出原因分组
（止损/止盈/时间）与父版同组盈亏对照；费前与费后期望对照父版 -88.80 元基线。不得提高任何
仓位或风险上限。

## 6. 裁决与预算

- `REJECT_TS_V6_3_RANKED_SUBSET_DISCOVERY`：任一门失败。本 scope 关闭不重跑；剩余 1 次
  效果预算只准用于退出机制单变量研究，否则 TS 支线权威关闭。
- `GO_TS_V6_3_DRAFT_SEPARATE_HOLDOUT_PROTOCOL_ONLY`：全部门通过。只授权起草留出期协议，
  不授权读留出、不授权模拟仓、不授权生产。
- `BLOCKED_PRE_EFFECT`：身份/覆盖/合同失败，修复边界同既有纪律（标记前至多 2 次技术
  release 且留回执，标记后不得同 scope 重跑）。

## 7. 执行控制（批准后）

协议提交先推送；工程实现推送后断网构建；runner 与 auditor 独立入口；合成 fixture 先于语义
读取标记；唯一 first pass + 唯一确定性复算 + 唯一独立审计；输出 write-once；
`docker_network_mode: none`；源挂载只读；不重启 scheduler；串行执行。

## 8. 待用户裁决项

1. 是否批准本协议进入工程实现（批准即消耗 1 次效果预算的承诺）；
2. 若无异议，下一步我将按本协议施工并回报 fixture 结果，真实读取前再停顿一次等你最终放行。
