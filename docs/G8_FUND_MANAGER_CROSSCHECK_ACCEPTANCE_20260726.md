# G8-2 管理人 HTTPS 交叉核验与费率谱系验收（2026-07-26）

> 权威协议：`g8-fund-manager-crosscheck-v1`
>
> 机器裁决：`NO_GO_G8_2`
>
> G8 状态：`NOT_READY`
>
> 生产授权：`none`

## 1. 结论

G8-2 已按结果前冻结协议完成首轮真实证据采集、幂等复跑和断网 Docker 复核，终态为
`NO_GO_G8_2`。这是基金证据认证门未通过，不是策略效果失败，也不否定六只产品本身；G8 继续
`NOT_READY`，不得进入总收益构造、G8 计算或 scheduler。

六只整体门中，仅 `016276` 完成管理人 HTTPS 身份和冻结八日单位/累计净值逐值一致。`017985`
官方页面与当前费率/法律文件索引双取成功，但冻结解析器没有从其 `div` 型历史净值表形成八条记录，
按协议隔离；其他四只在本次 Python HTTPS 传输中未形成 200 双取。独立只读探针表明其中部分官网可
由 curl 访问，因此这些连接失败不能解释为源数据不存在；失败原文和状态仍必须永久保留。

更关键的独立硬门是六只费率有效期谱系均未完成：当前页面或法律文件索引不等于已下载、哈希并证明
成立日至今的申购/赎回费率有效区间。即使未来只修传输或 HTML 解析，本次也不能改判为 GO。

## 2. 结果前身份

| 项目 | 冻结值 |
|---|---|
| 协议冻结提交 | `5431790c8271fd7118868296d608856a300873f8` |
| 协议 SHA-256 | `191b60531b8dcbcab82829c69304e866576e81cd2e4e488e3a9b73ccb8465af7` |
| 实现与脱敏账本提交 | `370bc34d4f776ff61fa9c49ebaa580bd5ac58b09` |
| 验证镜像内容 ID | `sha256:72012422f2b447fc17edc2d70a20bbe52fdaacfb73da80000b3b3c96733fcab1` |
| 跟踪账本 SHA-256 | `c24672fd8a1cf898b35e23e840dc0acf36f19eaeb79c48247a2a9374d712a5cc` |
| 忽略区报告 SHA-256 | `edadc73351020c399934590af7da548b6ccd27f75de89fd2e39095bf0c37e225` |
| 忽略区 manifest SHA-256 | `0bc3c64e96fe7ce958812b7878b469c2889c9893c860a33658b62d21efd03cbb` |

协议冻结提交先于真实采集；实现提交已推送并由独立镜像复核。真实首遍发生在实现工作树内容已完成、
但实现提交尚未生成的窗口，因此首遍 bundle 未内嵌实现 Git SHA；该限制不隐藏：实现内容随后以
`370bc34` 固化，原 bundle 不改写，断网镜像从同一原文重算出相同报告/manifest/裁决。由于结论为
NO-GO，未把这条 provenance 局限用于放宽任何门。

## 3. 机器结果

| 指标 | 结果 | 硬门 |
|---|---:|---:|
| 产品数 | 6 | 6 |
| 管理人 HTTPS 身份通过 | 1 | 6 |
| 冻结八日净值逐值一致 | 1 | 6 |
| 当前申赎费页面交叉核验 | 2 | 6 |
| 法律文件索引捕获 | 2 | 6 |
| 完整有效期费率谱系 | 0 | 6 |
| 整体裁决 | `NO_GO_G8_2` | 全门通过才 GO |

共执行 19 个逻辑请求、38 个观察，首遍追加 19 条脱敏账本。状态分解为：1 条
`MANAGER_NAV_CAPTURED`、2 条 `CURRENT_FEE_CAPTURED_NOT_LINEAGE`、2 条
`LEGAL_INDEX_CAPTURED_NOT_LINEAGE`、1 条 `MANAGER_NAV_REQUIRED_DATE_MISSING` 隔离和 13 条
`HTTPS_REQUEST_FAILED` 隔离。跟踪账本不含净值、费率数值、URL、绝对路径或凭据。

## 4. 幂等、离线复核与隔离

- 相同入口第二遍：新增 0、复用 19；账本、报告、manifest 三个 SHA-256 均不变；
- 宿主 `--verify-only`：新增 0、复用 19，终态和哈希不变；
- Docker：`--network none`、只读根、`cap_drop=ALL`、`no-new-privileges`，只读挂载项目内
  `data/g8` 与 `ledger`，新增 0、复用 19，裁决与三个哈希一致；
- 全仓 326 PASS；Ruff、compileall、pip check、diff-check PASS；
- scheduler 前后保持同一容器 `fd8e9615...a5adbb`、同一镜像
  `sha256:de87ec74...a0261`、同一创建时间 `2026-07-24T12:25:27Z`，持续 healthy，未重建或重启。

## 5. 后续边界

不得进入 G8-3。若未来继续 G8，必须另立恢复协议并保留本次 19 条证据，只允许明确区分：

1. 传输/解析恢复：对已证明 curl 可达的管理人站点，冻结 curl 或等价 TLS 客户端，并为华夏
   `div` 型表增加 fixture；不得删除本次连接和解析失败；
2. 华商来源阻断：管理人 HTTPS 端口当前拒绝连接，不能用 HTTP、CNINFO 或聚合站替代已冻结条件；
3. 费率谱系工程：逐只下载官方法律文件，哈希、页码/章节、公开日、生效日和全部变更公告闭环；
4. 只有六只同时满足 HTTPS 八日逐值与完整费率谱系，才可另立 G8-3，总收益和策略结果在此之前继续
   禁止读取。

本轮不自动启动恢复，避免在两个独立硬阻断仍存在时为提高局部通过数反复施工。
