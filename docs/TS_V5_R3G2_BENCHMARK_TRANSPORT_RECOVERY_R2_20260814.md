# TS-v5 R3G-2 H00906 宿主输出预检恢复 R2

日期：2026-08-14（UTC+8）

状态：`RESULT_UNKNOWN_HOST_OUTPUT_PREFLIGHT_RECOVERY_FROZEN`

机器真身：`config/ts_v5_r3g2_benchmark_transport_recovery_r2.yaml`

R1 的第一次宿主事实表传输在远端开始返回后，以 `curl` 退出码23失败：目标 `raw/` 子目录尚未创建，
因此没有文件落盘。两次历史请求、数据门评价、独立审计、secret读取和策略效果均为0。R1 不重试。

R2 不改变来源、URL、参数、响应数、质量门或权限，只增加请求前硬门：`raw/` 必须位于冻结项目输出
目录内、已经存在、可写，而且三个目标文件全部不存在。硬门通过后，重新允许 R1 固定的一份事实表和
两份相同历史响应各一次传输；随后仍只允许 Docker 断网评价和独立审计。

任一预检或传输失败就停止；不得用重试、替代源或降门槛救回。数据门 GO 仍不代表策略有效，也不授权
R3G-2效果读取、模拟仓或生产。
