# Astra审查采纳记录

2026-09-07用户确认记录审查，来源任务01a07a22-3411-7b53-a6ef-7c5a2798ed71。主线在HEAD
0adc78fdc5c4074224e914f654296fb0b56f330f只读复核三项P1，未实时核验Docker或远端。

- P1：src/shaiwei/daily_early_release_guard.py:284异常时直接恢复，:255附近按指针回滚，缺少
  首次真实写入检查。应持久化候选/日期写入边界，已写入或状态未知时禁止自动旧版恢复。
- P1：src/shaiwei/r2d_release_guard.py:352的CLI未强制校验scope、批准和原子单次消费。
  应绑定动作、配置SHA、候选、控制器、窗口、state/audit与批准，覆盖重复、并发及未知终态。
- P1：src/shaiwei/release.py:330默认readiness经config.load读取.env及凭据，需无密钥元数据
  入口及禁止凭据访问测试。以上静态缺陷不证明生产曾触发事故。
- P2（Astra报告，实施前逐项复核）：冻结容器ID、重启0、精确四挂载、FORWARD类别和产物实际哈希
  门不足；窗口需在mutation前复验，健康时间需拒绝未来值；补CLI到守卫的合成合同测试。

暂停R3E预检和启动，先冻结修复方案并完成工程、合成测试，再核实控制器身份和另立scope申请授权。
Phase A事实、旧scope及封存证据保留；不得覆盖旧哈希、重建候选或重跑封存fixture来迁就身份变化。
撤回“R3D prepare配置完整支持Phase B”：它无noop边界，start会走waiting_source；R3E修复noop
分支，不代表完整安全合同成立。撤回“等待期间无任务可做”：可推进主线静态审查、设计和合成测试。
G1须两层A/B、五路线、两噪声、强度/种子预算、完整传递链、Top30重合/Jaccard、NOT_FOR_ADMISSION
旁路、公开因子先验、误放率置信上界及依赖性说明，g1-v1不变。含未来信息合成校准禁止流入准入；
正控通过不证明历史拒绝全部正确，单次噪声拒绝不证明门可靠；未跟踪底稿不等于冻结合同。
Style须市场β、规模、价值/盈利、反转、流动性、波动、行业，双臂及转换器暴露差，绑定合法
clean_lgbm_control_v1。此前主线误输出paper效果行及Astra检索过宽已披露，不复述数值；后续
统一日期/状态/身份/哈希白名单，禁止完整paper行和结果JSON进入诊断输出。
本次只记录，未修复、运行预检、消费scope、读取密钥或效果、改变生产。自然账本及未跟踪文件保留。
