# TS-v5 首次 DeepSeek 机制研究 scope 提案

日期：2026-08-13（UTC+8）

状态：`AWAITING_EXPLICIT_USER_APPROVAL`

## 提案

首次批次固定为六类机制各1个独立候选、再各1个同机制反方改版，目标恰好12个完成响应，串行运行，
费用硬上限0.50美元。API完成但JSON/合同失败的响应仍计数，不递补；明确未形成完成响应的429/500/
503或连接失败才允许在每个席位最多3次传输尝试，读取超时等计费不确定状态立即停止整批。

本批只做非权威机制研究，不读取行情、收益或发现指标。允许发送治理约束、候选Schema、公开方法摘要、
v3/v4匿名失败计数、机制身份；第二阶段只发送同机制合法父候选，或父响应失败时的哈希和失败类别。
禁止发送证券、原始行情、持仓/订单/信号、任何效果指标、封存/前瞻/生产结果、路径或凭据。

官方资料于2026-08-13复核：`deepseek-v4-pro`、thinking high、JSON Output；缓存未命中输入0.435美元/
百万token，输出0.87美元/百万token。按每席位最多16,000输入token和1,800输出token计算，12席位全
缓存未命中最坏估算0.102312美元，0.50美元作为额外硬熔断，不代表计划消费。

机器scope为`config/ts_v5_llm_research_scope_v1.yaml`，SHA-256为
`9947e1bebc10d5da32df63ff462a8c8e9403a12986dbfef0a891f69956325a88`。当前`execution_authorized=false`、
`deepseek_api_called=false`。用户批准机器scope哈希后，才允许冻结执行release、补齐一次性批次协调器、
构建短命Docker镜像并运行；仍不授权行情/收益、参数搜索、回测、模拟仓、Web或生产。
