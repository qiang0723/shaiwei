# TS v3 数据契约恢复 R2 操作化附录 A1（结果前冻结）

日期：2026-08-12（UTC+8）

状态：`FROZEN_BEFORE_REAL_DATA_PROFILE`

机器真身：`config/ts_v3_data_gate_recovery_r2_addendum_v1.yaml`

## 价格谱系

“上一根有效证券日线高点”只由去重日线和同日复权因子构造，不能因为上一交易日缺少市值、行业归属
或当日为 ST 就被向后跳过。候选观察日自身仍必须同时具有唯一日线、复权、市值、PIT 申万一级行业
且非 ST。这样“恢复突破”的价格参照不会被过滤条件悄悄改写。

## Alpha158 本阶段只查键

TS-1A-R2 只能从冻结 OOS 缓存读取 `ts_code` 与 `trade_date`，用于候选事件键覆盖和重复键门；禁止读取
`baseline_score`，也禁止在本阶段做排序。原策略规划所说的“Alpha158 排名”必须等数据合同通过后另立
结果前协议，不能借数据画像偷看分数分布或证券名次。

## 指数响应与提交

两个请求各自返回的日期集合必须精确等于请求范围内 SSE 官方开市日集合；两份响应都完成代码、字段、
日期、唯一键、OHLC 和行数校验后，才允许写入第一份不可变 raw batch。提交后还必须核对既有历史加
新增批次共同覆盖三条指数的冻结范围，缺一天即失败关闭。

本附录不改变任何阈值、尝试数或研究授权，也不读取候选后的结果。
