# TS-v5 R3G-0 六机制可执行语义工程验收

日期：2026-08-13（UTC+8）

权威裁决：`GO_R3G_DENSITY_SCOPE_PROPOSAL_ONLY`

策略效果：`NOT_EVALUATED`

生产授权：`none`

## 结论

R3F 的六份合法 proposal 已被重新编译、登记并绑定到唯一的结果盲状态机语义。六种机制均通过正常合成执行路径，16 类对抗路径全部按冻结规则失败关闭或得到唯一结果；431 个有效参数组合能够确定性生成且无候选内重复。

本节点没有读取行情、证券、事件密度、收益、Alpha158 分数、验证集或锁定测试集；没有调用 DeepSeek、外网、模型或回测；没有修改模拟仓、Web、scheduler 或生产。因此当前只允许另立无收益密度投影 scope，不能声称任何候选有效、不能直接读取效果或进入模拟仓。

## 结果前冻结与补正

- 主 scope SHA-256：`3a066c13eeefd9409c159973a5ae1aff63fb5fc92b8caa67275ae5790b1345bb`，提交 `c7da5ef` 先行推送。
- 价格/相对强度量纲加注 SHA-256：`945abec5a281bad3ff5d888a8dcb06eb416578a8190131d3850b332c6dabd1f0`，提交 `a12c208` 在实现前推送。
- 六机制确认公式加注 SHA-256：`26e856576b036abea1e96f7a3e57cc27a8ceb74aec31fec4b440a68e0b17b48f`，提交 `4f83994` 在实现前推送。
- 两份加注均未删除、修补或重新编译候选，也未改变参数与阈值；只消除跨量纲比较和枚举到公式的歧义。
- 实现提交 `21d25ad`，断网构建修正 `7cb4993`，非有限滞后特征补丁 `f0d1005`；均已推送。

R3F 六份回答、scope/release、两账本、主报告、独立审计、proposal v3 合同、编译器和原候选 validator 均以冻结 SHA-256 复核；R3F 28 文件证据树重新计算仍为 `2f61c511...645b`。

## 候选登记与参数预算

六候选依次为：波动自适应回调、周结构分位、突破回踩、均线恢复、收缩扩张、行业相对强度回调。六个 fingerprint 和 semantic signature 与 R3F 完全一致且互不重复。

编译器给出的最大搜索预算依次为 81/125/81/81/32/81。整数槽去重后，有效参数点依次为 81/75/81/81/32/81，共 431 点；所有点只在合成层验证可生成和唯一，没有在真实数据上搜索，也没有读取效果。

候选登记产物只保存候选编号、机制、枚举、参数、必需字段和哈希，不保存 LLM 假设、经济解释、变更摘要、证伪自由文本、content 或 reasoning。

尝试记忆继续分账：累计 LLM 候选生成 28 次；TS-v5 策略效果尝试仍为 0。未来 431 个无收益密度画像须披露为设计搜索；真正读取效果的所有点（最多 18 个）无论成败，都计入同一效果试验族。

## 状态机与对抗验收

- 六机制正常路径全部到达 `EXECUTED`。
- 16 类对抗全部 PASS：未来结果字段、缺失字段、当前 bar 偷看、额外参数、非有限滞后成交额、同日取消优先、等待超时、候选特定流动性取消、终态重复 episode、确认连续性重置、下一开盘高于参考线取消/相等允许、价格参考量纲不匹配、相对强度开盘时点同量纲观察、相对强度参考缺失失败关闭、停牌无证券 bar 时等待龄推进。
- 每只证券同时最多一个 episode；触发时冻结 reference/threshold/结构止损；收盘确认、下一合法开盘执行；执行失败不在同一 episode 重试。
- 继承 TS-v3 月/周结构、大盘/板块、PIT 行业/市值、流动性和可交易资格；Alpha158 值与排名未进入状态机。

## 镜像、证据与验证

- 最终 Git：`f0d1005099645cd711cb0185fef18f647bd4da2c`，与 `origin/main` 一致。
- R3G 镜像使用已验收 R3F 本地镜像 `f3ccf7df...6efd` 作为固定运行时底座，`docker build --network=none`，没有依赖下载。
- 最终镜像：`sha256:34da009de6950276b11915127efadec07f3d3dfc6840ba035c89c3ab9b87c5e6`。
- 镜像内代码快照：`8e068eea94a882931ab6a38a32e318571a01cc20027399f8bd6b4c365f9dd308`。
- 候选 registry SHA-256：`9e51a58e080676d52797d551d3cd0e4fbcde3715655303c2cd0c04ac8846eaf4`。
- 工程报告 SHA-256：`e2fe2e119929d3c3c409a71cbddbc84c2e74212a96070bd871222a7bc7d5fc6a`。
- 独立审计 SHA-256：`a9f32f965544fd74e706d17901abb3811f199a1b53bf88cfec2de9a1cf435579`；13 项检查全 PASS。
- 工程 payload SHA-256：`6a4d5bcd962cdb4c542580ab977e4be92b63385ffa1a0ae29e42f7b6a2b8707b`。
- 最终主入口二次断网复跑，三份证据逐字节哈希完全不变。
- 专项测试 8 PASS；provenance+专项 13 PASS；架构门 13 PASS；实现前全仓回归 1,292 PASS、17 条既有 warning；Ruff、compileall、Compose config、diff-check 与凭据形态扫描 PASS。
- 五个生产模块行数 170/106/280/251/299，均不超过本专项 300 行目标；无新依赖、服务、公共账本 schema 或循环依赖。

第一次断网构建因 BuildKit pip 缓存缺 `annotated-types` 而失败，未开放网络。随后新增专用 Dockerfile，复用本机已验收固定底座并重算 release manifest；第一次通过镜像形成的三份 provisional R3G 报告在最终补丁后仅限本节点输出目录内显式清除并重建，上游数据、账本和证据均未删除或改写。

## 生产隔离与下一节点

scheduler 施工前后保持同一容器 `183b8c6c5edd`、镜像 `sha256:722f63de...13b76`、创建时间 2026-08-03 17:39:34（UTC+8），持续 `healthy`，未重启或替换。

下一合法节点为 `R3G-1` 结果盲密度投影：另立并先行推送 release scope 后，才可在固定 2019—2021 发现窗对 431 个参数点只计算合法事件数、事件日数和年度分布；不得读取入场后收益、验证/锁定测试、Alpha158 分数、模型、回测、模拟仓或生产。密度门通过后仍须先闭合 `CSI_H00906_TOTAL_RETURN` 可审计谱系，才可另立未来效果协议。
