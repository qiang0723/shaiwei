# M3-1 科创板三自建池价量因子研究预执行门验收

日期：2026-08-02（UTC+8）  
裁决：`GO_M3_1_PREEXECUTION_ONLY`  
策略状态：`NOT_EVALUATED`  
生产授权：`none`

## 裁决含义

M3-1 已把全科创板、中盘和小盘三个 `CUSTOM_RULE_BASED` PIT 研究池的下一批价量因子研究，收敛为
一套可复核、可失败关闭、可确定性复跑的预执行工程合同。当前 GO 只证明真实批次可以另立独立 live
release；不授权本次立即调用 DeepSeek，也不代表发现了有效因子，更不授权模型、回测、G1、信号或生产。

结果前协议提交 `f37ed4aa5b03e87488178ba6c5c56d0293e80885`，预执行实现提交
`f271376a558d029145594ef9c54f8a3c5e0d2941`，两者均已在真实候选、真实因子结果和封存结果出现前
推送到 `origin/main`。

## 冻结研究边界

- 唯一发现期为 2021-01-04 至 2022-12-15，共 474 个交易日；末条 10 日标签在 2022-12-30
  成熟。2023-01-03 至 2025-12-31 共 727 个交易日仍保持封存；2026 年至 7 月只作为未来近期/
  压力证据。
- 同一候选只能有一个规范 AST，并在三个池使用完全相同定义；方向只由全市场发现期锚定，中盘和
  小盘不得各自翻向。
- 下一批最多恰好 24 个完成响应，形成 72 个跨池评价单元，但只增加 24 次相关研究尝试；与既有
  GP 166 次、D1 40 次、M1 40 次合并后，多重检验有效 `N=270`。失败、重复和语义拒绝均计数且
  不补位。
- 未来批次固定使用受限 OHLCV/VWAP DSL，先过自由文本语义门，再过 AST、PIT、shift、覆盖、
  跨池同向、稳定性、成本容量、压力期和多重检验门。新股票池或新定义必须另立协议。
- 按 2026-08-02 冻结的官方 DeepSeek 价目和最大 token 上限，未来 24 次最坏估算为 0.33408 USD，
  独立 live 批次硬熔断 0.50 USD；未使用预算不自动授权其他批次。

## 输入真身与只读数据门

| 对象 | 结果 |
|---|---|
| M3-0 脱敏 manifest | `cf8cb88e…52638`，裁决仍为 `GO_CUSTOM_PIT_DATA_RULE_GATE_ONLY` |
| 三池逐日成员真身 | 779,271 行，SHA-256 `1983169e…5101` |
| 预执行输入快照 | SHA-256 `cfeb1c3c…ea9` |
| 发现期全市场 | 73,839 成员日 |
| 发现期中盘 | 24,676 成员日 |
| 发现期小盘 | 24,387 成员日 |
| 成员结构 | 子池均为全市场子集，中盘/小盘互斥，`.BJ=0` |
| 封存边界 | 只核对交易日计数 727；未读取或计算任何封存因子结果 |

## 纯 fixture 工程门

- 严格加载并核对上游协议、manifest、成员数据、质量报告和独立复核哈希；输入身份不符即失败。
- 合规候选通过语义门；包含“子池单独翻向”的对抗候选在进入 DSL 和数据评价前被拒绝。
- 两个完全 synthetic 候选完成受限 DSL 解析、最大回看审计、PIT/shift 哨兵、三池同定义评价、
  全市场方向锚定、跨池最弱值排序和机械 Top2。
- 三池评价单元与生成尝试分开计数：24 个响应映射 72 个评价单元，相关域 N 固定为 270。
- 预执行报告明确记录 `provider_calls=0`、`api_key_read=false`、`real_candidate_count=0`、
  `real_factor_results_inspected=false`、`model_or_portfolio_run=false`。

## 确定性、失败留痕与发布身份

最终断网只读镜像为
`sha256:83e5da58602c45b319ae1606fb42327fa9128b2bdfbfb2f87cf4e2c186362c30`，镜像内发布提交为
`f271376a558d029145594ef9c54f8a3c5e0d2941`，代码快照为
`5b9b99ad60621d9cf0eccaea9b501bb7fa2d12c9146364ad3547e7749ba12118`。

一次未注入发布提交号的临时镜像运行在 `git_head()` 处 fail closed；镜像没有 `.git`，因此没有用
当前目录猜测发布身份。随后按冻结实现提交显式重建，正式双跑均返回
`GO_M3_1_PREEXECUTION_ONLY`，报告 SHA-256 均为
`490552c3e922c065ac6c60eebe44b5c042302fb64553fe02b42dc66a96fabb4b`。失败过程没有供应商调用、
密钥读取、真实候选或结果写入。

## 工程验证与生产隔离

- M3-1 专项：15 PASS；只读根拒绝 pytest 缓存写入的提示符合容器边界。Docker 全仓：458 PASS，
  只有 1 条既有第三方弃用 warning。
- Ruff、compileall、pip check、Compose 解析、diff-check 和凭据模式扫描均 PASS。
- 新生产模块均不超过 400 行；数据合同、评价编排、提示合同和 CLI 分文件实现。
- M3-1 容器断网、非 root、只读根、无端口、无 Docker socket、无 `.env`，数据与上游证据只读。
- 生产 scheduler 始终保持容器 `fd8e96152b53…`、镜像 `sha256:de87ec74…0261`、创建时间
  `2026-07-24 20:25:27 +0800`，最终 healthy，未重启。

全仓回归最初直接放入最窄发布镜像时，分别因镜像不包含 `tools/.git/ledger/web-ui` 和嵌入的 release
manifest 不适用于挂载中的开发工作树而在收集/身份门失败。最终验收使用断网、无特权的一次性开发态
容器挂载完整项目，以无密钥 `.env.example` 覆盖 `.env`，并显式关闭发布镜像 manifest 模式后
458/458 PASS；没有为测试扩大 M3-1 运行服务的权限或读取项目密钥。

## 下一步

如继续，应另立 M3-2 真实发现批执行 release：绑定本验收的协议、实现、输入、镜像和报告哈希，
显式授权恰好 24 个完成响应及 0.50 USD 批次熔断，写入独立追加式尝试/传输账本。M3-2 仍只能读
发现期并机械锁定最多 Top2；验证窗、G1、模型、组合、信号和生产必须继续保持封存，另立目标。
