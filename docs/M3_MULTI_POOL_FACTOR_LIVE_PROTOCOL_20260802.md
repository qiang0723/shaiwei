# M3-2 三自建池价量因子发现批执行冻结

日期：2026-08-02（UTC+8）

状态：`M3_2_RESULT_BEFORE_EXECUTION_FROZEN`

执行版本：`m3-star-three-pool-price-volume-v1-batch-001`

## 1. 本阶段唯一目标

在 M3-1 已冻结的科创板全市场、中盘、小盘三个 `CUSTOM_RULE_BASED` PIT 池上，取得恰好 24 个
DeepSeek 完成响应；每个响应只允许一条受限价量 DSL，使用同一个规范 AST 在三个池的发现期评价。
全部 24 次结束后按冻结顺序机械锁定最多 Top2。本阶段不是策略回测、因子准入或生产接入。

## 2. 结果前补足的数据口径

M3-1 已冻结 OHLCV/VWAP、标签、中性化和研究时钟，但没有把复权链及 PIT 行业数据的物理批次身份
逐项写入执行 release。M3-2 在任何真实候选产生前补足，不改写 M3-1：

- 价格和标签统一使用 `sanitize_adj_factors` 后由 `transform_market_data` 生成的窗口首日基准后复权
  OHLC；成交量与价格按同一复权链调整，VWAP 仍按冻结量纲生成；禁止混入未复权价格或标签。
- PIT 行业只使用 `index_member_all` 的 `in_date/out_date` 区间，市值使用同日 `total_mv`；行业与
  对数市值仅用于中性化，不进入候选表达式。
- 发现信号期仍为 2021-01-04 至 2022-12-15，共 474 个交易日；为 50 日最大回看和 T+11 开盘
  标签成熟，物理行情窗固定为 2020-10-23 至 2022-12-30。
- 三池发现期共有 321 个历史证券、73,839 条全市场 PIT 暴露；北交所后缀为 0。

六类不可变输入的批次集合哈希、读取行数、M3-0 成员真身和市场变换代码哈希全部写入
`config/m3_multi_pool_factor_execution_v1.yaml`。断网只读 Docker 已复算得到输入快照
`90a8d377b40b...f1193`，provider 调用 0、密钥读取 false、真实因子结果未检查。

## 3. 响应、预算与多重检验

- 四主题固定顺序：趋势动量、反转均值回复、波动区间、流动性成交量；每主题 3 次独立提案和 3 次
  同主题有界变异，合计恰好 24 个完成响应。
- 空响应、截断、schema、语义、DSL、PIT/shift、重复或覆盖失败均计一次尝试且不补位；传输层在尚未
  形成完成响应时最多按原冻结合同有界重试。
- 既有 GP 166、D1 40、M1 40 共 246 次相关价量发现尝试继续计入；本批结束后有效 N=270。一个
  响应在三个池产生 3 个评价单元，但仍只算一次生成尝试。
- 用户总授权上限为 10 USD；本批最坏计划成本 0.33408 USD，独立硬熔断为 0.50 USD。未用预算不
  允许追加响应或新批次。

## 4. 评价和机械选择

- 每条规范 AST 在三个池保持完全相同定义；因子方向只用全市场发现期均值 RankIC 的符号冻结，
  子池不得独立翻向。
- 三池各自须达到覆盖率 90% 和至少 400 个有效日；只有三个池的定向 RankIC 均大于 0 才有资格。
- 排序固定为：三池最弱定向 RankIC、三池中位定向 RankIC、最弱覆盖率、表达式长度、全局序号。
  全部 24 次完成前不得选择；有效数不足 2 时终态必须是
  `PAUSE_INSUFFICIENT_CROSS_POOL_CANDIDATES`，不得补发或放宽门槛。
- 全局重复门同时读取原 Stage1、D1、M1 的规范表达式身份；重复候选计 N 并拒绝，不修改旧账本。

## 5. 运行和恢复边界

- 唯一 live 服务为一次性 Docker 容器：非 root、只读根、无端口、无 Docker socket，只把 M3 专属
  结果目录、两份专用账本和实验总账挂为可写；生产 scheduler 不在依赖图内。
- 只有 live 容器显式接收 `DEEPSEEK_API_KEY`，不加载 `.env`；客户端 `trust_env=false`，只允许
  `https://api.deepseek.com:443/chat/completions`。密钥不得进入命令输出、账本、产物、Git 或文档。
- 首个请求前写入不可变 live context，绑定 release、数据、代码快照、Git 提交和 TLS 证书哈希。
  同一不可变镜像/提交可在完整静态重哈希且无计费不确定性时从连续前缀续跑；代码或 release 改变
  必须先冻结结果前恢复补遗。已完成响应禁止替换或重发。
- 若 24 份完成响应已经存在而终端报告缺失，可只从既有静态证据组装报告，新增 provider 调用必须
  为 0。完整报告再次运行必须无密钥、无网络调用并保持所有证据哈希不变。

## 6. 明确禁止

本阶段不得读取 2023—2025 封存验证结果或 2026 压力/近期结果，不运行 G1、模型、组合、回测、
信号、模拟账户或生产任务；不修改中证800、科创50、Web、scheduler、模型和门禁；不访问观象或
项目外目录；不新增市场数据采集。M3-2 的 `GO` 只可能表示发现期 Top2 已机械锁定，不能表述为
因子有效、策略有效或生产授权。
