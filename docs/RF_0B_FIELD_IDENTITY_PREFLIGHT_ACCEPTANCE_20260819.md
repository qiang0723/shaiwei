# RF-0B 字段与身份结果盲预检验收（2026-08-19）

权威裁决：`BLOCKED_DATA`

策略有效性：`NOT_EVALUATED`

生产授权：`none`

## 1. 权威结论

唯一一次结果盲画像完成（R2 恢复 scope，首次失败在零数据读取处，审计入口再经 R3 恢复），独立
审计 8/8 PASS。字段质量门在冻结阈值处失败：**1,359,127 个 PIT 成员日中存在 5 个"无 bar 且无
停牌记录"的未解释缺失**，大于冻结上限 0。按协议本机制关闭，不重试、不降门槛。

## 2. 画像结果

字段链（2019-01-02—2025-12-31，PIT 中证800 成员日）：

| 指标 | 结果 | 冻结门 | 通过 |
|---|---:|---:|---|
| 成员日总数 | 1,359,127 | — | — |
| open 覆盖率 | 99.8665% | ≥99% | ✓ |
| 前收盘可连接率 | 99.8665% | ≥99% | ✓ |
| `.BJ` 行 | 0 | 0 | ✓ |
| 未解释无 bar 缺失 | **5** | ≤0 | ✗ |
| open 缺失/非正 | 0 | — | — |
| 重复记录 / 取值冲突 | 0 / 0 | — | — |
| 除权变动日 | 10,528（正常分类） | — | — |
| 一字涨停开盘代理 | 577（正常分类） | — | — |

身份注册表（全部构建成功，效果列零读取）：D1-v2 40 次尝试/38 条规范表达式/2 个重复簇；
M1 40 次/14 条；M3 24 次/24 条；G1 准入 29 行/29 个身份令牌；Alpha158 族 158 式。注册表
永久有效，可服务下一个机制家族的去重裁判。

## 3. 工程恢复留痕

1. 首次画像：`configure_store` 收到空临时根，在任何行情或账本读取**之前**失败（输出只有
   标记）；原 scope 关闭，R2 恢复记 0 消耗。
2. R2 画像与裁决完成；审计因载荷哈希复核把哈希字段本身纳入重算而 FAIL（审计
   `0c818ba3…9719` 留痕）；runner 未重跑，R3 仅审计恢复后 8/8 PASS
   （`e9eeab37…3f21`）。

## 4. 证据与身份

- 协议 SHA-256：`f98cecc7568b91d3ba30a1389127227d42d5646e85dd9acd69585fe70910a5b7`；
  R2 scope：`5b614441…9267`；R3 scope：`dd14317d…b604`。
- 运行 Git：`9935c128d3caea7b6e50220badbbc3b31d9f72a2`；镜像
  `sha256:a5606a241cb25bf0f3cbbd77cb6a97ce7f41b0bf7ecfb307eef39977642a3dba`。
- 画像 SHA-256：`d32a631b0790900e281145ae21afdd674074344e6c1803ce1c31d088a863377d`；
  manifest：`46d55f6a…8497`；注册表：`d3777d7a…56b0`；字段画像：`f7bc18e4…b761`；
  标记：`77abcc24…c645`。

## 5. 生产隔离

scheduler 原容器 `183b8c6c5edd` healthy、重启 0 次，全程未触碰。

## 6. 下一边界

- 本 scope 关闭；RF"隔夜跳空×日内反转"机制按冻结协议停止。
- 唯一合法的继续方式：另立一个**数据谱系诊断协议**，只解释那 5 个未解释成员日的来源
  （参照 M7 缺口谱系先例），不得借诊断降低已失败的门槛或夹带候选计算；是否立项由用户决定。
- 若选择下一个方法家族，身份注册表与字段画像机器可直接复用。
