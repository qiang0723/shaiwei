# 筛微仓库施工入口

本文件适用于整个仓库。任何施工或复核任务先读 `STATE.md`，再按任务读取
`docs/ROADMAP.md`、`docs/DATA_SPEC.md`、`docs/SENTINELS.md` 和 `docs/GATES.md`。涉及代码、公共
合同、数据源、策略臂、页面或部署的施工还必须遵守 `docs/ARCHITECTURE_CONSTITUTION.md`；其中稳定
可自动验证的阈值以 `config/architecture_constitution_v1.yaml` 为准。

## 当前冻结边界

- 锁竞争修复后当前代码版本的三次完整“信号 → 下一交易日开盘对账”已于 2026-07-22 通过；结论为核心任务 PASS、通知通道 WARN，证据见 `docs/P0_FORWARD_ACCEPTANCE_20260722.md`。
- 验收后飞书通知健壮性复核已于 2026-07-22 完成；P0.5 工程、四日 BACKFILL 和 2026-07-23 首个自然 `FORWARD` 均已 PASS，证据见 `docs/P05_FORWARD_ACCEPTANCE_20260723.md`。一天前瞻观察只完成工程闭环，不构成策略有效性结论。
- 2026-07-16 的首个真实信号是有效先导证据，但生成后发生了锁竞争处理修复，不计入当前代码版本三次连续稳定性计数。
- 后台主线顺序固定为：当前版本影子验收 → P0.5 模拟组合与前瞻绩效闭环 → 主力资金流增量验证 → 科创50独立策略臂。
- P0.5 只消费不可变信号和真实行情，不反向改变模型、信号、门禁或调仓规则；`model_baseline` 初始资金已冻结为人民币 50 万元，执行口径见 `docs/PAPER_EXECUTION_POLICY_V1.md`。
- P0.5 每轮完成后必须执行只读 `paper-verify`；它从追加式账本独立重放全部账户日并核对不可变产物，任何不一致必须失败并使 scheduler 进入 `degraded`，不得跳过或用最新快照覆盖旧证据。
- P0.5 每轮还必须执行 `paper-acceptance`：没有自然 `FORWARD` 时只能返回 `NOT_READY`；出现后必须同时通过代码/策略身份、Docker operator、新鲜度、`.BJ=0`、账本重放与飞书开始/完成投递，才能结束目标。
- 2026-07-26 用户授权新增 `model_top20` / 500,000 RMB 模拟比较账户；它只能将同一份不可变 Top30
  信号按原排名截取前 20 并等权到 5%，不得冒充新模型或 `n_drop2`。原 `model_baseline` 账户与历史
  必须字节不变；任何 Top20 结果只能在 `docs/PAPER_TOP20_PROTOCOL_20260726.md` 的冻结提交先行推送后
  生成，并须独立重放、幂等和通过发布隔离门。
- Top20 六日 BACKFILL 工程已 PASS。2026-07-26 Docker 身份复核的完整元数据输出曾包含环境变量；
  用户于 2026-07-27 知悉并接受不轮换 Tushare/飞书凭据的剩余风险，明确授权 Top20 模拟账户发布。
  后续 Docker 复核只准请求定向非敏感字段，禁止读取或输出完整容器元数据/环境变量；生产切换仍必须
  通过跨快照新交易日 readiness、不可变镜像、重放和前瞻门。见
  `docs/PAPER_TOP20_RELEASE_AUTHORIZATION_20260727.md`。
- Web 采用旁路线：P0.5 模拟仓只读查询已经稳定，v1.0 架构基线已冻结；Web 代码须另立目标，且不得修改模型、门禁、生产数据或延迟后台结果主线。
- D1-2A 真实调用前冻结已完成且原 `execution_authorized=false` / `$0.75` 记录不可回写。用户已于
  2026-07-25 追加批准 D1 总额度 `$10` 并指令开始下一任务；D1-2B 独立执行真身只授权首批恰好 40
  个完成响应和 `$1` 单批熔断，未用余额不得自动开新批。只有冻结提交推送、secret 不回显、TLS/数据/
  Docker/生产隔离门通过后才可读取唯一 DeepSeek secret 并串行调用；只读 2016-06-01—2018-12-31
  发现期，禁止 W1—W6、压力期、G1、前瞻和生产接入，D1-3 仍须再次授权。
- P1 六候选与 P2-2C 科创50基线均已权威 REJECT 并停止追加变体；P3-0 至 P3-4B 已 GO，Web 1.0
  七类本机只读页面完成。D1-3A 因自由文本语义合同违约权威 STOP，本批不得补发、递补或运行 G1。
- P4 科创100数据门已于 2026-07-26 权威NO-GO：源采集、首批100只和规则版本PASS，但当前公开
  官方归档无法闭合12期历史调入/调出成员对，PIT不可构造，策略保持NOT_EVALUATED。P4-1被阻断；
  只有取得带发布/版本证据的官方历史成员源并另立恢复协议后才能继续。不得用当前成分、ETF PCF或
  Tushare月末集合补造，不得构建qlib、训练、回测、选参、生成信号或接生产。
- D1 语义合同恢复工程门已于 2026-07-26 GO：未来新批必须在有效响应判定前同时通过结构字段与自由
  文本一致性、DSL/回看期和模糊即停门；该工程 GO 不回溯改变旧 STOP，也不授权新的 DeepSeek 调用、
  人工闸、W1—W6、G1 或生产。
- G8-1R 已完成监管 HTTP 主源 54 条不可覆盖证据；G8-2 管理人 HTTPS/费率谱系首轮权威
  `NO_GO_G8_2`，19 条通过/失败证据必须永久保留。当前禁止进入 G8-3、标 `VERIFIED`、构造产品
  总收益或运行 G8。若未来继续必须另立恢复协议，不得用 HTTP、CNINFO 或聚合平台替代管理人 HTTPS，
  也不得只修传输/解析后忽略成立日至今的费率有效期谱系。

## 不可协商纪律

- 只操作本仓库目录；其他文件夹或程序必须先获得用户批准。
- 服务部署和常驻任务使用 Docker；不得擅自改为裸机守护。
- 生产 scheduler 只准运行受控提升的不可变镜像，禁止整仓挂载开发工作树；发布、回滚和挂载白名单
  必须遵守 `docs/SCHEDULER_RELEASE_ISOLATION.md`。
- `data/raw/` 只增不改，账本只允许通过 `shaiwei.ledger.append_*` 追加。
- 北交所 `.BJ` 数据禁止进入当前股票池、日增量、模型或信号。
- 密钥只保存在被 Git 忽略的 `.env`；提交前必须运行脱敏检查，禁止提交原始/派生业务数据、运行日志
  或凭据。允许提交仓库已明确跟踪、仅含相对路径/哈希/行数/参数且通过脱敏检查的追加式审计 ledger；
  ledger 不能包含原始记录、绝对本地路径或秘密。
- 不修改冻结的 G0-G9/C0/G1/G8 判据；发现问题只记录并上报，不看结果调门槛。
- 一次只改变一个可归因变量；失败尝试和改判必须保留审计记录。

## 代码可维护性纪律

以下是架构宪法的施工摘要，不替代其开工门、ADR、迁移/回滚和例外规则。编码前先明确结果目标、
权威事实、复用/扩展边界、失败关闭和验收证据；代码改动交付前运行 `make architecture-check`。

- 新增生产模块遵守单一职责；数据读取、领域计算、查询投影、HTTP 传输和页面展示不得继续堆入同一
  文件。禁止创建无边界的 `utils`、`helpers` 或“万能服务”作为新的杂物间。
- 新生产文件以不超过 400 行为常态；401—600 行必须在施工前说明无法进一步按职责拆分的原因，
  超过 600 行禁止新增。已有超过 600 行的热点文件不要求无目标重写，但受机器清单冻结、不得增长或
  继续加入新的职责；相关需求优先把新增能力放入独立模块，并只在热点文件保留薄编排。
- 单函数以不超过 60 个逻辑行为常态；超过 100 个逻辑行、同时负责 I/O 与业务裁决、或出现三层以上
  嵌套时必须拆分。拆分依据是业务边界和可独立测试性，不为满足行数制造大量转发文件。
- 依赖方向必须单向且显式：核心领域代码不反向依赖 Docker、HTTP、Web 页面、`.env` 或生产配置；
  外部资源通过窄适配层进入。跨层复用前先检查是否会把不需要的依赖带入常驻服务。
- 新增公共契约、数据源、账户、策略臂或页面能力时，必须同时给出类型/Schema、失败关闭行为、独立
  测试和调用入口；禁止在前端硬编码业务数据，也禁止复制已有计算形成第二套口径。
- 合并前除正确性测试外，必须复核：文件职责是否扩大、是否出现重复逻辑/循环依赖、异常路径是否
  可测、旧接口是否被静默改变。需要跨模块的大重构须另立目标，不能夹带在功能提交中。
- 采用“棘轮式”治理：不因历史代码较大而停工，也不做频繁全盘返工；每次触碰热点文件都应至少不
  增加结构债，能安全抽离一项职责时再小步抽离，并保留回归证据。
- 项目必须在重大阶段边界进行受控整理与瘦身。首次完整检查点为 M6 系列关闭后、下一项重大策略池或
  Web 能力开工前；若不继续 M6，则在下一重大功能前触发。先做只读 `A1-0` 候选清单并由用户复核，
  再做 `A1-1` 小批删除/拆分；冻结协议、失败证据、不可变数据/账本、迁移、发布回滚和唯一历史复算
  实现不得作为普通废代码删除。具体规则见 `docs/CODEBASE_CONSOLIDATION_PLAN_20260807.md`。

## 每次交付

1. 先查看 `git status`，保留用户已有改动。
2. 运行与改动成比例的测试；代码提交至少通过 `make architecture-check`、`make test`、Ruff 和
   `git diff --check`。
3. 更新 `STATE.md` 的当前进度、待答点或作废记录。
4. 只暂存本次范围内文件，完成脱敏检查后再提交和推送。

## 备份

原始数据当前仍只在本机项目目录。用户已决定等远程服务器就绪后再实施异地备份；在获得目标地址和授权前，不创建或写入项目外备份。
