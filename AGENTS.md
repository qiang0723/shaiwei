# 筛微仓库施工入口

本文件适用于整个仓库。任何施工或复核任务先读 `STATE.md`，再按任务读取
`docs/ROADMAP.md`、`docs/DATA_SPEC.md`、`docs/SENTINELS.md` 和 `docs/GATES.md`。

## 当前冻结边界

- 前瞻影子正在做锁竞争修复后当前代码版本的三次完整“信号 → 下一交易日开盘对账”验收；正式完成最早为 2026-07-22。
- 验收完成前，不修改 `src/`、`config/`、模型、生产数据口径、门禁或调仓规则。只有真实运行故障阻断验收时，才允许先取证、上报并做最小修复。
- 2026-07-16 的首个真实信号是有效先导证据，但生成后发生了锁竞争处理修复，不计入当前代码版本三次连续稳定性计数。
- 后续顺序固定为：当前版本影子验收 → 主力资金流增量验证 → 科创50独立策略臂 → 专业只读 Web。

## 不可协商纪律

- 只操作本仓库目录；其他文件夹或程序必须先获得用户批准。
- 服务部署和常驻任务使用 Docker；不得擅自改为裸机守护。
- `data/raw/` 只增不改，账本只允许通过 `shaiwei.ledger.append_*` 追加。
- 北交所 `.BJ` 数据禁止进入当前股票池、日增量、模型或信号。
- 密钥只保存在被 Git 忽略的 `.env`；提交前必须运行脱敏检查，禁止提交数据、日志或凭据。
- 不修改冻结的 G0-G9/C0/G1/G8 判据；发现问题只记录并上报，不看结果调门槛。
- 一次只改变一个可归因变量；失败尝试和改判必须保留审计记录。

## 每次交付

1. 先查看 `git status`，保留用户已有改动。
2. 运行与改动成比例的测试；代码提交至少通过 `make test`、Ruff 和 `git diff --check`。
3. 更新 `STATE.md` 的当前进度、待答点或作废记录。
4. 只暂存本次范围内文件，完成脱敏检查后再提交和推送。

## 备份

原始数据当前仍只在本机项目目录。用户已决定等远程服务器就绪后再实施异地备份；在获得目标地址和授权前，不创建或写入项目外备份。
